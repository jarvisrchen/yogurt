---
phase: 01-design-system
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - web/package.json
  - web/pnpm-lock.yaml
  - web/src/router.tsx
  - web/src/router.test.tsx
  - web/src/routes/StyleGuide.tsx
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/App.test.tsx
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
  - DESIGN-05
  - DESIGN-06
must_haves:
  truths:
    - "React Router 7 is installed and mounted via RouterProvider in main.tsx"
    - "router.tsx exports `routes` and `router`, with `/` → App and `/style-guide` → StyleGuide"
    - "/style-guide renders every PRD §16 token group and every Phase-1 component primitive in a single scrollable page"
    - "Visiting `/` shows the new Phase-1-styled hello page composing Logo + Card + Pill + a Link to /style-guide"
    - "App.tsx links to /style-guide via React Router Link"
    - "Full Vitest suite passes (34 tests total)"
    - "Release build smoke: ./target/release/yogurt start serves `/` and `/style-guide` (SPA fallback) with HTTP 200"
    - "Phase 1 icon-system strategy demonstrated: inline SVG Logo + traffic-light SVG dots + Unicode glyphs in showcase (Lucide/Phosphor selection deferred to Phase 7)"
  artifacts:
    - path: "web/src/router.tsx"
      provides: "createBrowserRouter route map with `/` and `/style-guide`"
      contains: "createBrowserRouter"
      min_lines: 10
    - path: "web/src/router.test.tsx"
      provides: "createMemoryRouter smoke tests for both routes"
      contains: "createMemoryRouter"
    - path: "web/src/routes/StyleGuide.tsx"
      provides: "Full visual showcase of all design tokens + components (Color/Typography/Spacing/Radius/Elevation/Motion/Logo/Buttons/Pills/Cards/BrowserChrome)"
      contains: "Style guide"
      min_lines: 200
    - path: "web/src/main.tsx"
      provides: "RouterProvider mount, font side-effect imports preserved"
      contains: "RouterProvider"
    - path: "web/src/App.tsx"
      provides: "Phase-1 hello-world composing Logo + Card + Pill with Link to /style-guide"
      contains: "/style-guide"
    - path: "web/src/App.test.tsx"
      provides: "Updated assertions wrapping App in MemoryRouter"
      contains: "MemoryRouter"
    - path: "web/package.json"
      provides: "react-router@^7 dependency"
      contains: "react-router"
  key_links:
    - from: "web/src/main.tsx"
      to: "web/src/router.tsx"
      via: "imports RouterProvider + router and renders <RouterProvider router={router} />"
      pattern: "RouterProvider router=\\{router\\}"
    - from: "web/src/router.tsx"
      to: "web/src/App.tsx and web/src/routes/StyleGuide.tsx"
      via: "routes table maps path:'/' → App and path:'/style-guide' → StyleGuide"
      pattern: "path: \"/style-guide\""
    - from: "web/src/routes/StyleGuide.tsx"
      to: "web/src/components/{Logo,Button,Pill,Card,BrowserChrome}.tsx"
      via: "imports + composes every Phase-1 primitive to validate token wiring"
      pattern: "from \"../components/"
    - from: "web/src/App.tsx"
      to: "web/src/components/{Logo,Card,Pill}.tsx"
      via: "imports primitives from Plan 02"
      pattern: "from \"./components/"
---

<objective>
Mount React Router 7, wire the `/` hello-world to compose Plan-02 primitives, and stand up the full `/style-guide` showcase that renders every token (color/typography/spacing/radius/elevation/motion) and every component primitive (Logo at multiple sizes, all Button variants, every Pill tone, RecordingBadge, ProviderChip variants, Card with and without `active`, BrowserChrome wrapping a small mock). The showcase is the **phase gate** — a single long scrollable page that doubles as living documentation and the visual proof that the system is wired correctly.

Purpose: Lands the routing layer, ties together everything from Plans 01 and 02, and provides the eyeball-validation surface for DESIGN-01 through DESIGN-06. DESIGN-06 (icon system) is satisfied at the Phase-1 level by demonstrating the icon-strategy with inline SVGs and Unicode glyphs; full Lucide/Phosphor selection is deferred to Phase 7 per CONTEXT D-13.
Output: `router.tsx` + test, `routes/StyleGuide.tsx`, modified `main.tsx`, modified `App.tsx`, modified `App.test.tsx`, react-router@^7 in package.json. Final Vitest count: 34 passing tests (4 + 7 + 9 + 5 + 4 + 2 + 3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/phases/01-design-system/01-01-SUMMARY.md
@.planning/phases/01-design-system/01-02-SUMMARY.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md
@docs/PRD.md

@web/src/main.tsx
@web/src/App.tsx
@web/src/App.test.tsx
@web/src/index.css
@web/src/components/Logo.tsx
@web/src/components/Button.tsx
@web/src/components/Pill.tsx
@web/src/components/Card.tsx
@web/src/components/BrowserChrome.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-router 7 and wire createBrowserRouter route map</name>
  <files>web/package.json, web/pnpm-lock.yaml, web/src/router.tsx, web/src/router.test.tsx, web/src/routes/StyleGuide.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Implement Task 1.8 Steps 1-5 from the superpowers plan (lines 1099-1184).

    NOTE: React Router 7 renamed the package from `react-router-dom` to `react-router`. Use `react-router`, NOT `react-router-dom`.

    Step 1: From `web/`, run `pnpm add react-router@^7`. Confirm it appears under `dependencies` in `web/package.json`.

    Step 2 (TDD red): Create `web/src/router.test.tsx` with the 2-case suite from plan lines 1108-1126 + the local `vi.mock("./lib/api", …)` block from plan lines 1131-1135. The two cases:
      - createMemoryRouter with initialEntries=["/"] renders heading /yogurt/i (the App)
      - createMemoryRouter with initialEntries=["/style-guide"] renders heading /style guide/i

    Step 3: Run `pnpm --dir web test -- router` — expect failure (cannot resolve `./router` or `./routes/StyleGuide`).

    Step 4: Create the placeholder `web/src/routes/StyleGuide.tsx` per plan lines 1145-1161. Just the header + "Coming together in Task 1.9" line — minimal enough to make the router test pass. Task 2 below will expand it to the full showcase.

    Step 5: Create `web/src/router.tsx` per plan lines 1165-1183. Exports:
      - `export const routes: RouteObject[] = [{ path: "/", element: <App /> }, { path: "/style-guide", element: <StyleGuide /> }];`
      - `export const router = createBrowserRouter(routes);`

    Step 6: Run `pnpm --dir web test -- router` — expect 2 passed.
  </action>
  <verify>pnpm --dir web test -- router</verify>
  <acceptance_criteria>
    - web/package.json dependencies contains "react-router" (NOT "react-router-dom")
    - web/src/router.tsx contains `createBrowserRouter` and exports both `routes` and `router`
    - web/src/router.tsx maps path "/" to <App /> and path "/style-guide" to <StyleGuide />
    - web/src/router.test.tsx contains 2 `it(...)` blocks and uses `createMemoryRouter`
    - web/src/routes/StyleGuide.tsx exists with a "Style guide" heading rendered
    - `pnpm --dir web test -- router` reports 2 passing tests
  </acceptance_criteria>
  <done>Router + route map + placeholder StyleGuide are wired and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Mount RouterProvider in main.tsx and rewrite App.tsx to compose Phase-1 primitives</name>
  <files>web/src/main.tsx, web/src/App.tsx, web/src/App.test.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md, web/src/main.tsx, web/src/App.tsx, web/src/App.test.tsx</read_first>
  <action>
    Implement Task 1.8 Steps 6-9 from the superpowers plan (lines 1186-1330).

    Step 1: Modify `web/src/main.tsx` per plan lines 1190-1214 — keep the 10 @fontsource imports from Plan 01, swap the render call from `<App />` to `<RouterProvider router={router} />`. Imports needed: `RouterProvider` from `react-router` and `router` from `./router`.

    Step 2: Replace `web/src/App.tsx` body per plan lines 1220-1271. The new App:
      - Imports: `useEditor`, `EditorContent` from @tiptap/react; `StarterKit` from @tiptap/starter-kit; `useEffect`, `useState` from react; `Link` from react-router; `fetchHealth`, `HealthResponse` from ./lib/api; `Logo`, `Card`, `Pill` from ./components/*
      - Renders a `<main className="mx-auto max-w-2xl px-10 py-12 space-y-6">` with:
        * `<header>` containing `<Logo size={44} ariaLabel="Yogurt" />` + Instrument-Serif "yogurt" headline (44px) + "phase 1 design system · " + `<Link to="/style-guide">/style-guide →</Link>`
        * A `<Pill tone="matcha">server: <code className="font-mono">…</code></Pill>` driven by `health` state
        * A `<Card padding="md">` wrapping `<EditorContent editor={editor} />`

    Step 3: Replace `web/src/App.test.tsx` body per plan lines 1278-1313. The new test:
      - `vi.mock("./lib/api", () => ({ fetchHealth: vi.fn().mockResolvedValue({ status: "ok", service: "yogurt-server" }) }));`
      - `renderApp()` helper wraps `<App />` in `<MemoryRouter>` (from react-router)
      - Three cases: renders /yogurt/i heading; shows "yogurt-server ok" via waitFor; link role with name /style-guide/i has href="/style-guide"

    Step 4: Run `pnpm --dir web test` — expect ALL tests to pass: Logo (4), Button (7), Pill (9), Card (5), BrowserChrome (4), router (2), App (3) = **30 passing tests** total at this checkpoint. (Plan 03 Task 3 will add the full StyleGuide content but does not add new test cases.)

    Note: Plan §1.9 step 1 line 1330 says "34 passed" — that count is the same as the count at this checkpoint because Task 3 below adds no new tests, just expands the existing StyleGuide.tsx content. Re-verify in Task 4.
  </action>
  <verify>pnpm --dir web test</verify>
  <acceptance_criteria>
    - web/src/main.tsx imports `RouterProvider` from "react-router" and `router` from "./router"
    - web/src/main.tsx renders `<RouterProvider router={router} />` inside `<StrictMode>` (no longer renders `<App />` directly)
    - web/src/main.tsx still contains all 10 @fontsource imports above `./index.css`
    - web/src/App.tsx imports `Link` from "react-router" and `Logo`, `Card`, `Pill` from "./components/*"
    - web/src/App.tsx contains `<Link to="/style-guide"` (uses react-router Link, not anchor)
    - web/src/App.tsx renders `<Logo size={44} ariaLabel="Yogurt" />`, a Pill with tone="matcha", and a Card wrapping EditorContent
    - web/src/App.test.tsx imports `MemoryRouter` from "react-router" and wraps `<App />` in it for rendering
    - web/src/App.test.tsx contains 3 `it(...)` blocks and uses `vi.mock("./lib/api", …)`
    - `pnpm --dir web test` reports the full suite passing (≥30 cases at this checkpoint; final target 34 after Task 3 + Task 4)
  </acceptance_criteria>
  <done>RouterProvider mounted, App composes Phase-1 primitives, App.test.tsx rewritten — full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Expand /style-guide to the full token + component showcase</name>
  <files>web/src/routes/StyleGuide.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md, web/src/components/Logo.tsx, web/src/components/Button.tsx, web/src/components/Pill.tsx, web/src/components/Card.tsx, web/src/components/BrowserChrome.tsx</read_first>
  <action>
    Implement Task 1.9 from the superpowers plan (lines 1350-1726). Replace the placeholder StyleGuide.tsx body with the full showcase.

    Step 1: Replace `web/src/routes/StyleGuide.tsx` with the exact snippet at plan lines 1361-1691. The file structure:

    Imports: `Logo`, `Button`, `Pill`, `RecordingBadge`, `ProviderChip`, `Card`, `BrowserChrome` from `../components/*`.

    Three const-array constants at file top:
      - `COLORS` (12 entries): {token, hex, use} for paper/card/ink/grey/line/blue/blsoft/straw/strsoft/matcha/mtsoft/mut with exact hex values matching @theme
      - `RADII` (4 entries): {name, px, bg} for chip(6,bg-blsoft), button(9,bg-blsoft), card(14,bg-card+border), pill(999,bg-mtsoft)
      - `SHADOWS` (3 entries): {name, className, use} for shadow-card, shadow-pop (arbitrary), shadow-window (arbitrary)
      - `MOTION` (7 entries): {name, duration, use, className} for recpulse/blink/shimmer/wave/float/pop-up/slide-in-right — last two use empty className (rendered as muted "scripted" placeholder)

    Sections (each wrapped in a local `<Section title="" caption="">` helper component defined at file bottom):
      1. Header: Logo size=60 + Instrument-Serif "Yogurt style guide" (52px) + Hanken caption
      2. Color tokens — grid of 12 Card swatches with hex + token name + use
      3. Typography — Card showcasing Instrument Serif 52/38/30 + Hanken Grotesk 400/500/600/700/800 + JetBrains Mono 400/500/600 specimens (visibly distinct families)
      4. Spacing — Card with 4·8·12·16·24·32·48 px blueberry blocks
      5. Border radius — grid of 4 Cards showing each radius
      6. Elevation — 3 cards showing shadow-card / shadow-pop / shadow-window
      7. Motion — Card with 7 rows, each showing token name + duration + a live animation preview cell + use-case copy
      8. Logo — Card with Logo at sizes 19/24/32/44/60
      9. Buttons — Card with three rows: primary group (incl. disabled), secondary group (incl. disabled), ghost group (incl. disabled)
      10. Pills — Card with three rows: base tones (neutral/blue/matcha/straw), three RecordingBadge timers, ProviderChip variants (active/neutral/local)
      11. Cards — grid of 3 Cards with sm/md/lg padding (lg is `active`)
      12. BrowserChrome — wraps a Logo + welcome heading mockup at `url="localhost:7878/welcome"`
      13. Footer: PRD §16 attribution + last-updated date in mono

    The local `Section` helper renders Instrument-Serif h2 (30px) + Hanken caption + children below.

    Do NOT add new tests for the showcase itself — the components it composes are already covered by Plan 02. The verification surface is the eyeball check + the existing test suite still passing.

    Step 2: Run `pnpm --dir web test` — expect the same 34 tests passing (no new test files added; the test count of 34 in the superpowers plan refers to the full suite at this checkpoint: Logo 4 + Button 7 + Pill 9 + Card 5 + BrowserChrome 4 + router 2 + App 3 = 34).
  </action>
  <verify>pnpm --dir web test &amp;&amp; pnpm --dir web build</verify>
  <acceptance_criteria>
    - web/src/routes/StyleGuide.tsx imports Logo, Button, Pill, RecordingBadge, ProviderChip, Card, BrowserChrome from "../components/*"
    - web/src/routes/StyleGuide.tsx defines const arrays COLORS (12 entries), RADII (4), SHADOWS (3), MOTION (7)
    - web/src/routes/StyleGuide.tsx contains the exact hex values "#FBF7EF", "#211D18", "#5B4FC7", "#E07A66", "#5E9E73" inside its COLORS array
    - web/src/routes/StyleGuide.tsx renders sections including "Color tokens", "Typography", "Spacing", "Border radius", "Elevation", "Motion", "Logo", "Buttons", "Pills", "Cards", "BrowserChrome"
    - web/src/routes/StyleGuide.tsx contains `<BrowserChrome url="localhost:7878/welcome">`
    - web/src/routes/StyleGuide.tsx defines a local `Section` helper component
    - `pnpm --dir web test` reports 34 passing tests
    - `pnpm --dir web build` exits 0
  </acceptance_criteria>
  <done>Style guide expands to render every token and component — phase-gate eyeball surface ready.</done>
</task>

<task type="auto">
  <name>Task 4: Full Phase-1 acceptance — tests + build + release smoke + cargo hygiene</name>
  <files></files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Implement Task 1.10 from the superpowers plan (lines 1730-1800).

    Step 1: `pnpm --dir web test` — expect 34/34 passing.

    Step 2: `cargo test --workspace` — expect Phase-0 suite still passing (no Rust files modified by Phase 1).

    Step 3: `pnpm --dir web build` — expect clean. Confirm `web/dist/index.html` exists; `web/dist/assets/*.css` includes the @theme variable declarations; `web/dist/assets/*.woff2` count >= 10. Disk usage of `web/dist` ≈ under ~1.5 MB.

    Step 4: Dev-mode visual smoke:
      - Run `pnpm --dir web dev` in background → open `http://localhost:5173/` → confirm Logo + "yogurt" headline (Instrument Serif) + matcha health pill + TipTap inside a Card.
      - Click "/style-guide →" link → confirm navigation to `/style-guide` and the full showcase renders without console errors.
      - Stop Vite.

    Step 5: Release-mode smoke (proves SPA fallback works for the new route):
      ```bash
      pnpm --dir web build
      cargo build --release
      ./target/release/yogurt start --no-open &
      sleep 1
      curl -s -o /dev/null -w "%{http_code}\n" http://localhost:7878/
      curl -s -o /dev/null -w "%{http_code}\n" http://localhost:7878/style-guide
      curl -s -o /dev/null -w "%{http_code}\n" http://localhost:7878/api/health
      kill %1
      ```
    Expected: three `200` responses. `/style-guide` returns 200 via the Phase-0 SPA fallback in `assets.rs` — the server returns `index.html` and the client-side router handles the route.

    Step 6: `cargo fmt --all && cargo clippy --all-targets -- -D warnings` — clean (no Rust files were touched, but Phase-0 hygiene must still hold).

    Step 7: Do NOT push or tag — those are user-driven decisions.
  </action>
  <verify>pnpm --dir web test &amp;&amp; pnpm --dir web build &amp;&amp; cargo test --workspace &amp;&amp; cargo fmt --all --check &amp;&amp; cargo clippy --all-targets -- -D warnings</verify>
  <acceptance_criteria>
    - `pnpm --dir web test` reports 34 passing tests
    - `pnpm --dir web build` exits 0 with web/dist/index.html, web/dist/assets/*.css containing @theme vars, and ≥10 .woff2 files
    - `cargo test --workspace` exits 0 (Phase-0 suite unchanged)
    - `cargo fmt --all --check` and `cargo clippy --all-targets -- -D warnings` are clean
    - Release-mode smoke: GET /, GET /style-guide, GET /api/health all return 200 (the first two via SPA fallback + embedded assets)
  </acceptance_criteria>
  <done>Phase 1 acceptance gate passed: tests green, build clean, release binary serves the design system end-to-end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir web test` reports 34 passing test cases (Logo 4 + Button 7 + Pill 9 + Card 5 + BrowserChrome 4 + router 2 + App 3)
- [ ] `pnpm --dir web build` exits 0 with @theme CSS variables in the output and ≥10 .woff2 font files
- [ ] `cargo test --workspace` exits 0 (no Rust changes from Phase 1)
- [ ] `cargo fmt --all --check` and `cargo clippy --all-targets -- -D warnings` both pass
- [ ] Release binary smoke: GET `/`, GET `/style-guide`, GET `/api/health` all return 200
- [ ] Manual visual check at `/style-guide`: 12 color swatches, 3 type specimens (visibly distinct families), 4-base spacing ladder, 4 radius examples, 3 elevations, 5 visible motion animations (recpulse/blink/shimmer/wave/float), all Button + Pill + RecordingBadge + ProviderChip + Card + BrowserChrome compositions render without console errors
</verification>

<success_criteria>
- All 4 tasks completed
- All verification checks pass
- Phase 1 acceptance criteria from `docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md` lines 1806-1812 are all true
- All 6 DESIGN-* requirements are demonstrably satisfied:
  * DESIGN-01 — 12 color tokens rendered in the showcase Color Tokens section
  * DESIGN-02 — three font families rendered in the showcase Typography section
  * DESIGN-03 — spacing scale + radius scale + elevation scale all visible in their dedicated sections
  * DESIGN-04 — 7 motion tokens declared in @theme; recpulse/blink/shimmer/wave/float visibly animate in the showcase; pop-up + slide-in-right are scripted-only (used by Phase 3/6)
  * DESIGN-05 — Logo, Button (primary/secondary/ghost), Pill, RecordingBadge, ProviderChip, Card, BrowserChrome all rendered in their respective showcase sections; Tab group documented as deferred to Phase 4
  * DESIGN-06 — Phase-1 icon strategy demonstrated: inline SVGs (Logo, traffic-light dots) + Unicode glyphs (⌘K, ↳, +, →, ⌄, ✨, ⚙) in the showcase; full Lucide/Phosphor library selection deferred to Phase 7 per CONTEXT D-13
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-03-SUMMARY.md`
</output>
