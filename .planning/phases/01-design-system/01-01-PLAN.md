---
phase: 01-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/index.css
  - web/src/main.tsx
  - web/package.json
  - web/pnpm-lock.yaml
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-03
  - DESIGN-04
must_haves:
  truths:
    - "web/src/index.css contains an @theme block with all 12 PRD §16.2 color tokens"
    - "web/src/index.css declares the three font-family token chains (--font-serif/--font-sans/--font-mono)"
    - "web/src/index.css declares all 4 radius tokens, 4 shadow tokens, 2 easing tokens, and 7 --animate-* tokens"
    - "web/src/index.css declares matching @keyframes for recpulse, blink, shimmer, wave, float, pop-up, slide-in-right"
    - "pnpm --dir web build succeeds and the CSS chunk emits the design-token CSS variables"
    - "10 @fontsource/* font-weight CSS imports are present in web/src/main.tsx above the ./index.css import"
    - "Three @fontsource/* packages appear in web/package.json dependencies"
  artifacts:
    - path: "web/src/index.css"
      provides: "Full Tailwind 4 @theme token block + keyframes + base resets per PRD §16"
      contains: "@theme"
      min_lines: 80
    - path: "web/src/main.tsx"
      provides: "Font side-effect imports for Instrument Serif / Hanken Grotesk / JetBrains Mono"
      contains: "@fontsource/instrument-serif"
    - path: "web/package.json"
      provides: "@fontsource/* dependencies (instrument-serif, hanken-grotesk, jetbrains-mono)"
      contains: "@fontsource/hanken-grotesk"
  key_links:
    - from: "web/src/main.tsx"
      to: "web/src/index.css"
      via: "side-effect imports register @font-face rules BEFORE Tailwind/theme CSS loads"
      pattern: "@fontsource/.*\\.css"
    - from: "web/src/index.css @theme"
      to: "Tailwind utilities (bg-paper, text-ink, font-serif, rounded-card, etc.)"
      via: "Tailwind 4 CSS-first config emits utilities for each --token-*"
      pattern: "--color-(paper|ink|blue|matcha|straw)"
---

<objective>
Wire the Tailwind 4 `@theme` block with every design token from PRD §16 (colors, type families, radii, shadows, easings, animations + keyframes), then install and side-effect-import the three font families via `@fontsource/*`. This is the token foundation — every downstream component primitive (Plan 02) and the style-guide showcase (Plan 03) consume utilities derived from these tokens.

Purpose: DESIGN-01 (color), DESIGN-02 (typography), DESIGN-03 (spacing/radius/elevation), DESIGN-04 (motion) all land here. No components yet — just the variables and the fonts.
Output: `web/src/index.css` with full `@theme` block + keyframes + base resets; `web/src/main.tsx` with 10 font imports; `web/package.json` with 3 new dependencies.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-design-system/01-CONTEXT.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md
@docs/PRD.md

@web/src/index.css
@web/src/main.tsx
@web/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace web/src/index.css with Tailwind 4 @theme block + keyframes + base resets</name>
  <files>web/src/index.css</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md, web/src/index.css, docs/PRD.md</read_first>
  <action>
    Implement Task 1.1 from the superpowers plan (lines 79-225 of the plan file).

    Step 1: Confirm current file is the Phase-0 placeholder (`@import "tailwindcss";` + body styling with hardcoded `#FBF7EF` / `#211D18`).

    Step 2: Replace the entire `web/src/index.css` with the full content specified in Task 1.1 Step 2 of the superpowers plan. This includes:
      - `@import "tailwindcss";`
      - `@theme { … }` block with EXACTLY these tokens (values per PRD §16.2/16.3/16.4/16.5):
        * Colors (12): --color-paper #FBF7EF, --color-card #FFFFFF, --color-ink #211D18, --color-grey #A89F90, --color-line #EBE3D5, --color-blue #5B4FC7, --color-blsoft #ECE9FB, --color-straw #E07A66, --color-strsoft #FBE6E0, --color-matcha #5E9E73, --color-mtsoft #E7F0E8, --color-mut #8A8174
        * Fonts (3): --font-serif "Instrument Serif" ui-serif Georgia serif; --font-sans "Hanken Grotesk" ui-sans-serif system-ui -apple-system "Segoe UI" sans-serif; --font-mono "JetBrains Mono" ui-monospace "SFMono-Regular" Menlo monospace
        * Radii (4): --radius-chip 6px, --radius-button 9px, --radius-card 14px, --radius-pill 999px
        * Shadows (4): --shadow-card, --shadow-pop, --shadow-window, --shadow-button-blue with EXACT rgba values from the plan
        * Easings (2): --ease-pop cubic-bezier(0,0,0.2,1); --ease-slide cubic-bezier(0.2,0.7,0.2,1)
        * Animations (7): --animate-recpulse, --animate-blink, --animate-shimmer, --animate-wave, --animate-float, --animate-pop-up, --animate-slide-in-right (exact durations + easings per plan)
      - 7 @keyframes blocks (recpulse, blink, shimmer, wave, float, pop-up, slide-in-right) referenced by the --animate-* tokens. Exact keyframe content per the plan.
      - Base resets: `:root { color-scheme: light; }` and `html, body { … }` block that references `var(--color-paper)`, `var(--color-ink)`, `var(--font-sans)`, sets font-size 15px, line-height 1.5, and applies font-smoothing.

    Do NOT add extra tokens not listed. Do NOT inline-format keyframe declarations. Do NOT replace any token hex values with derivatives. Copy the snippet verbatim from the superpowers plan.

    Step 3: Run `pnpm --dir web build` from project root. Vite must succeed; CSS chunk should be present in dist.

    Step 4: Commit (the orchestrator will handle git wrapping — implementer should leave the file modified for verification).
  </action>
  <verify>pnpm --dir web build &amp;&amp; grep -c "@theme" web/src/index.css &amp;&amp; grep -c "color-blue" web/src/index.css</verify>
  <acceptance_criteria>
    - web/src/index.css contains the exact string "@theme {"
    - web/src/index.css contains "--color-paper:   #FBF7EF" and "--color-ink:     #211D18" and "--color-blue:    #5B4FC7" and "--color-matcha:  #5E9E73" and "--color-straw:   #E07A66"
    - web/src/index.css contains "--font-serif:" with "Instrument Serif" and "--font-sans:" with "Hanken Grotesk" and "--font-mono:" with "JetBrains Mono"
    - web/src/index.css contains "--radius-chip:   6px", "--radius-button: 9px", "--radius-card:   14px", "--radius-pill:   999px"
    - web/src/index.css contains "--shadow-card:", "--shadow-pop:", "--shadow-window:", "--shadow-button-blue:"
    - web/src/index.css contains "--ease-pop:" and "--ease-slide:" with the exact cubic-bezier values
    - web/src/index.css contains all 7 --animate-* tokens (recpulse, blink, shimmer, wave, float, pop-up, slide-in-right)
    - web/src/index.css contains "@keyframes recpulse", "@keyframes blink", "@keyframes shimmer", "@keyframes wave", "@keyframes float", "@keyframes pop-up", "@keyframes slide-in-right"
    - web/src/index.css base resets reference "var(--color-paper)" and "var(--font-sans)"
    - `pnpm --dir web build` exits 0
  </acceptance_criteria>
  <done>The @theme block, keyframes, and base resets are in place; Vite builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Install @fontsource/* packages and wire 10 font side-effect imports in main.tsx</name>
  <files>web/package.json, web/pnpm-lock.yaml, web/src/main.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md, web/src/main.tsx, web/package.json</read_first>
  <action>
    Implement Task 1.2 from the superpowers plan (lines 228-296 of the plan file).

    Step 1: From the `web/` directory, run `pnpm add @fontsource/instrument-serif @fontsource/hanken-grotesk @fontsource/jetbrains-mono`. Confirm all three appear under `dependencies` in `web/package.json` and the lockfile updates.

    Step 2: Modify `web/src/main.tsx` — prepend the EXACT 10 font CSS imports below ABOVE the existing `StrictMode` / `createRoot` / `App` / `./index.css` imports. Order: instrument-serif 400, instrument-serif 400-italic, hanken-grotesk 400/500/600/700/800, jetbrains-mono 400/500/600. These are side-effect imports — they register @font-face rules in the bundle.

    ```tsx
    import "@fontsource/instrument-serif/400.css";
    import "@fontsource/instrument-serif/400-italic.css";
    import "@fontsource/hanken-grotesk/400.css";
    import "@fontsource/hanken-grotesk/500.css";
    import "@fontsource/hanken-grotesk/600.css";
    import "@fontsource/hanken-grotesk/700.css";
    import "@fontsource/hanken-grotesk/800.css";
    import "@fontsource/jetbrains-mono/400.css";
    import "@fontsource/jetbrains-mono/500.css";
    import "@fontsource/jetbrains-mono/600.css";
    ```

    Keep the existing `StrictMode`, `createRoot`, `App`, and `./index.css` imports unchanged. Do NOT yet swap to RouterProvider — that happens in Plan 03 (Task 1.8). The current render call should still mount `<App />` directly.

    Step 3: Verify `pnpm --dir web build` succeeds and that production output includes woff2 files in `web/dist/assets/`.
  </action>
  <verify>pnpm --dir web build &amp;&amp; grep -c "@fontsource/" web/src/main.tsx &amp;&amp; ls web/dist/assets/*.woff2 | wc -l</verify>
  <acceptance_criteria>
    - web/package.json dependencies contain "@fontsource/instrument-serif", "@fontsource/hanken-grotesk", "@fontsource/jetbrains-mono"
    - web/pnpm-lock.yaml updated with the three packages
    - web/src/main.tsx contains all 10 import lines: "@fontsource/instrument-serif/400.css", "@fontsource/instrument-serif/400-italic.css", "@fontsource/hanken-grotesk/400.css", "@fontsource/hanken-grotesk/500.css", "@fontsource/hanken-grotesk/600.css", "@fontsource/hanken-grotesk/700.css", "@fontsource/hanken-grotesk/800.css", "@fontsource/jetbrains-mono/400.css", "@fontsource/jetbrains-mono/500.css", "@fontsource/jetbrains-mono/600.css"
    - The 10 @fontsource imports appear ABOVE the `import "./index.css";` line
    - `pnpm --dir web build` exits 0 and emits at least 10 .woff2 files into web/dist/assets/
  </acceptance_criteria>
  <done>Font packages installed, all 10 weight imports present in main.tsx, build emits the woff2 files.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke test — Phase 0 page still renders, fonts load, no console errors</name>
  <files></files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Run `pnpm --dir web test` to confirm the existing Phase-0 tests still pass against the rewritten index.css (the Phase-0 test asserts the page renders; switching token values from hardcoded hex to var references must not break it).

    If any Phase-0 test fails because it grepped against the old hardcoded color string, do NOT mutate it here — those App-level test rewrites belong to Plan 03 Task 1.8. Note any failures in the SUMMARY but do not block this plan; the foundational tokens + fonts are the deliverable.

    Then run `pnpm --dir web build` one more time end-to-end to confirm a clean production build with the tokens + fonts in place.
  </action>
  <verify>pnpm --dir web build</verify>
  <acceptance_criteria>
    - `pnpm --dir web build` exits 0
    - No "FontFace failed to load" or @theme syntax errors in the build output
    - web/dist/assets/ contains both a .css file (with @theme variable declarations) and at least 10 .woff2 files
  </acceptance_criteria>
  <done>Build is green; tokens and fonts are wired and ready for component primitives in Plan 02.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir web build` succeeds with zero errors
- [ ] `grep "@theme" web/src/index.css` returns a match
- [ ] All 12 color tokens, 3 font-family tokens, 4 radius tokens, 4 shadow tokens, 2 easing tokens, 7 animation tokens, and 7 @keyframes blocks are present in web/src/index.css
- [ ] All 10 `@fontsource/*` imports are present in web/src/main.tsx above `./index.css`
- [ ] web/package.json lists all three `@fontsource/*` packages under dependencies
- [ ] web/dist/assets/ contains the woff2 font files after a build
</verification>

<success_criteria>
- All 3 tasks completed
- All verification checks pass
- Token foundation is consumable by downstream plans: any future component can use `bg-paper`, `text-ink`, `font-serif`, `rounded-card`, `shadow-card`, `animate-recpulse`, etc.
- No Rust changes — `cargo test --workspace` still passes (verified by Phase 0 baseline; this plan does not touch backend)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-01-SUMMARY.md`
</output>
