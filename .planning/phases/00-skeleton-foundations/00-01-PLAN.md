---
phase: 00-skeleton-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - rust-toolchain.toml
  - .gitignore
  - crates/yogurt-cli/Cargo.toml
  - crates/yogurt-cli/src/main.rs
  - crates/yogurt-cli/src/commands/mod.rs
  - crates/yogurt-cli/src/commands/start.rs
  - crates/yogurt-cli/tests/cli.rs
  - crates/yogurt-server/Cargo.toml
  - crates/yogurt-server/build.rs
  - crates/yogurt-server/src/lib.rs
  - crates/yogurt-server/src/routes.rs
  - crates/yogurt-server/tests/health.rs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
user_setup: []

must_haves:
  truths:
    - "`cargo build --workspace` succeeds with zero warnings"
    - "`cargo run -p yogurt -- --help` prints help mentioning the `start` subcommand"
    - "`yogurt start --no-open` binds axum on 127.0.0.1:7878 and `GET /api/health` returns {\"status\":\"ok\",\"service\":\"yogurt-server\"}"
    - "`cargo test --workspace` passes including CLI integration tests and the server health integration test"
  artifacts:
    - path: "Cargo.toml"
      provides: "workspace root with resolver=2 and pinned workspace.dependencies"
      contains: 'resolver = "2"'
    - path: "rust-toolchain.toml"
      provides: "Rust 1.83 pin"
      contains: 'channel = "1.83"'
    - path: "crates/yogurt-cli/Cargo.toml"
      provides: "yogurt binary package"
      contains: 'name = "yogurt"'
    - path: "crates/yogurt-cli/src/main.rs"
      provides: "clap subcommand router with Start{port,no_open,dev}"
      contains: "Subcommand"
    - path: "crates/yogurt-cli/src/commands/start.rs"
      provides: "start command implementation calling yogurt_server::run"
      contains: "yogurt_server::run"
    - path: "crates/yogurt-server/src/lib.rs"
      provides: "pub async fn run(addr, mode) -> Result<()> and pub enum Mode { Dev, Release }"
      contains: "pub enum Mode"
    - path: "crates/yogurt-server/src/routes.rs"
      provides: "axum Router with GET /api/health"
      contains: "/api/health"
  key_links:
    - from: "crates/yogurt-cli/Cargo.toml"
      to: "crates/yogurt-server"
      via: "path dependency"
      pattern: 'yogurt-server\s*=\s*\{\s*path'
    - from: "crates/yogurt-cli/src/commands/start.rs"
      to: "yogurt_server::run"
      via: "library invocation"
      pattern: "yogurt_server::run"
---

<objective>
Stand up the Cargo workspace, the `yogurt` CLI binary with a `start` subcommand stub, and the `yogurt-server` library exposing an axum scaffold with a `/api/health` route. By the end of this plan, `cargo run -p yogurt -- start --no-open` boots an axum server on 127.0.0.1:7878 and the health endpoint returns JSON.

Purpose: Establish the binary/library split (FOUND-01) and the runnable CLI surface (FOUND-02) that every later phase will build on.
Output: Workspace + CLI + server crates with passing integration tests for `--help` and `/api/health`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-skeleton-foundations/00-CONTEXT.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-0-skeleton.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Cargo workspace, Rust toolchain pin, and .gitignore</name>
  <files>Cargo.toml, rust-toolchain.toml, .gitignore</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-0-skeleton.md, .gitignore</read_first>
  <action>Follow superpowers plan Task 0.1 Steps 1-5 exactly.

1. Read existing `.gitignore` to see what is already covered (the Vercel-style starter already has `.env`, `.env.local`, `.env*.local`, `.next/`, `dist/`, `build/`, `node_modules/`).
2. Create workspace root `Cargo.toml` with the EXACT content from Task 0.1 Step 2: `[workspace] resolver = "2"` with `members = ["crates/yogurt-cli", "crates/yogurt-server"]`, `[workspace.package]` with `version = "0.1.0"`, `edition = "2021"`, `authors = ["Jarvis Chen <3chenr@gmail.com>"]`, `license = "MIT"`, `repository = "https://github.com/jarvisrchen/yogurt"`, `rust-version = "1.83"`, and the full `[workspace.dependencies]` block (tokio 1.42 features=full, axum 0.8 features=macros, tower 0.5, tower-http 0.6 features=fs+trace, clap 4.5 features=derive, tracing 0.1, tracing-subscriber 0.3 features=env-filter, rust-embed 8.5 features=mime-guess, anyhow 1, serde 1 features=derive, serde_json 1, reqwest 0.12 default-features=false features=json+rustls-tls+stream, mime_guess 2, assert_cmd 2), plus `[profile.release]` with `lto = "thin"`, `codegen-units = 1`, `strip = true`.
3. Create `rust-toolchain.toml` with `[toolchain] channel = "1.83" components = ["rustfmt", "clippy"]`.
4. APPEND (do not overwrite) to `.gitignore`: a `# Rust` block with `/target/` and `**/*.rs.bk`, a `# pnpm + frontend` block with `.pnpm-store/`, and a `# Lavish artifacts` block with `.lavish/`. Do not re-add lines already present (node_modules, dist, .env, .env.local — already there).
5. Verify env protection: `git check-ignore -v .env.local` must report a match. If it does not, fix the gitignore before proceeding.
6. Run `cargo metadata --no-deps` — it MUST fail because the member crates do not exist yet; this confirms cargo is reading the workspace config. (Do not commit yet — the next tasks add the crates.)</action>
  <verify>git check-ignore -v .env.local && grep -q '^/target/$' .gitignore && grep -q 'channel = "1.83"' rust-toolchain.toml && grep -q 'resolver = "2"' Cargo.toml</verify>
  <acceptance_criteria>
    - `Cargo.toml` exists at repo root and contains `resolver = "2"`, `rust-version = "1.83"`, both workspace members, and a `[workspace.dependencies]` block with all 14 pinned crates listed in superpowers Task 0.1 Step 2
    - `rust-toolchain.toml` contains `channel = "1.83"` and `components = ["rustfmt", "clippy"]`
    - `.gitignore` contains `/target/`, `**/*.rs.bk`, `.pnpm-store/`, `.lavish/` AND `git check-ignore -v .env.local` reports a match
    - No regression to existing gitignore entries (still ignoring `node_modules`, `dist`, `.env`, `.env.local`)
  </acceptance_criteria>
  <done>Workspace skeleton exists; gitignore protects `.env.local`; `cargo metadata --no-deps` complains about missing member crates (expected — Task 2 adds them).</done>
</task>

<task type="auto">
  <name>Task 2: Create yogurt-cli crate with `--help` + Start subcommand stub</name>
  <files>crates/yogurt-cli/Cargo.toml, crates/yogurt-cli/src/main.rs, crates/yogurt-cli/src/commands/mod.rs, crates/yogurt-cli/src/commands/start.rs, crates/yogurt-cli/tests/cli.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-0-skeleton.md, Cargo.toml</read_first>
  <action>Follow superpowers plan Task 0.2 Steps 1-8 in TDD order.

1. Create `crates/yogurt-cli/Cargo.toml` with EXACT content from Task 0.2 Step 1: `name = "yogurt"`, `version.workspace = true` + other workspace inheritance, `description = "Local-first meeting copilot — Granola's UX, your machine."`, `[[bin]] name = "yogurt" path = "src/main.rs"`, dependencies pulling `tokio`, `clap`, `tracing`, `tracing-subscriber`, `anyhow` from `workspace = true`, dev-dependencies pulling `assert_cmd` from `workspace = true`.
2. Write the failing integration test FIRST at `crates/yogurt-cli/tests/cli.rs` per Task 0.2 Step 2: uses `assert_cmd::Command::cargo_bin("yogurt")`, asserts `--help` stdout contains both `yogurt` and `start`. Run `cargo test -p yogurt --test cli` to confirm it fails (binary doesn't exist yet).
3. Create `crates/yogurt-cli/src/commands/mod.rs` containing only `pub mod start;`.
4. Create `crates/yogurt-cli/src/commands/start.rs` with the stub from Task 0.2 Step 5: `pub async fn run() -> anyhow::Result<()>` printing `yogurt start: not yet wired (task 0.4)` and returning `Ok(())`. (Task 3 of THIS plan replaces this body — but keep the stub here so the `--help` test passes first, just like the superpowers TDD sequence.)
5. Create `crates/yogurt-cli/src/main.rs` per Task 0.2 Step 6: `mod commands;` declaration, clap `#[derive(Parser)]` `Cli` struct with `name = "yogurt"`, `about = "yogurt — local-first meeting copilot."`, `Cmd::Start` enum variant (no args yet — args are added in Task 3 below), `#[tokio::main]` async main initializing `tracing_subscriber::fmt` with `EnvFilter` default `yogurt=info,yogurt_server=info`, parsing `Cli`, dispatching to `commands::start::run().await`.
6. Re-run `cargo test -p yogurt --test cli` — it MUST pass (`it_prints_help` shows `ok`).
7. Manually verify: `cargo run -p yogurt -- --help` prints the about text and lists `start` as a subcommand; `cargo run -p yogurt -- start` prints `yogurt start: not yet wired (task 0.4)` and exits 0.</action>
  <verify>cargo test -p yogurt --test cli && cargo run -p yogurt -- --help 2>&1 | grep -q "start"</verify>
  <acceptance_criteria>
    - `crates/yogurt-cli/Cargo.toml` declares `name = "yogurt"` and `[[bin]] name = "yogurt"`
    - `crates/yogurt-cli/src/main.rs` uses `clap::{Parser, Subcommand}` and registers a `Start` subcommand
    - `crates/yogurt-cli/tests/cli.rs` contains `it_prints_help` and the test passes
    - `cargo run -p yogurt -- --help` exits 0 and stdout contains both `yogurt` and `start`
  </acceptance_criteria>
  <done>CLI binary builds, `--help` works, integration test green.</done>
</task>

<task type="auto">
  <name>Task 3: Create yogurt-server crate with axum scaffold + `/api/health`, then wire yogurt-cli `start` to call it</name>
  <files>crates/yogurt-server/Cargo.toml, crates/yogurt-server/build.rs, crates/yogurt-server/src/lib.rs, crates/yogurt-server/src/routes.rs, crates/yogurt-server/tests/health.rs, crates/yogurt-cli/Cargo.toml, crates/yogurt-cli/src/main.rs, crates/yogurt-cli/src/commands/start.rs, crates/yogurt-cli/tests/cli.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-0-skeleton.md, crates/yogurt-cli/src/main.rs, crates/yogurt-cli/src/commands/start.rs</read_first>
  <action>Combine superpowers Task 0.3 (server scaffold) and Task 0.4 (wire CLI → server).

PART A — Server scaffold (superpowers Task 0.3 Steps 1-7):

1. Create `crates/yogurt-server/Cargo.toml` per Task 0.3 Step 1 with workspace-inherited tokio/axum/tower/tower-http/tracing/anyhow/serde/serde_json/rust-embed/reqwest deps and dev-deps tokio + reqwest. Use `version.workspace = true` etc.
2. Create `crates/yogurt-server/build.rs` per Task 0.3 Step 2 — a one-line `cargo:rerun-if-changed=build.rs` noop. Real asset embedding wires in Plan 02.
3. Write the failing test FIRST at `crates/yogurt-server/tests/health.rs` per Task 0.3 Step 3: `#[tokio::test] async fn it_responds_to_health` spawns `yogurt_server::run` on `127.0.0.1:17878` in `Mode::Release`, sleeps 200ms, GETs `/api/health`, asserts `body["status"] == "ok"`.
4. Create `crates/yogurt-server/src/routes.rs` per Task 0.3 Step 5 — `pub fn router() -> Router` registering `GET /` returning a static string `"hello yogurt — phase 0 scaffold (web UI coming in task 0.5)"` and `GET /api/health` returning `Json(json!({"status":"ok","service":"yogurt-server"}))`. NOTE: the `GET /` route is transitional — Plan 02 deletes it when embedded asset fallback lands.
5. Create `crates/yogurt-server/src/lib.rs` per Task 0.3 Step 6: declare `mod routes;`, `#[derive(Debug, Clone, Copy)] pub enum Mode { Dev, Release }`, and `pub async fn run(addr: SocketAddr, mode: Mode) -> anyhow::Result<()>` which logs `tracing::info!(?addr, ?mode, "yogurt-server starting")`, binds `tokio::net::TcpListener`, and calls `axum::serve(listener, routes::router()).await`.
6. Run `cargo test -p yogurt-server --test health` — must show `it_responds_to_health ... ok`.

PART B — Wire CLI to server (superpowers Task 0.4 Steps 1-6):

7. Append to `crates/yogurt-cli/Cargo.toml` `[dependencies]`: `yogurt-server = { path = "../yogurt-server" }` and `open = "5"`. Append to `[dev-dependencies]`: `reqwest = { workspace = true }`. (Tokio is already a dependency with the `full` feature so it covers `tokio::process` + `tokio::time` for the new test.)
8. Append `it_starts_server_and_serves_health` to `crates/yogurt-cli/tests/cli.rs` per Task 0.4 Step 2 — `#[tokio::test(flavor = "multi_thread")]` spawning `Command::new(env!("CARGO_BIN_EXE_yogurt")).args(["start", "--port", "17879", "--no-open"])`, sleeping 400ms, GETting `http://127.0.0.1:17879/api/health`, asserting body contains `"\"status\":\"ok\""`, then killing the child.
9. Rewrite `Cmd::Start` in `crates/yogurt-cli/src/main.rs` to carry a `StartArgs` per Task 0.4 Step 4: clap-defined `StartArgs { port: u16 (default 7878), no_open: bool, dev: bool }` (long flags `--port`, `--no-open`, `--dev`). In `main`, convert the clap StartArgs to a `commands::start::StartArgs` mirror and pass to `commands::start::run`.
10. Replace `crates/yogurt-cli/src/commands/start.rs` body per Task 0.4 Step 4 (`Make commands::start::run look like:` block): declare `pub struct StartArgs { pub port: u16, pub no_open: bool, pub dev: bool }`; `pub async fn run(args: StartArgs) -> Result<()>` computes `addr = ([127,0,0,1], args.port).into()` (LOCALHOST-ONLY per D-11), selects `Mode::Dev` or `Mode::Release` from `args.dev`, formats `url = format!("http://127.0.0.1:{}", args.port)`, and if `!args.no_open` spawns a background `tokio::spawn` calling `open::that(&url_for_open)` with a `tracing::warn` on failure (so a browser-open failure cannot block server bind). Log `tracing::info!(%url, "yogurt is starting")` then `yogurt_server::run(addr, mode).await`.
11. Run `cargo test -p yogurt --test cli -- --nocapture` — both `it_prints_help` and `it_starts_server_and_serves_health` must pass.
12. Manual smoke: `cargo run -p yogurt -- start --no-open` boots; `curl localhost:7878/api/health` returns `{"status":"ok","service":"yogurt-server"}`; Ctrl-C stops cleanly.</action>
  <verify>cargo test --workspace && cargo build --workspace 2>&1 | grep -E "warning:" | wc -l | tr -d ' ' | grep -qE '^0$'</verify>
  <acceptance_criteria>
    - `crates/yogurt-server/src/lib.rs` declares `pub enum Mode { Dev, Release }` and `pub async fn run(addr: SocketAddr, mode: Mode) -> Result<()>`
    - `crates/yogurt-server/src/routes.rs` registers `/api/health` returning JSON containing `"status":"ok"` and `"service":"yogurt-server"`
    - `crates/yogurt-cli/Cargo.toml` `[dependencies]` contains both `yogurt-server = { path = "../yogurt-server" }` AND `open = "5"`
    - `crates/yogurt-cli/src/main.rs` clap `StartArgs` defines `--port` (default 7878), `--no-open`, and `--dev` flags
    - `crates/yogurt-cli/src/commands/start.rs` calls `yogurt_server::run(addr, mode).await` with addr = `([127, 0, 0, 1], args.port).into()` (localhost-only bind)
    - `cargo test --workspace` passes all tests including `it_prints_help`, `it_starts_server_and_serves_health`, and `it_responds_to_health`
    - `cargo build --workspace` produces zero warnings
  </acceptance_criteria>
  <done>`yogurt start --no-open` boots axum on 127.0.0.1:7878, health endpoint returns canonical JSON, full workspace builds clean.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build --workspace` succeeds with zero warnings
- [ ] `cargo test --workspace` passes (3 integration tests minimum: `it_prints_help`, `it_starts_server_and_serves_health`, `it_responds_to_health`)
- [ ] `cargo run -p yogurt -- --help` exits 0 with stdout mentioning `yogurt` and `start`
- [ ] `cargo run -p yogurt -- start --no-open` boots and `curl -s localhost:7878/api/health` returns `{"status":"ok","service":"yogurt-server"}`
- [ ] `git check-ignore -v .env.local` reports a match
- [ ] `cargo clippy --all-targets -- -D warnings` is clean
</verification>

<success_criteria>
- All 3 tasks completed
- Workspace + 2 crates exist and compile cleanly
- CLI binary `yogurt` exposes `start --port --no-open --dev` flags
- axum server binds to 127.0.0.1 (localhost-only) and serves the canonical health JSON
- FOUND-01 (workspace builds) and FOUND-02 (`yogurt start` launches axum on localhost:7878) are demonstrably met
</success_criteria>

<output>
After completion, create `.planning/phases/00-skeleton-foundations/00-01-SUMMARY.md`
</output>
