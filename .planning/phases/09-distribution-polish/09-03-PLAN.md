---
phase: 09-distribution-polish
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - crates/yogurt-cli/Cargo.toml
  - crates/yogurt-cli/src/main.rs
  - crates/yogurt-cli/src/commands/mod.rs
  - crates/yogurt-cli/src/commands/doctor.rs
  - crates/yogurt-cli/tests/doctor.rs
  - crates/yogurt-cli/build.rs
  - README.md
  - CONTRIBUTING.md
  - scripts/release-checklist.md
autonomous: false
requirements: [DIST-08, DIST-09, DIST-10]

must_haves:
  truths:
    - "`yogurt doctor` runs and prints diagnostic sections: rust, macOS, screen recording, db path, providers, stt, models"
    - "`yogurt doctor --json` produces valid JSON parseable by jq, containing `service: yogurt-doctor`"
    - "`yogurt doctor` exposes a TCC reset command (`tccutil reset ScreenCapture ai.yogurt.app`) and a port-conflict check (whether 7878 is in use) and a whisper-model re-download path"
    - "`yogurt --version` prints a clean version string from Cargo.toml"
    - "README documents all three install channels (Homebrew, cargo, direct), dev workflow, configuration, and threat model"
    - "A non-technical user on a fresh Mac can run `brew install jarvisrchen/yogurt/yogurt && yogurt start` and successfully record a first meeting end-to-end (DIST-09 phase gate)"
  artifacts:
    - path: "crates/yogurt-cli/src/commands/doctor.rs"
      provides: "Diagnostic dump command — rust, macOS, perms, providers, stt, models, TCC reset, port check, model re-download"
      contains: "pub async fn run"
      min_lines: 100
    - path: "crates/yogurt-cli/tests/doctor.rs"
      provides: "Integration tests for `yogurt doctor` and `yogurt doctor --json`"
      contains: "it_runs_doctor"
    - path: "README.md"
      provides: "Install matrix (brew/cargo/direct), first-run flow, architecture diagram, dev setup, privacy posture, license"
      contains: "brew install jarvisrchen/yogurt/yogurt"
      min_lines: 60
    - path: "scripts/release-checklist.md"
      provides: "Manual checklist for cutting v0.1.0 and future releases"
      contains: "dry-run"
  key_links:
    - from: "crates/yogurt-cli/src/main.rs"
      to: "crates/yogurt-cli/src/commands/doctor.rs"
      via: "Cmd::Doctor variant in clap enum dispatches to commands::doctor::run"
      pattern: "Cmd::Doctor"
    - from: "crates/yogurt-cli/src/commands/doctor.rs"
      to: "ai.yogurt.app"
      via: "TCC reset invokes `tccutil reset ScreenCapture ai.yogurt.app` (bundle ID from plan 09-01)"
      pattern: "ai\\.yogurt\\.app"
    - from: "README.md"
      to: "docs/PRD.md"
      via: "README references PRD §15 for privacy posture"
      pattern: "PRD"
---

<objective>
Ship the user-facing polish that turns the release pipeline (plans 09-01, 09-02) into something a non-technical user can actually install. Three deliverables:

1. **`yogurt doctor` subcommand** (DIST-08) — diagnostic dump (rust/macOS/perms/providers/stt/models), TCC reset (`tccutil reset ScreenCapture ai.yogurt.app`), port-conflict check, whisper-model re-download.
2. **README + CONTRIBUTING + release checklist** (DIST-10) — three install snippets, first-run flow, architecture diagram, dev setup, privacy posture.
3. **End-to-end install acceptance** (DIST-09) — the v1 ship gate: a non-technical user on a fresh Mac runs `brew install yogurt && yogurt start` and successfully records a first meeting. This is a `checkpoint:human-verify` task that gates phase completion.

Purpose: This is the v1 ship gate. If DIST-09 fails, Phase 9 is not done — full stop. Notarization (plans 09-01, 09-02) without a working install UX is half a product.
Output: Working `yogurt doctor`, polished README, and a verified end-to-end install path on a fresh macOS environment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-distribution-polish/09-CONTEXT.md
@.planning/phases/09-distribution-polish/09-01-SUMMARY.md
@.planning/phases/09-distribution-polish/09-02-SUMMARY.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-9-polish-and-distribution.md
@docs/PRD.md
@crates/yogurt-cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `yogurt doctor` subcommand with integration tests</name>
  <files>crates/yogurt-cli/Cargo.toml, crates/yogurt-cli/src/main.rs, crates/yogurt-cli/src/commands/mod.rs, crates/yogurt-cli/src/commands/doctor.rs, crates/yogurt-cli/tests/doctor.rs, crates/yogurt-cli/build.rs</files>
  <read_first>crates/yogurt-cli/src/main.rs, crates/yogurt-cli/src/commands/mod.rs, crates/yogurt-cli/Cargo.toml, docs/superpowers/plans/2026-06-25-yogurt-phase-9-polish-and-distribution.md</read_first>
  <action>
    Mirror Task 9.3 of the superpowers plan with three extensions specific to the ROADMAP §Phase 9 Success Criterion 4 (TCC reset, port-conflict, model re-download).

    **1. `crates/yogurt-cli/Cargo.toml`** — add to `[dependencies]`:
    ```toml
    sysinfo = "0.32"
    dirs = "5"
    serde = { workspace = true }
    serde_json = { workspace = true }
    ```
    Add to `[dev-dependencies]`:
    ```toml
    assert_cmd = "2"
    ```

    **2. `crates/yogurt-cli/build.rs`** — capture rustc version into env:
    ```rust
    // SPDX-License-Identifier: MIT
    use std::process::Command;
    fn main() {
        println!("cargo:rerun-if-changed=build.rs");
        let v = Command::new("rustc").arg("--version").output()
            .ok().and_then(|o| String::from_utf8(o.stdout).ok())
            .map(|s| s.trim().to_string())
            .unwrap_or_else(|| "unknown".to_string());
        println!("cargo:rustc-env=YOGURT_RUSTC_VERSION={v}");
    }
    ```

    **3. `crates/yogurt-cli/src/main.rs`** — extend the `Cmd` enum with:
    ```rust
    /// Print diagnostic info (rust, macOS, perms, providers, models) + repair actions.
    Doctor(DoctorArgs),
    ```
    Define `DoctorArgs`:
    ```rust
    #[derive(clap::Args, Debug)]
    struct DoctorArgs {
        /// Emit diagnostics as JSON.
        #[arg(long)]
        json: bool,
        /// Reset Screen Recording TCC permission for ai.yogurt.app (forces re-prompt).
        #[arg(long)]
        reset_screen_recording: bool,
        /// Check whether port 7878 (or --port) is in use.
        #[arg(long)]
        check_port: bool,
        /// Re-download a whisper.cpp model (e.g. small.en).
        #[arg(long, value_name = "MODEL")]
        redownload_model: Option<String>,
    }
    ```
    Wire dispatch: `Cmd::Doctor(args) => commands::doctor::run(args.into()).await`.

    **4. `crates/yogurt-cli/src/commands/mod.rs`** — add `pub mod doctor;`.

    **5. `crates/yogurt-cli/src/commands/doctor.rs`** — implement `pub async fn run(args: DoctorArgs) -> Result<()>` with branches:
    - `--reset-screen-recording`: invoke `Command::new("tccutil").args(["reset", "ScreenCapture", "ai.yogurt.app"])`. Print result. Bundle ID `ai.yogurt.app` is the SAME id pinned in plan 09-01's codesign + notarize steps — they must match for the reset to actually clear TCC state.
    - `--check-port`: bind to `127.0.0.1:7878` (or the configured port) via `std::net::TcpListener::bind`; if Err with `AddrInUse`, print "port 7878 is in use" + suggest `yogurt start --port 7879`; else print "port 7878 is free".
    - `--redownload-model <name>`: print "removing ~/.yogurt/models/ggml-<name>.bin (will re-download on next use)" and `fs::remove_file`. Re-download itself happens on next `yogurt start` (whisper.cpp model-download flow from Phase 8 handles it).
    - Default (no flags): collect the diagnostic Report (rust version from `YOGURT_RUSTC_VERSION` env, macOS version from `sysinfo`, screen recording status placeholder, db path = `~/.yogurt/db.sqlite`, db exists check, providers from `~/.yogurt/config.toml` scrape for `[providers.NAME]`, active stt from same TOML, models from `~/.yogurt/models/`).
    - `--json` branch: `println!("{}", serde_json::to_string_pretty(&report)?)`. JSON shape: `{ service: "yogurt-doctor", version, rust, macos, screen_recording, db_path, db_exists, providers: [name1, ...], stt, models: [...] }`. NEVER include API key VALUES — only provider names.
    - Default human output: section labels (`rust:`, `macOS:`, `screen recording:`, `db path:`, `db exists:`, `providers:`, `stt:`, `models:`, `config:`, `notes:`) + a closing line: "paste this output into any issue at https://github.com/jarvisrchen/yogurt/issues".

    **6. `crates/yogurt-cli/tests/doctor.rs`** — integration tests using `assert_cmd`:
    - `it_runs_doctor_and_prints_diagnostics`: runs `yogurt doctor`, asserts stdout contains each section label (`yogurt doctor`, `rust:`, `macos:`, `screen recording:`, `db path:`, `providers:`, `stt:`, `models:`).
    - `it_runs_doctor_with_json_flag`: runs `yogurt doctor --json`, parses stdout as JSON, asserts `service == "yogurt-doctor"` and `rust`/`macos` are strings.
    - `it_runs_doctor_check_port`: runs `yogurt doctor --check-port`, asserts stdout matches `port \d+ is (free|in use)`.

    Verify `--version` works: `cargo run -p yogurt -- --version` prints `yogurt <workspace-version>` (clap auto-derives this from Cargo.toml; no code change needed — verify only).
  </action>
  <verify>
    `cargo test -p yogurt --test doctor` passes all three tests. `cargo run -p yogurt -- doctor` prints all expected sections. `cargo run -p yogurt -- doctor --json | jq .service` returns `"yogurt-doctor"`. `cargo run -p yogurt -- --version` prints `yogurt <version>`.
  </verify>
  <acceptance_criteria>
    - File `crates/yogurt-cli/src/commands/doctor.rs` exists with `pub async fn run`
    - File `crates/yogurt-cli/tests/doctor.rs` exists with at least 3 tests
    - Doctor output contains all section labels listed above
    - `doctor --json` produces valid JSON parseable by jq
    - `doctor --reset-screen-recording` references `ai.yogurt.app` (matching plan 09-01 bundle ID)
    - `doctor --check-port` reports free or in-use status
    - `doctor --redownload-model <name>` removes `~/.yogurt/models/ggml-<name>.bin`
    - `cargo test -p yogurt --test doctor` passes
    - `cargo run -p yogurt -- --version` prints version cleanly
  </acceptance_criteria>
  <done>`yogurt doctor` ships with diagnostics + repair actions; tests pass; bundle ID matches notarization config.</done>
</task>

<task type="auto">
  <name>Task 2: Write README, CONTRIBUTING, and release-checklist docs</name>
  <files>README.md, CONTRIBUTING.md, scripts/release-checklist.md</files>
  <read_first>README.md, docs/superpowers/plans/2026-06-25-yogurt-phase-9-polish-and-distribution.md, docs/PRD.md</read_first>
  <action>
    Replace `README.md` content (or write fresh if minimal stub from Phase 0). Mirror Task 9.12 of the superpowers plan adapted for v1-with-notarization (no Gatekeeper bypass section since notarization makes that unnecessary):

    **README.md** sections:
    - Title + tagline: "Local-first, open-source meeting copilot for macOS. Granola's augmented-notes UX, your machine."
    - Badges: MIT license, Crates.io version, GitHub Release, CI status (use shields.io URLs).
    - Hero screenshot reference: `docs/screenshots/meeting.png` (placeholder if no screenshots committed yet).
    - 1-paragraph elevator pitch: mic + system audio, no bot, augmented black/grey notes, deep-links to transcript, BYO OpenAI-compatible LLM.
    - **Install** section with THREE subsections:
      - **Homebrew (recommended)**: `brew install jarvisrchen/yogurt/yogurt && yogurt start`.
      - **Cargo**: `cargo install yogurt && yogurt start`.
      - **Direct download**: curl-snippet for both architectures pointing at `https://github.com/jarvisrchen/yogurt/releases/latest/download/yogurt-{aarch64,x86_64}-apple-darwin.tar.gz`.
    - **First-run** (4 steps): start command, Screen Recording prompt, paste LLM key, record + transcribe.
    - **Architecture** ascii diagram (browser ↔ yogurt binary ↔ optional cloud STT/LLM).
    - **Run from source** snippet: `brew install rust pnpm`, `git clone`, `pnpm install`, two-terminal dev loop.
    - **Diagnostics**: `yogurt doctor`, `yogurt doctor --json`, `yogurt --version`. Mention `--reset-screen-recording`, `--check-port`, `--redownload-model` flags.
    - **Privacy posture** (links to PRD §15): audio never leaves machine unless cloud STT chosen, no phone-home / no telemetry / no Sentry, API keys in macOS Keychain, audio deleted post-transcription, all data local at `~/.yogurt/`.
    - **Threat model** subsection: localhost trust assumption (anyone with `localhost:7878` access on this Mac can read notes — same trust boundary as the Granola desktop app). Required by ROADMAP §Phase 9 Success Criterion 5.
    - **License**: MIT.

    **CONTRIBUTING.md** sections:
    - Dev environment setup (`brew install rust pnpm`, clone, install).
    - Two-terminal dev loop.
    - Running tests (cargo + pnpm). Mention insta snapshot review workflow if applicable.
    - Workspace layout (eight crates listed).
    - Code style: `cargo fmt`, `cargo clippy -D warnings`, Conventional Commits.
    - How to add a new STT provider (implement `SttEngine`) or LLM provider (preset card edit).
    - How to file issues (run `yogurt doctor --json` and paste — no keys leak).
    - License notice.

    **scripts/release-checklist.md** sections:
    - Mirror Appendix A of the superpowers plan: 12-step manual checklist for cutting v0.1.0 and beyond.
    - Include the publish hygiene rule (GitHub Release artifacts live BEFORE tap PR or `cargo publish`).
    - Include the smoke-test sequence for all three install channels (brew uninstall, brew tap + install, cargo install --force, direct curl + extract).

    Run `mkdir -p docs/screenshots` and `mkdir -p scripts`. If screenshots from `yogurt-app-design/project/screenshots/` exist, copy 3-4 in (library, meeting, settings, onboarding). If not, leave the README image references as-is — they will resolve once screenshots are committed.
  </action>
  <verify>`wc -l README.md` >= 60. `grep -c "brew install" README.md` >= 2. `grep -c "cargo install" README.md` >= 1. `grep "PRD" README.md` succeeds. File `scripts/release-checklist.md` exists.</verify>
  <acceptance_criteria>
    - README.md has Install section with all three channels
    - README.md has First-run, Architecture, Privacy posture, Threat model sections
    - README.md references `yogurt doctor` and its flags
    - CONTRIBUTING.md has dev setup, tests, workspace layout, code style
    - scripts/release-checklist.md exists with 12-step checklist
    - scripts/release-checklist.md mentions the publish hygiene rule
  </acceptance_criteria>
  <done>Docs ready for public release; the README is the front door for first-time users.</done>
</task>

<task type="auto">
  <name>Task 3: Commit polish; trigger one final release dry-run</name>
  <files>(none — git operations only)</files>
  <action>
    1. `git add crates/yogurt-cli/ README.md CONTRIBUTING.md scripts/release-checklist.md docs/screenshots/` (last path only if it exists).
    2. Commit: `git commit -m "feat(cli): add yogurt doctor + polish README/CONTRIBUTING/release-checklist"`.
    3. `git push origin main`.
    4. Trigger final dry-run: `gh workflow run release.yml -f dry-run=true && gh run watch`.
    5. Verify: dry-run succeeds, tarballs include the new `doctor` subcommand (extract one and run `./yogurt doctor` against it).
  </action>
  <verify>
    `gh run list --workflow=release.yml --limit=1 --json conclusion --jq '.[0].conclusion'` returns `success`. After downloading and extracting the dry-run tarball: `./yogurt doctor` prints diagnostic output; `./yogurt --version` prints version.
  </verify>
  <acceptance_criteria>
    - Commit lands on main
    - Most recent dry-run run conclusion is success
    - Dry-run tarball, when extracted, ships `yogurt doctor` and `yogurt --version`
  </acceptance_criteria>
  <done>Final dry-run includes doctor + docs; only the real tag remains.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Cut the v0.1.0 tag now and trigger the real release pipeline?</decision>
  <context>
    Tagging `v0.1.0` and pushing triggers the live release workflow: real GitHub Release with notarized tarballs, real PR opened against `jarvisrchen/homebrew-yogurt`, and real `cargo publish` to crates.io. ALL of these are PUBLIC and HARD TO REVERSE (`cargo yank` only hides a version; the SHA stays on crates.io forever).

    Before cutting the tag, verify:
    - Plan 09-01's `spctl -a -vv` Gatekeeper check passed on dry-run artifacts.
    - Plan 09-02's dry-run showed `tap` and `publish` as SKIPPED (proves the `if` guards work).
    - This plan's `yogurt doctor` integration tests pass.
    - The README install snippet is correct.
    - All four PRD §14 success criteria pass locally (cargo test, pnpm build, fmt/clippy, manual smoke).
  </context>
  <options>
    <option id="cut-tag-now">
      <name>Cut v0.1.0 now</name>
      <pros>Phase 9 ships v1 to the public; DIST-09 acceptance gate runs against the real release.</pros>
      <cons>Public exposure — any leftover bug ships. Hard to retract from crates.io. Homebrew formula PR will land in the sibling repo.</cons>
    </option>
    <option id="hold-tag">
      <name>Hold the tag — additional dry-run verification first</name>
      <pros>More confidence before public exposure.</pros>
      <cons>Phase 9 doesn't finish; the DIST-09 gate can't be tested without a real `brew install jarvisrchen/yogurt/yogurt` working.</cons>
    </option>
  </options>
  <resume-signal>Select: cut-tag-now or hold-tag</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Cut v0.1.0 — gated on the decision checkpoint above</name>
  <files>(none — git operations only)</files>
  <action>
    ONLY proceed if the previous checkpoint returned `cut-tag-now`. Otherwise SKIP this task and end the plan.

    1. Ensure `main` is green on CI: `gh run list --workflow=ci.yml --limit=1 --branch=main --json conclusion --jq '.[0].conclusion'` returns `success`.
    2. Update `CHANGELOG.md`: replace `## [0.1.0] — 2026-07-XX` with today's date.
    3. Commit: `git add CHANGELOG.md && git commit -m "docs: stamp CHANGELOG.md v0.1.0 release date"`.
    4. Push: `git push origin main`.
    5. Tag: `git tag -a v0.1.0 -m "yogurt v0.1.0 — first public release"`.
    6. Push tag: `git push origin v0.1.0`. This triggers the real release workflow.
    7. Watch: `gh run watch`. Four jobs (`build` matrix × 2, `release`, `tap`, `publish`) must all go green.
    8. Verify GitHub Release: `gh release view v0.1.0` shows both tarballs + `SHA256SUMS` attached.
    9. Verify tap PR: `gh pr list --repo jarvisrchen/homebrew-yogurt` shows the auto-PR. Merge it: `gh pr merge --squash --delete-branch --repo jarvisrchen/homebrew-yogurt <pr-number>`.
    10. Verify crates.io: `cargo search yogurt` shows `yogurt = "0.1.0"`.
  </action>
  <verify>
    `gh release view v0.1.0 --json assets --jq '.assets[].name'` lists both `yogurt-aarch64-apple-darwin.tar.gz` and `yogurt-x86_64-apple-darwin.tar.gz` and `SHA256SUMS`. `cargo search yogurt | grep -q '^yogurt = "0.1.0"'`. Tap PR is merged.
  </verify>
  <acceptance_criteria>
    - Tag `v0.1.0` exists in the repo
    - GitHub Release `v0.1.0` is live with both tarballs + SHA256SUMS
    - Homebrew tap PR is opened and merged
    - crates.io has yogurt v0.1.0
    - All four release workflow jobs (build × 2, release, tap, publish) succeeded
  </acceptance_criteria>
  <done>v0.1.0 is live across all three install channels. The DIST-09 acceptance gate (next task) can now be tested for real.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    v0.1.0 of yogurt is live on GitHub Releases, Homebrew tap, and crates.io. The DIST-09 acceptance gate now tests whether a NON-TECHNICAL user on a FRESH Mac can install and use yogurt end-to-end. This is the v1 ship gate.
  </what-built>
  <how-to-verify>
    Perform on a FRESH macOS environment (ideal: a Mac that has never had yogurt installed; acceptable: a clean shell with no cached binaries after running `brew uninstall yogurt; brew untap jarvisrchen/yogurt; cargo uninstall yogurt; rm -rf ~/.yogurt`).

    **Step 1 — Install via Homebrew (the non-technical-user path):**
    ```bash
    brew tap jarvisrchen/yogurt
    brew install yogurt
    ```
    Verify: install completes without errors. No "yogurt is from an unidentified developer" Gatekeeper popup. `which yogurt` returns `/opt/homebrew/bin/yogurt` (or equivalent).

    **Step 2 — Verify Gatekeeper acceptance:**
    ```bash
    spctl -a -vv -t install $(which yogurt)
    ```
    Expected: `accepted` with source `Notarized Developer ID`.

    **Step 3 — Start yogurt:**
    ```bash
    yogurt start
    ```
    Expected: browser opens to `http://localhost:7878` showing the yogurt UI. No crash, no port conflict, no console errors.

    **Step 4 — Grant Screen Recording permission:**
    Click "New meeting" or similar. macOS should prompt for Screen Recording permission. Grant it via System Settings → Privacy → Screen Recording. Restart yogurt if needed.

    **Step 5 — Configure an LLM provider:**
    Navigate to Settings → Model. Paste a valid OpenAI-compatible API key (Minimax, OpenAI, OpenRouter, or local Ollama — any one works). Verify it's stored and the model section unlocks.

    **Step 6 — Record a 5-minute test meeting:**
    Start a meeting. Type some sparse bullets in the notes panel. Speak into the mic. Play some audio out of the speakers (e.g. a YouTube video). End the meeting.

    **Step 7 — Verify augmented notes appear:**
    Within ~30 seconds of "End meeting", verify augmented notes appear: user-typed bullets remain BLACK, AI-added bullets render GREY with deep-link `↳ HH:MM` to transcript timestamps.

    **Step 8 — Verify markdown file:**
    `ls -la ~/.yogurt/notes/` — should contain a `.md` file with today's timestamp slug. Open it: front-matter present, notes + transcript appendix included.

    **Step 9 — Verify `yogurt doctor`:**
    ```bash
    yogurt doctor
    yogurt doctor --json | jq .service
    ```
    Expected: section labels print; JSON returns `"yogurt-doctor"`.

    All 9 steps must succeed end-to-end. If ANY step fails, DIST-09 is NOT met and Phase 9 is incomplete — file an issue, fix, and re-run.

    Additionally verify the OTHER two install channels work in a fresh shell:
    - `brew uninstall yogurt; cargo install yogurt --force; yogurt --version`
    - `brew uninstall yogurt; cargo uninstall yogurt; curl -L https://github.com/jarvisrchen/yogurt/releases/download/v0.1.0/yogurt-aarch64-apple-darwin.tar.gz | tar xz; ./yogurt --version`

    Reference: superpowers plan Task 9.13 Step 5 + Phase 9 acceptance criteria section.
  </how-to-verify>
  <resume-signal>Type "approved · v0.1.0 ships" if all 9 steps + cross-channel install checks pass. Otherwise describe which step failed and what the error was — Phase 9 cannot complete without DIST-09.</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `yogurt doctor` runs and prints diagnostic sections
- [ ] `yogurt doctor --json` produces valid JSON
- [ ] `yogurt doctor --reset-screen-recording` references `ai.yogurt.app`
- [ ] `yogurt doctor --check-port` reports port status
- [ ] `yogurt doctor --redownload-model <name>` removes the model file
- [ ] `yogurt --version` prints clean version
- [ ] README has install matrix (brew + cargo + direct)
- [ ] README has threat model + privacy posture sections
- [ ] CONTRIBUTING.md exists with dev setup + workspace layout
- [ ] scripts/release-checklist.md exists
- [ ] If tag was cut: GitHub Release live, Homebrew PR opened+merged, crates.io has v0.1.0
- [ ] DIST-09 human-verify checkpoint passed: non-technical user on fresh Mac successfully records a first meeting via `brew install`
</verification>

<success_criteria>
- DIST-08 satisfied: `yogurt doctor` subcommand covers TCC reset (`ai.yogurt.app`), port-conflict check, whisper-model re-download, full diagnostic dump
- DIST-09 satisfied: human-verify confirms `brew install yogurt && yogurt start` works end-to-end for a non-technical user on a fresh Mac with a first-meeting recording
- DIST-10 satisfied: README documents install, dev workflow, configuration, threat model
- All ROADMAP §Phase 9 Success Criteria pass:
  1. `v*` tag triggers matrix release, notarized, bundle ID `ai.yogurt.app`, tarballs uploaded (verified plans 09-01, 09-02)
  2. CI opens tap PR after release is live, then publishes to crates.io (verified plan 09-02)
  3. `brew install yogurt && yogurt start` works on clean macOS, passes `spctl -a -vv`, Screen Recording prompt fires (verified this plan's checkpoint)
  4. `yogurt doctor` runs TCC reset, port diagnostics, model re-download (verified this plan's Task 1)
  5. README documents install + dev + threat model + 5-minute contributor onboarding (verified this plan's Task 2)
</success_criteria>

<output>
After completion, create `.planning/phases/09-distribution-polish/09-03-SUMMARY.md`.

This is the LAST plan in Phase 9 and the LAST plan in v1. Once 09-03 completes and the DIST-09 checkpoint passes, Phase 9 is done and yogurt v1 ships.
</output>
