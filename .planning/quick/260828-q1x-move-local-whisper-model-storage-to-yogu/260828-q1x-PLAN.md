---
phase: quick-260828-q1x
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/yogurt-stt/src/models.rs
  - crates/yogurt-stt/tests/whisper_smoke.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "model_path() resolves to ~/.yogurt/models/<filename>, matching where db.sqlite, notes, and the session token already live"
    - "Existing model files (and .sha256 sidecars) at the old Application Support path are moved to ~/.yogurt/models/ on first resolution, so a user with 4.8GB of downloaded models does not re-download"
    - "No doc comment in the repo still claims models live under Application Support"
    - "cargo test -p yogurt-stt --features local-stt and cargo build both pass; no test touches the real home directory"
  artifacts:
    - path: "crates/yogurt-stt/src/models.rs"
      provides: "~/.yogurt/models resolution + migrate_legacy_model helper + unit tests for the migration"
      contains: "join(\".yogurt\")"
  key_links:
    - from: "crates/yogurt-stt/src/models.rs"
      to: "~/.yogurt/models"
      via: "directories::BaseDirs::home_dir() (mirrors crates/yogurt-db/src/paths.rs:18)"
      pattern: "home_dir\\(\\)\\.join\\(\"\\.yogurt\"\\)"
    - from: "migrate_legacy_model"
      to: "legacy ProjectDirs path"
      via: "fs::rename, confined to the migration helper only"
      pattern: "ProjectDirs::from\\(\"com\", \"yogurt\", \"yogurt\"\\)"
---

<objective>
Move the local Whisper model storage default from `~/Library/Application Support/com.yogurt.yogurt/models/` to `~/.yogurt/models/` so all app data (db, notes, session token, models) lives under `~/.yogurt/`.
Add a one-time migration that renames existing model files and their `.sha256` sidecars from the old path to the new one, and fix stale doc comments.

Purpose: single predictable data directory; real users (including this machine, ~4.8GB of models) must not re-download after upgrade.
Output: updated `model_path()` + `migrate_legacy_model()` helper with unit tests; corrected doc comments; `whisper_smoke.rs` reusing `model_path()` instead of duplicating path resolution.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@crates/yogurt-stt/src/models.rs
@crates/yogurt-db/src/paths.rs
@crates/yogurt-stt/tests/whisper_smoke.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint model_path() to ~/.yogurt/models with one-time legacy migration</name>
  <files>crates/yogurt-stt/src/models.rs</files>
  <action>
    In `crates/yogurt-stt/src/models.rs`:

    1. Rewrite `model_path()` (currently line 134) to resolve `~/.yogurt/models/<spec.filename>` via `directories::BaseDirs::new()` + `home_dir().join(".yogurt").join("models")`, mirroring the existing pattern in `crates/yogurt-db/src/paths.rs:18` (and `yogurt-server/src/storage.rs`, `session.rs`). Do NOT add any new dependency; `directories` is already an optional dep behind the `local-stt` feature. Keep the `std::io::Result<PathBuf>` signature and the `create_dir_all` call. On `BaseDirs::new()` returning `None`, keep the existing `io::Error::new(NotFound, ...)` shape with message "could not resolve user home directory".

    2. After creating the new dir and before returning, run the legacy migration: if `directories::ProjectDirs::from("com", "yogurt", "yogurt")` resolves, call a new private helper `migrate_legacy_model(&old_models_dir, &new_dir, spec.filename)` where `old_models_dir = project_dirs.data_local_dir().join("models")`. The ProjectDirs resolution must appear ONLY inside `model_path()`/the migration call site — nowhere else in the crate after this change.

    3. Implement `fn migrate_legacy_model(old_dir: &Path, new_dir: &Path, filename: &str)` (path-injectable for tests, returns nothing):
       - If `new_dir/filename` already exists: return (never clobber).
       - If `old_dir/filename` does not exist: return.
       - Otherwise `std::fs::rename` `old_dir/filename` -> `new_dir/filename`. Both paths are under `$HOME` on the same volume, so rename is atomic and instant even for a 3GB file — do NOT copy. On rename error: `tracing::warn!` and return; the normal re-download path recovers.
       - After a successful model rename, best-effort rename the sidecar `marker_path(&old_dir.join(filename))` -> `marker_path(&new_dir.join(filename))` if it exists; on failure just `tracing::warn!` — `is_downloaded_at` re-hashes and self-heals a missing marker, so a lost sidecar is not fatal.

    4. Add unit tests in the existing `#[cfg(test)] mod tests` using `tempfile::tempdir()` (already a dev-dep) — never the real home dir:
       - old dir has model + sidecar, new dir empty -> both files end up in new dir, old copies gone.
       - new dir already has the model, old dir also has one -> old file left untouched (no clobber).
       - old dir missing / empty -> no-op, no panic.
  </action>
  <verify>
    <automated>cargo test -p yogurt-stt --features local-stt</automated>
  </verify>
  <done>`model_path()` returns `~/.yogurt/models/<filename>`; migration helper moves model + sidecar exactly once, never clobbers, never copies; all yogurt-stt tests pass without touching the real home dir.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale doc comments and dedupe whisper_smoke path resolution</name>
  <files>crates/yogurt-stt/src/models.rs, crates/yogurt-stt/tests/whisper_smoke.rs</files>
  <action>
    1. `models.rs` module docs, `# Layout` section (currently lines 24-35): rewrite to state models live at `~/.yogurt/models/ggml-<name>.bin` alongside the rest of the app data (db.sqlite, session token), resolved via `BaseDirs::home_dir()` like `yogurt-db::paths` / `yogurt-server::storage`. Drop the "doc lie" history paragraph and the "Phase 5 set data_local_dir" claim (it was never accurate); add one line noting the one-time migration from the legacy `~/Library/Application Support/com.yogurt.yogurt/models/` path performed by `migrate_legacy_model`.

    2. `models.rs` `model_path()` doc comment (currently lines 127-133): describe the `~/.yogurt/models/<filename>` resolution, the keep-in-sync note with `yogurt-db/src/paths.rs`, and the legacy migration side effect.

    3. `whisper_smoke.rs` lines 44-49: replace the duplicated `ProjectDirs` resolution with the crate's own resolver — `models::model_path(models::lookup("small.en").expect("registry"))`. This runs after the `RUN_WHISPER_SMOKE` env gate, so plain test runs still never touch the real home dir. Update the stale comment above it.

    4. Confirm `crates/yogurt-server/src/ws.rs` line ~73 (`file persisted at ~/.yogurt/models/<filename>`) is now accurate — no edit expected; adjust only if wording conflicts.

    5. Sweep: after edits, `ProjectDirs`/`com.yogurt.yogurt`/`Application Support` may appear in `crates/**/*.rs` ONLY inside models.rs's migration helper + its call site and the one migration doc line.
  </action>
  <verify>
    <automated>cargo build && [ "$(grep -rl 'data_local_dir\|Application Support' crates --include='*.rs')" = "crates/yogurt-stt/src/models.rs" ]</automated>
  </verify>
  <done>All doc comments describe `~/.yogurt/models/`; whisper_smoke reuses `model_path()`; grep shows the legacy path confined to models.rs; workspace builds clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Change moves files between two directories inside `$HOME`; no network, no new deps, no untrusted input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q1x-01 | Tampering | migrate_legacy_model | mitigate | Never clobber an existing file at the new path; rename only (atomic on-volume), never copy; failures degrade to the existing SHA256-verified re-download path. |
</threat_model>

<verification>
- `cargo test -p yogurt-stt --features local-stt` passes (registry, marker, download, and new migration tests).
- `cargo build` passes for the whole workspace.
- `grep -rn 'data_local_dir' crates --include='*.rs'` matches only `crates/yogurt-stt/src/models.rs`.
- On this machine (manual, post-merge): first `model_path()` call moves `ggml-large-v3.bin`, `ggml-medium.en.bin`, `ggml-small.en.bin` + `.sha256` sidecars from `~/Library/Application Support/com.yogurt.yogurt/models/` to `~/.yogurt/models/`, and the Settings model picker still shows all three as Downloaded.
</verification>

<success_criteria>
- Models resolve to `~/.yogurt/models/` via the same BaseDirs pattern as the rest of the app; no new dependency added.
- Existing downloads migrate via rename (no re-download, no copy) exactly once; no-clobber and failure-tolerant.
- No stale Application Support doc comments remain outside the migration helper.
- Unit tests cover the migration helper with temp dirs; full `cargo test -p yogurt-stt --features local-stt` and `cargo build` green.
</success_criteria>

<output>
Create `.planning/quick/260828-q1x-move-local-whisper-model-storage-to-yogu/260828-q1x-SUMMARY.md` when done.
</output>
