//! Structural block-level diff/merge between user_md and enriched_md.
//! Filled in by Task 2 (TDD against the 5 fixtures).

use crate::ast::Block;
use crate::{MergedBlock, Source, TranscriptSegment};

pub fn merge(
    _user: &[Block],
    _enriched: &[Block],
    _t: &[TranscriptSegment],
) -> Vec<MergedBlock> {
    // Filled in Task 2.
    let _ = Source::User;
    Vec::new()
}
