[package]
name = "yogurt-notes"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "Markdown AST diff + merge for yogurt's augmented-notes feature."

[dependencies]
pulldown-cmark = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
tracing = { workspace = true }
regex-lite = { workspace = true }
# BL-2 (Phase 4 review): escape user-controlled text before interpolation
# into wire-format spans inside render::wrap_ai. Defense in depth pairs with
# ammonia sanitization in yogurt-server.
html-escape = { workspace = true }

[dev-dependencies]
insta = { workspace = true }
