# Feature map

Every user-facing feature: what it does, where it lives in the UI and API, and what test covers it.
`scripts/check-docs.sh` fails the build if a real server route or a `web/src/router.tsx` path is missing from this table or from the `## Internal routes` list below.

The four Settings rows share one router path, `/settings/:section`; the `section` value in each row's UI path cell is the one that route renders for that row.

| Feature | What it does | UI path | API | ctl | Covered by | Source anchor |
|---|---|---|---|---|---|---|
| Library and filters | Lists meetings grouped by date, with search and starred/label filters in the sidebar. | `/` `/starred` `/label/:labelId` | `/api/meetings` `/api/meetings/search` | `yogurt ctl meeting list` `yogurt ctl meeting search` | `web/e2e/library-navigation.spec.ts` `web/src/components/library/MeetingCard.test.tsx` `web/src/components/library/DateGroup.test.ts` | `web/src/routes/Library.tsx` |
| New meeting and live page | Creates a meeting row, opens the in-progress recording view, and starts/stops audio capture. | `/meeting/new` `/meeting/:id` | `/api/meetings` `/api/meetings/:id/start` `/api/meetings/:id/stop` | `yogurt ctl meeting new` `yogurt ctl meeting start` `yogurt ctl meeting stop` | `web/src/routes/Meeting.test.tsx` `crates/yogurt-server/tests/meeting_rest.rs` | `web/src/routes/Meeting.tsx` |
| Transcript dock | Streams live transcript segments over the per-meeting WebSocket into a scrollable dock. | `/meeting/:id` | `/ws/meetings/:id` | `yogurt ctl meeting transcript` | `web/src/components/TranscriptDock.test.tsx` `crates/yogurt-server/tests/meeting_ws.rs` | `web/src/components/TranscriptDock.tsx` |
| Mic mute | Pauses and resumes the mic channel on the active recording without touching system audio. | `/meeting/:id` | `/api/meetings/:id/mic-muted` | `yogurt ctl meeting mute` | `web/src/components/MicMuteToggle.test.tsx` `crates/yogurt-server/tests/mic_mute.rs` | `web/src/components/MicMuteToggle.tsx` |
| Device switch | Hot-swaps the mic device on an actively-recording meeting and shows the resolved device name. | `/meeting/:id` | `/api/audio/devices` `/api/meetings/:id/audio-device` | - | `web/src/components/MicDevicePicker.test.tsx` `crates/yogurt-server/tests/audio_device_switch.rs` | `web/src/components/MicDevicePicker.tsx` |
| Mic echo | Echoes the mic to a chosen output device (e.g. BlackHole) during a recording, hot-swappable, silenced while the mic is muted. | `/meeting/:id` | `/api/audio/output-devices` `/api/meetings/:id/echo` | - | `web/src/components/MicEchoToggle.test.tsx` `web/src/components/EchoDevicePicker.test.tsx` | `crates/yogurt-audio/src/echo.rs` `web/src/components/MicEchoToggle.tsx` |
| Return-to-recording pill | Polls for the one active recording and shows a floating pill back to it from any other route. | `*` (every route but the live meeting view) | `/api/meetings/active` | `yogurt ctl status` | `web/src/components/RecordingPill.test.tsx` | `web/src/components/RecordingPill.tsx` |
| Detection banner | Prompts to record when a meeting-looking window is detected, and suppresses the prompt on dismiss. | `*` (every route) | `/api/meetings/detected` `/api/meetings/detected/dismiss` | `yogurt ctl detect` `yogurt ctl detect dismiss` | `web/src/components/MeetingDetectedBanner.test.tsx` | `web/src/components/MeetingDetectedBanner.tsx` |
| Post-meeting page and enhance | Runs augmented-notes generation over the transcript and user notes, with a note-format picker (auto-detected, or forced by name) and a re-enhance action. | `/meeting/:id/post` | `/api/meetings/:id/enhance` `/api/templates` | `yogurt ctl meeting enhance` | `web/src/routes/MeetingPost.test.tsx` `web/src/routes/MeetingPost.autosave.test.tsx` `crates/yogurt-server/tests/enhance_endpoint.rs` `crates/yogurt-server/tests/enhance_templates.rs` | `web/src/routes/MeetingPost.tsx` |
| Notes editor and export | Edits notes as markdown-backed rich text, and copies or reveals the on-disk export from the Library kebab menu. | `/meeting/:id` `/meeting/:id/post` `/` | `/api/meetings/:id` `/api/meetings/:id/markdown` `/api/meetings/:id/reveal` | `yogurt ctl meeting show` `yogurt ctl meeting summary` | `web/src/editor/index.test.tsx` `web/src/editor/markdown.test.ts` `crates/yogurt-server/tests/meetings_api.rs` | `web/src/editor/index.tsx` |
| Chat | Answers questions about the meeting, streaming LLM output token by token over the meeting WebSocket. | `/meeting/:id` `/meeting/:id/post` | `/api/meetings/:id/chat` `/ws/meetings/:id` | - | `web/src/components/__tests__/ChatWindow.test.tsx` `web/src/components/__tests__/AskPill.test.tsx` `crates/yogurt-server/tests/chat_streaming.rs` | `web/src/components/ChatWindow.tsx` |
| Labels | Creates, assigns, and filters by Granola-style meeting labels from the Library sidebar. | `/` `/label/:labelId` | `/api/labels` `/api/labels/:id` | - | `web/src/components/labels/LabelPicker.test.tsx` `web/src/components/library/Sidebar.labels.test.tsx` `crates/yogurt-server/tests/labels_api.rs` | `web/src/components/labels/LabelPicker.tsx` |
| Delete | Confirms and deletes a meeting row and its on-disk markdown export. | `/` | `/api/meetings/:id` | `yogurt ctl meeting delete` | `web/src/components/library/DeleteMeetingConfirm.test.tsx` `crates/yogurt-server/tests/meetings_api.rs` | `web/src/components/library/DeleteMeetingConfirm.tsx` |
| Settings - model | Adds, activates, and tests OpenAI-compatible LLM providers, with one-click presets. | `/settings/:section` (section=`model`) | `/api/settings` `/api/settings/providers` `/api/settings/providers/:id` `/api/settings/providers/:id/activate` `/api/settings/providers/:id/key` `/api/settings/providers/:id/test` `/api/settings/providers/:id/models` `/api/settings/presets` | `yogurt ctl provider list` `yogurt ctl provider activate` `yogurt ctl provider test` | `web/src/components/settings/ProviderCard.test.tsx` `web/src/components/settings/AddProviderForm.test.tsx` `crates/yogurt-server/tests/settings_api.rs` | `web/src/routes/Settings.tsx` |
| Settings - transcription | Switches between cloud (Deepgram) and local (whisper.cpp) speech-to-text, and downloads local models with live progress. | `/settings/:section` (section=`transcription`) | `/api/settings/stt/key` `/api/settings/stt/test` `/api/settings/stt/local/test` `/api/stt/models` `/api/stt/models/:name/download` `/api/stt/models/:name` `/ws` | `yogurt ctl models list` `yogurt ctl models download` `yogurt ctl models delete` | `web/src/components/settings/STTPicker.test.tsx` `web/src/components/settings/LocalSTTCard.test.tsx` `web/src/components/settings/ModelPicker.test.tsx` `yogurt-server::api::stt_models` inline tests | `web/src/components/settings/STTPicker.tsx` |
| Settings - audio | Picks the default mic input device used for new recordings, plus the mic echo output device and buffer size. | `/settings/:section` (section=`audio`) | `/api/audio/devices` `/api/audio/output-devices` `/api/settings` | `yogurt ctl settings get` `yogurt ctl settings set` | `crates/yogurt-server/tests/audio_api.rs` | `web/src/components/settings/AudioSection.tsx` |
| Settings - general | Sets other app-wide preferences and the light/dark/system theme (browser-local, not server state). | `/settings/:section` (section=`general`) | `/api/settings` | `yogurt ctl settings get` `yogurt ctl settings set` | `web/src/components/settings/GeneralSection.test.tsx` `web/src/lib/theme.test.ts` | `web/src/components/settings/GeneralSection.tsx` |
| Onboarding | Walks a first-run user through granting microphone and screen-recording permissions, then redirects to the Library. | `/welcome` | `/api/audio/permission` `/api/audio/microphone/request` `/api/audio/screen-recording/request` | - | `web/src/hooks/useFirstRunRedirect.test.tsx` | `web/src/routes/Welcome.tsx` |
| Style guide | Renders the design-system showcase (tokens, components) with no backend calls. | `/style-guide` | (none) | - | manual | `web/src/routes/StyleGuide.tsx` |
| CLI | Controls and reads a running instance from the shell: status, meeting lifecycle, detection, on-screen windows, settings, providers, STT models, and raw websocket frames. | (none) | drives the routes above over HTTP and `/ws` | `yogurt ctl status` `yogurt ctl meeting` `yogurt ctl detect` `yogurt ctl windows` `yogurt ctl settings` `yogurt ctl provider` `yogurt ctl models` `yogurt ctl ws` | `crates/yogurt-cli/tests/ctl_smoke.rs` | `crates/yogurt-cli/src/commands/ctl/mod.rs` |

## Internal routes

Routes with no user-facing feature of their own - infrastructure the app needs to boot or fail cleanly, not something a user or agent picks off a menu.

- `/api/health` - unauthenticated liveness probe; the SPA and `yogurt ctl` poll it, but there is no "health" screen.
- `/api/session-token` - bootstrap endpoint the SPA calls once on boot to learn the auth token used by every other call.
- `/api/:rest` - honest JSON 404 for any unmatched `/api/*` path (normalized from the extracted `/api/{*rest}` route).
- `/settings` - bare redirect to `/settings/model`.
- `*` - the React Router catch-all; redirects an unknown path to `/`.
