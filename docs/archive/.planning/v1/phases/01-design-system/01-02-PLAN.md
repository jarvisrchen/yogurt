---
phase: 01-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - web/src/components/Logo.tsx
  - web/src/components/Logo.test.tsx
  - web/src/components/Button.tsx
  - web/src/components/Button.test.tsx
  - web/src/components/Pill.tsx
  - web/src/components/Pill.test.tsx
  - web/src/components/Card.tsx
  - web/src/components/Card.test.tsx
  - web/src/components/BrowserChrome.tsx
  - web/src/components/BrowserChrome.test.tsx
autonomous: true
requirements:
  - DESIGN-05
must_haves:
  truths:
    - "Logo renders an SVG with viewBox 0 0 44 44 and brand colors #5B4FC7 + #E07A66"
    - "Button supports primary/secondary/ghost variants with the documented Tailwind class signatures"
    - "Pill base component supports neutral/blue/matcha/straw tones"
    - "RecordingBadge renders a pulsing strawberry dot (animate-recpulse) + a JetBrains-Mono timer"
    - "ProviderChip renders active (blue tone) and local (matcha tone) states"
    - "Card renders default (line border) and active (blueberry border) variants with sm/md/lg padding"
    - "BrowserChrome renders 3 traffic-light dots + centered URL pill + body slot"
    - "All component .test.tsx files pass via `pnpm --dir web test`"
  artifacts:
    - path: "web/src/components/Logo.tsx"
      provides: "Spoon-and-swirl SVG logo, sizable, optional ariaLabel"
      contains: "#5B4FC7"
      min_lines: 25
    - path: "web/src/components/Logo.test.tsx"
      provides: "Vitest smoke for Logo (4 cases)"
      contains: "viewBox"
    - path: "web/src/components/Button.tsx"
      provides: "primary/secondary/ghost Button (PRD §16.6)"
      contains: "variant"
      min_lines: 40
    - path: "web/src/components/Button.test.tsx"
      provides: "Vitest smoke for Button (7 cases)"
      contains: "variant=\"secondary\""
    - path: "web/src/components/Pill.tsx"
      provides: "Pill base + RecordingBadge + ProviderChip named exports"
      contains: "RecordingBadge"
      min_lines: 50
    - path: "web/src/components/Pill.test.tsx"
      provides: "Vitest smoke for Pill / RecordingBadge / ProviderChip (9 cases total)"
      contains: "animate-recpulse"
    - path: "web/src/components/Card.tsx"
      provides: "Card with active variant + sm/md/lg padding + polymorphic `as`"
      contains: "rounded-card"
      min_lines: 30
    - path: "web/src/components/Card.test.tsx"
      provides: "Vitest smoke for Card (5 cases)"
      contains: "active"
    - path: "web/src/components/BrowserChrome.tsx"
      provides: "Fake-Safari mockup wrapper (header + URL pill + body slot)"
      contains: "traffic-dot"
      min_lines: 40
    - path: "web/src/components/BrowserChrome.test.tsx"
      provides: "Vitest smoke for BrowserChrome (4 cases)"
      contains: "localhost:7878"
  key_links:
    - from: "web/src/components/Pill.tsx RecordingBadge"
      to: "@theme --animate-recpulse"
      via: "className 'animate-recpulse' utility derived from --animate-recpulse token"
      pattern: "animate-recpulse"
    - from: "web/src/components/Card.tsx"
      to: "@theme --radius-card, --shadow-card, --color-line"
      via: "className 'bg-card rounded-card shadow-card border-line' Tailwind utilities"
      pattern: "rounded-card.*shadow-card"
    - from: "web/src/components/Button.tsx primary variant"
      to: "@theme --color-blue"
      via: "className 'bg-blue text-white' utility derived from --color-blue token"
      pattern: "bg-blue.*text-white"
---

<objective>
Build the five core React component primitives that downstream phases (library, settings, onboarding, post-meeting view) will compose: `Logo`, `Button`, `Pill` (+ named `RecordingBadge` and `ProviderChip` variants), `Card`, `BrowserChrome`. Each component is colocated with a Vitest + @testing-library/react smoke-test file. The TDD loop for each is: write failing test → run/fail → implement → run/pass.

Purpose: DESIGN-05 (Core component primitives shipped) lands here. The components consume the token utilities established in Plan 01 (`bg-paper`, `text-ink`, `font-serif`, `rounded-card`, `shadow-card`, `animate-recpulse`, etc.) — no new tokens introduced.
Output: 10 new files (5 components × {.tsx, .test.tsx}). Total Vitest count after this plan: Phase-0 baseline + 4 (Logo) + 7 (Button) + 9 (Pill) + 5 (Card) + 4 (BrowserChrome) = 29 component test cases.

Deferred (do NOT implement): Tab group primitive (Phase 4); End-meeting "ink" Button variant (Phase 4); the StyleGuide route and Router wiring (Plan 03).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/phases/01-design-system/01-01-SUMMARY.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md
@docs/PRD.md

@web/src/index.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Logo component (spoon-and-swirl SVG)</name>
  <files>web/src/components/Logo.tsx, web/src/components/Logo.test.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Implement Task 1.3 from the superpowers plan (lines 299-409).

    Step 1 (TDD red): Create `web/src/components/Logo.test.tsx` with the exact 4-case suite from the plan (lines 311-346). The four cases:
      - renders an SVG with viewBox "0 0 44 44" and width/height 44 when size=44
      - uses brand colors (asserts container has elements with fill="#5B4FC7" AND fill="#E07A66")
      - defaults to 44px when no size is provided
      - forwards an ariaLabel via getByLabelText

    Step 2: Run `pnpm --dir web test -- Logo` — expect compile failure (`Cannot find module './Logo'`).

    Step 3 (TDD green): Implement `web/src/components/Logo.tsx` per the exact snippet at plan lines 357-396. Component signature: `{ size?: number, ariaLabel?: string, className?: string }`. Default size=44. The SVG has three primitives:
      - `<circle cx="22" cy="22" r="22" fill="#5B4FC7" />` (blueberry circle)
      - `<path d="M13 31 C 13 22, 31 25, 31 17 C 31 11, 19 12.5, 19 19" fill="none" stroke="#fff" strokeWidth="3.4" strokeLinecap="round" />` (white spoon-curve)
      - `<circle cx="19" cy="19" r="1.7" fill="#E07A66" />` (strawberry dot at spoon tip)
      - ariaLabel forwards role="img" + aria-label; absence → aria-hidden=true
      - Outer style={{ flex: "none" }}

    Step 4: Run `pnpm --dir web test -- Logo` — expect 4 passed.
  </action>
  <verify>pnpm --dir web test -- Logo</verify>
  <acceptance_criteria>
    - web/src/components/Logo.tsx exports `Logo` and contains fill="#5B4FC7" and fill="#E07A66"
    - web/src/components/Logo.tsx contains `viewBox="0 0 44 44"`
    - web/src/components/Logo.tsx default size param is 44
    - web/src/components/Logo.test.tsx contains 4 `it(...)` blocks
    - `pnpm --dir web test -- Logo` reports 4 passing tests
  </acceptance_criteria>
  <done>Logo component lands with 4 passing test cases.</done>
</task>

<task type="auto">
  <name>Task 2: Button component (primary/secondary/ghost variants)</name>
  <files>web/src/components/Button.tsx, web/src/components/Button.test.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Implement Task 1.4 from the superpowers plan (lines 413-579).

    Step 1 (TDD red): Create `web/src/components/Button.test.tsx` with the exact 7-case suite from plan lines 430-486. Cases:
      - renders children as label text
      - defaults to primary variant (className matches /bg-blue/ and /text-white/)
      - secondary variant has /bg-card/, /border/, /text-ink/
      - ghost variant has /bg-transparent/, /text-mut/
      - fires onClick
      - respects disabled (onClick not called, attribute set)
      - supports type='submit'

    Step 2: Run `pnpm --dir web test -- Button` — expect compile failure.

    Step 3 (TDD green): Implement `web/src/components/Button.tsx` per the exact snippet at plan lines 496-566. Component signature: `{ variant?: "primary"|"secondary"|"ghost"; children: ReactNode; className?: string } & Omit<ButtonHTMLAttributes<HTMLButtonElement>, "className">`. Default variant=primary, default type=button.

    The BASE classes (joined with " "):
      `inline-flex items-center justify-center gap-2 px-4 py-2 rounded-button font-sans text-[13.5px] font-semibold leading-none transition-colors transition-shadow duration-150 ease-out select-none disabled:opacity-50 disabled:cursor-not-allowed`

    VARIANT map:
      - primary: `bg-blue text-white shadow-[0_2px_8px_rgba(91,79,199,0.3)] hover:opacity-90 active:opacity-100`
      - secondary: `bg-card text-ink border border-[#D9D0C0] hover:bg-[#F8F2E5] active:bg-[#F1E9D7]`
      - ghost: `bg-transparent text-mut hover:text-ink hover:bg-blsoft/50`

    Include the JSDoc note that the "ink" variant is deferred to Phase 4 (see plan line 424).

    Step 4: Run `pnpm --dir web test -- Button` — expect 7 passed.
  </action>
  <verify>pnpm --dir web test -- Button</verify>
  <acceptance_criteria>
    - web/src/components/Button.tsx exports `Button` and supports variants "primary"|"secondary"|"ghost"
    - web/src/components/Button.tsx primary variant className contains "bg-blue" and "text-white"
    - web/src/components/Button.tsx secondary variant className contains "bg-card" and "text-ink" and "border"
    - web/src/components/Button.tsx ghost variant className contains "bg-transparent" and "text-mut"
    - web/src/components/Button.tsx contains "disabled:opacity-50" and "rounded-button"
    - web/src/components/Button.tsx defaults `type` to "button"
    - web/src/components/Button.test.tsx contains 7 `it(...)` blocks
    - `pnpm --dir web test -- Button` reports 7 passing tests
  </acceptance_criteria>
  <done>Button component lands with primary/secondary/ghost variants and 7 passing test cases.</done>
</task>

<task type="auto">
  <name>Task 3: Pill component + RecordingBadge + ProviderChip variants</name>
  <files>web/src/components/Pill.tsx, web/src/components/Pill.test.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Implement Task 1.5 from the superpowers plan (lines 583-794).

    Step 1 (TDD red): Create `web/src/components/Pill.test.tsx` with the exact 9-case suite from plan lines 602-676. Three describe blocks:
      - Pill (5 cases): renders children; defaults to neutral tone (rounded-pill + border); matcha tone uses bg-mtsoft + text-matcha; blue tone uses bg-blsoft + text-blue; straw tone uses bg-strsoft + text-straw
      - RecordingBadge (2 cases): pulsing dot via [data-testid="recording-dot"] with className matching /animate-recpulse/ + timer text; timer renders in /font-mono/
      - ProviderChip (3 cases): renders provider name; active shows /bg-blsoft/; local shows /bg-mtsoft/

    Step 2: Run `pnpm --dir web test -- Pill` — expect compile failure.

    Step 3 (TDD green): Implement `web/src/components/Pill.tsx` per the exact snippet at plan lines 685-781. Three exports:
      - `Pill({ tone?: "neutral"|"blue"|"matcha"|"straw" = "neutral", children, className? })` — base inline-flex with `rounded-pill px-2.5 py-1 text-[12px] font-medium leading-none border whitespace-nowrap`; tone map: neutral→`bg-paper text-mut border-line`, blue→`bg-blsoft text-blue border-blue/20`, matcha→`bg-mtsoft text-matcha border-matcha/25`, straw→`bg-strsoft text-straw border-straw/25`
      - `RecordingBadge({ elapsed: string })` — white surface + strawberry hairline border; contains `<span data-testid="recording-dot" className="inline-block w-2 h-2 rounded-pill bg-straw animate-recpulse" aria-hidden />` and a JetBrains-Mono timer span `<span className="font-mono text-[12px] tracking-tight">{elapsed}</span>`
      - `ProviderChip({ name, active?, local? })` — composes `<Pill tone={active ? "blue" : local ? "matcha" : "neutral"}>` with a leading 1.5×1.5 colored dot (`bg-blue` / `bg-matcha` / `bg-mut`) + name

    Step 4: Run `pnpm --dir web test -- Pill` — expect 9 passed.
  </action>
  <verify>pnpm --dir web test -- Pill</verify>
  <acceptance_criteria>
    - web/src/components/Pill.tsx exports `Pill`, `RecordingBadge`, `ProviderChip`
    - web/src/components/Pill.tsx contains "rounded-pill" and the four tone branches (neutral/blue/matcha/straw)
    - web/src/components/Pill.tsx RecordingBadge contains `data-testid="recording-dot"` and `animate-recpulse` and `font-mono`
    - web/src/components/Pill.tsx ProviderChip toggles between blue/matcha/neutral tones based on active/local props
    - web/src/components/Pill.test.tsx has 3 describe blocks ("Pill", "RecordingBadge", "ProviderChip") with 9 total it() cases
    - `pnpm --dir web test -- Pill` reports 9 passing tests
  </acceptance_criteria>
  <done>Pill family lands with 9 passing test cases covering base + RecordingBadge + ProviderChip.</done>
</task>

<task type="auto">
  <name>Task 4: Card component (sm/md/lg padding + active variant + polymorphic 'as')</name>
  <files>web/src/components/Card.tsx, web/src/components/Card.test.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Implement Task 1.6 from the superpowers plan (lines 798-923).

    Step 1 (TDD red): Create `web/src/components/Card.test.tsx` with the exact 5-case suite from plan lines 810-849. Cases:
      - renders children (heading rendered)
      - applies bg-card + rounded-card + shadow-card by default
      - supports `active` prop adding /border-blue/
      - supports padding="lg" adding /p-8/
      - renders as <article> when as="article"

    Step 2: Run `pnpm --dir web test -- Card` — expect compile failure.

    Step 3 (TDD green): Implement `web/src/components/Card.tsx` per the exact snippet at plan lines 859-910. Props: `{ children, active?: boolean = false, padding?: "sm"|"md"|"lg" = "md", as?: ElementType, className? }`. PADDING map: sm→p-3, md→p-5, lg→p-8. Border branch: `active ? "border-[1.5px] border-blue" : "border border-line"`. Composed classes: `bg-card rounded-card shadow-card ${border} ${PADDING[padding]} ${className}`.

    Step 4: Run `pnpm --dir web test -- Card` — expect 5 passed.
  </action>
  <verify>pnpm --dir web test -- Card</verify>
  <acceptance_criteria>
    - web/src/components/Card.tsx exports `Card`
    - web/src/components/Card.tsx default className contains "bg-card", "rounded-card", "shadow-card", "border border-line", "p-5"
    - web/src/components/Card.tsx active branch produces "border-[1.5px] border-blue"
    - web/src/components/Card.tsx padding "lg" branch produces "p-8"
    - web/src/components/Card.tsx supports polymorphic `as` prop rendering as different element
    - `pnpm --dir web test -- Card` reports 5 passing tests
  </acceptance_criteria>
  <done>Card component lands with active variant, padding scale, and polymorphic `as` — 5 passing test cases.</done>
</task>

<task type="auto">
  <name>Task 5: BrowserChrome component (fake-Safari mockup wrapper)</name>
  <files>web/src/components/BrowserChrome.tsx, web/src/components/BrowserChrome.test.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Implement Task 1.7 from the superpowers plan (lines 927-1083).

    Step 1 (TDD red): Create `web/src/components/BrowserChrome.test.tsx` with the exact 4-case suite from plan lines 939-982. Cases:
      - renders the URL in the centered pill (getByText("localhost:7878/welcome"))
      - renders three [data-testid="traffic-dot"] elements
      - renders children inside the chrome body (getByTestId("content"))
      - root className contains /shadow-/

    Step 2: Run `pnpm --dir web test -- BrowserChrome` — expect compile failure.

    Step 3 (TDD green): Implement `web/src/components/BrowserChrome.tsx` per the exact snippet at plan lines 992-1070. Props: `{ url: string, children: ReactNode, className? }`. Structure:
      - Outer div: `overflow-hidden rounded-card border border-line bg-card shadow-[0_26px_60px_-28px_rgba(40,30,15,0.4)] ${className}`
      - Header: `h-[42px] flex items-center px-4 border-b border-line` with inline style `background: "#F4EEE3"`
      - Three traffic-light dots (data-testid="traffic-dot"): `w-3 h-3 rounded-pill` with inline backgrounds `#FF5F57`, `#FEBC2E`, `#28C840`
      - Centered URL pill: `inline-block rounded-pill px-3 py-1 bg-card border border-line font-mono text-[11px] text-mut`
      - Right-side 52px spacer to balance the centered URL
      - Body slot wraps `{children}` in `<div className="bg-paper">`

    Step 4: Run `pnpm --dir web test -- BrowserChrome` — expect 4 passed.
  </action>
  <verify>pnpm --dir web test -- BrowserChrome</verify>
  <acceptance_criteria>
    - web/src/components/BrowserChrome.tsx exports `BrowserChrome`
    - web/src/components/BrowserChrome.tsx contains three elements with `data-testid="traffic-dot"` and the hex colors #FF5F57, #FEBC2E, #28C840
    - web/src/components/BrowserChrome.tsx header uses inline background "#F4EEE3" and height h-[42px]
    - web/src/components/BrowserChrome.tsx centered URL pill uses font-mono + text-[11px] + text-mut
    - web/src/components/BrowserChrome.tsx outer className contains "shadow-[0_26px_60px_-28px_rgba(40,30,15,0.4)]"
    - `pnpm --dir web test -- BrowserChrome` reports 4 passing tests
  </acceptance_criteria>
  <done>BrowserChrome lands with traffic-light header + centered URL pill + body slot — 4 passing test cases.</done>
</task>

<task type="auto">
  <name>Task 6: Full Vitest suite sanity check</name>
  <files></files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-1-design-system.md</read_first>
  <action>
    Run the entire test suite to confirm component tests don't interfere with one another or with the Phase-0 App test.

    Expected per the superpowers plan:
      - Logo.test.tsx: 4 passed
      - Button.test.tsx: 7 passed
      - Pill.test.tsx: 9 passed
      - Card.test.tsx: 5 passed
      - BrowserChrome.test.tsx: 4 passed
      - App.test.tsx (Phase 0): still passing — Plan 03 will rewrite its assertions, but the existing assertions should still hold since `App.tsx` has not yet been modified by this plan

    If App.test.tsx is now failing because of incidental coupling, document the failure in the SUMMARY — do NOT modify App.tsx or App.test.tsx in this plan. Those changes belong to Plan 03 Task 1.8.

    Then run `pnpm --dir web build` to confirm the production build still succeeds.
  </action>
  <verify>pnpm --dir web test &amp;&amp; pnpm --dir web build</verify>
  <acceptance_criteria>
    - All 5 new component test files pass: Logo (4), Button (7), Pill (9), Card (5), BrowserChrome (4) = 29 component test cases
    - `pnpm --dir web build` exits 0
  </acceptance_criteria>
  <done>All component primitives ship with green tests and a clean production build.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 10 new files exist under `web/src/components/`
- [ ] `pnpm --dir web test` runs the 29 component test cases and they all pass
- [ ] `pnpm --dir web build` exits 0
- [ ] grep "RecordingBadge" web/src/components/Pill.tsx returns a match
- [ ] grep "BrowserChrome" web/src/components/BrowserChrome.tsx returns a match
- [ ] grep -c "data-testid=\"traffic-dot\"" web/src/components/BrowserChrome.tsx returns 3
</verification>

<success_criteria>
- All 6 tasks completed
- All 5 component primitives are exported and tested
- Vitest suite has at least 29 new passing test cases on top of the Phase-0 baseline
- DESIGN-05 fully satisfied at the component-primitive level (Logo, Button, Pill family, Card, BrowserChrome). Tab group is documented as deferred to Phase 4 — not a Phase 1 gap.
- No Rust changes; `cargo test --workspace` unchanged from Phase 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-02-SUMMARY.md`
</output>
