---
phase: 07-library-onboarding-states
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/yogurt-db/Cargo.toml
  - crates/yogurt-db/src/lib.rs
  - crates/yogurt-db/src/meetings.rs
  - crates/yogurt-db/src/migrations/V003__meetings.sql
  - crates/yogurt-server/Cargo.toml
  - crates/yogurt-server/src/lib.rs
  - crates/yogurt-server/src/api/mod.rs
  - crates/yogurt-server/src/api/meetings.rs
  - crates/yogurt-server/src/meetings.rs
  - crates/yogurt-server/src/ws.rs
  - crates/yogurt-server/tests/meetings_api.rs
  - web/src/lib/api/meetings.ts
  - web/src/hooks/useGreeting.ts
  - web/src/hooks/useGreeting.test.ts
  - web/src/components/library/Sidebar.tsx
  - web/src/components/library/MeetingCard.tsx
  - web/src/components/library/DateGroup.tsx
  - web/src/components/library/DateGroup.test.ts
  - web/src/components/library/Greeting.tsx
  - web/src/routes/Library.tsx
  - web/src/App.tsx
autonomous: false
requirements:
  - LIB-01
  - LIB-02
  - LIB-03
  - LIB-04
  - LIB-05
  - LIB-08
  - LIB-09
  - LIB-10
user_setup: []

must_haves:
  truths:
    - "Visiting localhost:7878/ renders the Library route (not the meeting view)"
    - "Sidebar shows swirl logo + wordmark + '+ New meeting' blueberry button + 'All meetings' / 'Starred' nav + green 'Local-only · on' pill when no cloud provider active + Settings row"
    - "Main pane shows Instrument-Serif greeting + mono caption with meeting count"
    - "Meetings render grouped under TODAY / YESTERDAY / EARLIER headers with 42px tinted-avatar cards"
    - "Clicking a card navigates to /m/:id; clicking '+ New meeting' creates a meeting and navigates to it"
    - "DELETE /api/meetings/:id removes the SQLite row and cascades chat_messages but leaves the markdown file in place"
    - "Phase-3 in-memory meeting HashMap is gone — all reads/writes go through MeetingRepo + MarkdownExporter"
  artifacts:
    - path: "crates/yogurt-db/src/migrations/V003__meetings.sql"
      provides: "meetings table + chat_messages FK retrofit + first_run_completed seed"
      contains: "CREATE TABLE meetings"
    - path: "crates/yogurt-db/src/meetings.rs"
      provides: "MeetingRepo CRUD"
      contains: "impl MeetingRepo"
      min_lines: 200
    - path: "crates/yogurt-server/src/api/meetings.rs"
      provides: "REST handlers GET/POST/PATCH/DELETE /api/meetings[/:id]"
      contains: "fn router() -> Router<AppState>"
    - path: "web/src/routes/Library.tsx"
      provides: "Library route shell"
      contains: "Sidebar"
    - path: "web/src/components/library/Sidebar.tsx"
      provides: "212px left sidebar"
      contains: "Local-only"
    - path: "web/src/components/library/MeetingCard.tsx"
      provides: "Meeting card visual"
      contains: "42px"
    - path: "web/src/components/library/DateGroup.tsx"
      provides: "TODAY/YESTERDAY/EARLIER bucketing"
      contains: "bucketFor"
  key_links:
    - from: "web/src/routes/Library.tsx"
      to: "/api/meetings"
      via: "useMeetings TanStack-Query hook"
      pattern: "useMeetings"
    - from: "crates/yogurt-server/src/api/meetings.rs"
      to: "yogurt_db::MeetingRepo"
      via: "AppState.meetings"
      pattern: "s\\.meetings\\.(create|get|list|patch|delete)"
    - from: "crates/yogurt-server/src/ws.rs"
      to: "AppState::patch_and_export"
      via: "WS notes_edit / transcript-append / enhance-complete handlers"
      pattern: "patch_and_export"
---

<objective>
Promote the Library to the default route, replace the Phase-3 in-memory meeting store with SQLite-backed `MeetingRepo`, and ship the full sidebar + date-grouped meeting cards UI with REST + TanStack-Query CRUD wiring.

Purpose: This is the user's home screen after Phase 7. Without it, yogurt feels like a tech demo. It also retires the Phase-3 in-memory store — required so settings + library + chat agree on what meetings exist.
Output: V003 SQLite migration + MeetingRepo + REST endpoints + TanStack-Query hooks + Sidebar/MeetingCard/DateGroup/Greeting/Library components + App.tsx router rewire.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-library-onboarding-states/07-CONTEXT.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-7-library-and-onboarding.md
@docs/PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: V003 migration + MeetingRepo CRUD (TDD)</name>
  <files>crates/yogurt-db/Cargo.toml, crates/yogurt-db/src/migrations/V003__meetings.sql, crates/yogurt-db/src/meetings.rs, crates/yogurt-db/src/lib.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-7-library-and-onboarding.md, crates/yogurt-db/src/lib.rs, crates/yogurt-db/src/migrations/V002__chat_messages.sql</read_first>
  <action>
1. Add deps to `crates/yogurt-db/Cargo.toml`: `chrono = { version = "0.4", features = ["serde"] }`, `ulid = { version = "1.1", features = ["serde"] }`, `slug = "0.1"`, `serde_yaml = "0.9"`.
2. Create `crates/yogurt-db/src/migrations/V003__meetings.sql` exactly per superpowers Task 7.1 Step 3 — `meetings` table (id TEXT PRIMARY KEY ulid, title, started_at INTEGER, ended_at INTEGER nullable, notes_md TEXT default '', enriched_md TEXT nullable, transcript_json TEXT default '[]', starred INTEGER default 0, created_at INTEGER, updated_at INTEGER); index `idx_meetings_started` on `started_at DESC`; partial index `idx_meetings_starred` WHERE starred = 1; seed `INSERT OR IGNORE INTO settings (key, value) VALUES ('first_run_completed', 'false')`; rebuild `chat_messages` table with `FOREIGN KEY (meeting_id) REFERENCES meetings(id) ON DELETE CASCADE` (copy rows with `WHERE meeting_id IN (SELECT id FROM meetings)`).
3. Register V003 in `crates/yogurt-db/src/lib.rs` `MIGRATIONS` slice following the V001/V002 pattern with `include_str!`.
4. Ensure SQLite FK enforcement is on in `Db::open` / `Db::open_in_memory` — add `PRAGMA foreign_keys = ON` if not present (Phase-5 may have done this).
5. Write `crates/yogurt-db/src/meetings.rs` test-first per Task 7.2 Step 1: 7 tests covering ulid creation, empty-title rejection, newest-first list, patch updates `updated_at`, `MeetingPatch.enriched_md: Option<Option<String>>` distinguishes clear vs leave-alone, missing-id returns None/false/Err, and FK cascade-deletes chat_messages.
6. Implement `MeetingRepo::{new, create, get, list, patch, delete}` + `row_to_meeting` per Task 7.2 Step 3.
7. Add `pub mod meetings; pub use meetings::{Meeting, MeetingPatch, MeetingRepo, NewMeeting};` to `lib.rs`.
  </action>
  <verify>cd crates/yogurt-db && cargo test -p yogurt-db</verify>
  <acceptance_criteria>
    - V003__meetings.sql contains "CREATE TABLE meetings" and "REFERENCES meetings(id) ON DELETE CASCADE"
    - V003__meetings.sql contains "INSERT OR IGNORE INTO settings (key, value) VALUES ('first_run_completed'"
    - crates/yogurt-db/src/meetings.rs exports `pub struct MeetingRepo` with create/get/list/patch/delete methods
    - `MeetingPatch.enriched_md` is exactly `Option<Option<String>>` (not `Option<String>`)
    - `cargo test -p yogurt-db meetings` reports 7 passing tests (including `it_cascade_deletes_chat_messages`)
    - lib.rs re-exports `Meeting`, `MeetingPatch`, `MeetingRepo`, `NewMeeting`
  </acceptance_criteria>
  <done>7 MeetingRepo unit tests pass; V003 migration runs cleanly; chat_messages FK cascade verified.</done>
</task>

<task type="auto">
  <name>Task 2: REST endpoints + AppState wiring + replace in-memory store</name>
  <files>crates/yogurt-server/Cargo.toml, crates/yogurt-server/src/api/mod.rs, crates/yogurt-server/src/api/meetings.rs, crates/yogurt-server/src/lib.rs, crates/yogurt-server/src/meetings.rs, crates/yogurt-server/src/ws.rs, crates/yogurt-server/tests/meetings_api.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-7-library-and-onboarding.md, crates/yogurt-server/src/lib.rs, crates/yogurt-server/src/meetings.rs, crates/yogurt-server/src/ws.rs, crates/yogurt-server/src/markdown_export.rs</read_first>
  <action>
1. Extend `AppState` in `crates/yogurt-server/src/lib.rs` to include `pub meetings: Arc<MeetingRepo>` (re-use the existing `pub exporter: Arc<MarkdownExporter>` from Phase 4). Initialize in `run()`: `let meetings = Arc::new(MeetingRepo::new(db.clone()));`.
2. Add helper `impl AppState { pub fn patch_and_export(&self, id: &str, p: MeetingPatch) -> anyhow::Result<Meeting> { let m = self.meetings.patch(id, p)?; self.exporter.write(&m)?; Ok(m) } }`.
3. Create `crates/yogurt-server/src/api/meetings.rs` per Task 7.4 Step 5: `pub fn router() -> Router<AppState>` exposing GET/POST `/api/meetings` and GET/PATCH/DELETE `/api/meetings/:id`. Use `ApiError` enum with `NotFound / BadRequest(String) / Internal(anyhow::Error)` and `IntoResponse`. `create` calls `s.meetings.create(...)` + `s.exporter.write(&m)`. `patch_one` uses `s.patch_and_export(...)` and maps "not found" string in error to `NotFound`. `delete_one` returns `StatusCode::NO_CONTENT` on success, `NotFound` if no row removed — **deliberately does NOT delete the markdown file** (add comment per superpowers plan).
4. Add `pub mod meetings;` to `crates/yogurt-server/src/api/mod.rs`. Merge router in `lib.rs` `run()`: `.merge(api::meetings::router())` BEFORE `.with_state(state)`.
5. Replace Phase-3 in-memory store in `crates/yogurt-server/src/meetings.rs` and `crates/yogurt-server/src/ws.rs`: delete any `Arc<Mutex<HashMap<String, Meeting>>>` `MeetingsState`; rewire each call site to `state.meetings.<op>` or `state.patch_and_export(...)`. Specifically: `notes_edit` WS message → `state.patch_and_export(&id, MeetingPatch { notes_md: Some(md), ..Default::default() })`; transcript-chunk arrival → read-modify-write under a per-meeting `tokio::sync::Mutex` (use `DashMap<String, Arc<Mutex<()>>>` on `AppState` if not present) to avoid lost updates; `enhance_complete` → `state.patch_and_export(&id, MeetingPatch { enriched_md: Some(Some(md)), ended_at: Some(Some(Utc::now())), ..Default::default() })`.
6. Write integration tests in `crates/yogurt-server/tests/meetings_api.rs` per Task 7.4 Step 3 — 4 tests using `tempfile::tempdir()` + `YOGURT_HOME` env var: create+list returns the meeting; PATCH notes writes a `.md` file with front-matter; DELETE returns 204 then GET returns 404; GET missing returns 404. If `yogurt_server::bind()` does not exist (returning `(SocketAddr, impl Future)`), add it as a thin wrapper around `run()`.
7. Update any existing Phase-3 WS tests that read the in-memory store to instead read via `MeetingRepo::get` against the same `Db`.
  </action>
  <verify>cargo test -p yogurt-server && cargo test -p yogurt-server --test meetings_api</verify>
  <acceptance_criteria>
    - crates/yogurt-server/src/api/meetings.rs contains routes for GET/POST `/api/meetings` and GET/PATCH/DELETE `/api/meetings/:id`
    - crates/yogurt-server/src/lib.rs `AppState` contains `meetings: Arc<MeetingRepo>` and `exporter: Arc<MarkdownExporter>` fields
    - crates/yogurt-server/src/lib.rs contains `fn patch_and_export` helper
    - `grep -n "HashMap" crates/yogurt-server/src/meetings.rs crates/yogurt-server/src/ws.rs` returns NO matches for meeting state (any remaining HashMap usage is unrelated)
    - `cargo test -p yogurt-server --test meetings_api` reports 4 passing tests
    - `cargo test -p yogurt-server` (full server tests) passes
    - delete handler does NOT call `std::fs::remove_file` on the notes path
  </acceptance_criteria>
  <done>REST CRUD works against SQLite; markdown file written on every mutate; Phase-3 in-memory store fully removed; all existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Frontend TanStack hooks + Library route + Sidebar + cards + router rewire</name>
  <files>web/src/lib/api/meetings.ts, web/src/hooks/useGreeting.ts, web/src/hooks/useGreeting.test.ts, web/src/components/library/Sidebar.tsx, web/src/components/library/MeetingCard.tsx, web/src/components/library/DateGroup.tsx, web/src/components/library/DateGroup.test.ts, web/src/components/library/Greeting.tsx, web/src/routes/Library.tsx, web/src/App.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-7-library-and-onboarding.md, web/src/App.tsx, web/package.json</read_first>
  <action>
1. Create `web/src/lib/api/meetings.ts` per Task 7.6 Step 2: TypeScript `Meeting` + `MeetingPatch` interfaces matching the Rust JSON shape (ISO 8601 strings for dates, `enriched_md: string | null`, `starred: boolean`); `meetingsKey = ["meetings"]` and `meetingKey(id)` cache keys; `json<T>` helper that handles 204; hooks `useMeetings()` (5s staleTime), `useMeeting(id)`, `useCreateMeeting()` (POST title, invalidate list), `useDeleteMeeting()` (DELETE, invalidate list).
2. Create `web/src/hooks/useGreeting.ts` per Task 7.7 Step 1: returns `{ timeOfDay, name, greeting }` where `timeOfDay` cycles `morning < 12 / afternoon < 18 / evening`, `name` defaults to "you" with `nameOverride` param. Add Vitest `useGreeting.test.ts` with 4 cases (morning/afternoon/evening/override) per Task 7.7 Step 2.
3. Create `web/src/components/library/Sidebar.tsx` per Task 7.7 Step 3: 212px wide aside, `bg-[var(--paper)]` with `border-r border-[var(--line)]`. Top: SwirlLogo (28px) + "yogurt" wordmark in font-serif. Primary "+ New meeting" blueberry button calling `useCreateMeeting().mutateAsync("Untitled meeting")` then `nav(`/m/${m.id}`)`. NavLinks "All meetings" (active state = `bg-[var(--blsoft)] text-[var(--blue)]`) and "Starred". FOLDERS section with 3 hardcoded SAMPLE_FOLDERS (Work/blue, Hiring/straw, 1:1s/matcha), each `title="Coming in v1.1"` and opacity-60. Footer: `isLocalOnly = !settings.providers?.some(p => p.kind === "cloud" && p.active)` — when true, render matcha "Local-only · on" pill (`bg-[var(--mtsoft)] text-[var(--matcha)] rounded-full`). NavLink to `/settings` with ⚙ icon.
4. Create `web/src/components/library/MeetingCard.tsx` per Task 7.7 Step 4: deterministic `avatarTint(id)` cycling 3 PALETTE colors (`var(--blsoft)`, `var(--mtsoft)`, `#FBE6E0`) by string hash of ulid; `initials(title)` returns up to 2 uppercase initials or `·`; `formatMeta(m)` returns `{HH:MM} · {N} min · enhanced` (suffix iff `enriched_md != null`). Card is `<Link to={`/m/${m.id}`}>` with 42px rounded-[10px] avatar (font-serif 18px), Hanken-bold 15px title (truncated), JetBrains-Mono 12px meta, right-aligned `Local` badge pill.
5. Create `web/src/components/library/DateGroup.tsx` per Task 7.7 Step 4 (second block): export `bucketFor(d, now) -> "TODAY"|"YESTERDAY"|"EARLIER"` using local-midnight boundaries (clone+setHours(0,0,0,0), prior-day setDate-1). Export `groupMeetings(meetings, now)` returning `Record<Bucket, Meeting[]>`. `<DateGroup>` component renders sections for each non-empty bucket with `text-[11px] font-mono uppercase tracking-wider text-[var(--mut)]` headers and stacked MeetingCards. Add Vitest `DateGroup.test.ts` with 5 cases per Task 7.7 Step 5.
6. Create `web/src/components/library/Greeting.tsx` per Task 7.7 Step 7: `font-serif text-[40px]` greeting + mono caption `{count} meeting{s} · all on this Mac`.
7. Create `web/src/routes/Library.tsx` per Task 7.7 Step 7: `<div className="flex"><Sidebar /><main className="flex-1 px-12 py-10 max-w-[860px]">…</main></div>`. Renders Greeting + (search slot — leave a placeholder `<div data-search-slot />` that plan 02 fills) + `<DateGroup>` when meetings.length > 0, else `<EmptyLibrary />` placeholder (plan 04 will create the real EmptyLibrary; for now import is allowed to fail-soft via `// @ts-expect-error wired in plan 04` or import a temporary `() => null` stub component). Use `useScreenRecordingStatus()` from Phase 2 to short-circuit to PermissionDenied (also plan 04 stub).
8. Rewire `web/src/App.tsx` per Task 7.10 Step 2: `<QueryClientProvider><BrowserRouter><Shell></Shell></BrowserRouter></QueryClientProvider>` where Shell mounts `useFirstRunRedirect()` (plan 04 owns the hook implementation; for this plan, conditionally call only if the import resolves — or land an empty `useFirstRunRedirect.ts` stub and let plan 04 fill it). Routes: `/` → Library, `/welcome` → Welcome (plan 04), `/m/:id` → existing Meeting (Phase 3), `/settings` → existing Settings (Phase 5), `/starred` → `<Navigate to="/" replace />`, `*` → `<Navigate to="/" replace />`.
9. Run `pnpm --dir web test`: prior tests + 4 new `useGreeting` + 5 new `DateGroup` cases pass.
  </action>
  <verify>pnpm --dir web test && pnpm --dir web build</verify>
  <acceptance_criteria>
    - web/src/lib/api/meetings.ts exports `useMeetings`, `useMeeting`, `useCreateMeeting`, `useDeleteMeeting`
    - web/src/components/library/Sidebar.tsx contains "+ New meeting" and "Local-only · on"
    - web/src/components/library/Sidebar.tsx contains the conditional `isLocalOnly` check on `p.kind === "cloud"`
    - web/src/components/library/MeetingCard.tsx contains `w-[42px] h-[42px]` and reads `m.enriched_md` for the enhanced suffix
    - web/src/components/library/DateGroup.tsx exports `bucketFor` and `groupMeetings`
    - web/src/routes/Library.tsx renders `<Sidebar />` and `<DateGroup meetings=...>`
    - web/src/App.tsx routes `/` to Library and `/m/:id` to Meeting
    - DateGroup.test.ts and useGreeting.test.ts pass
    - `pnpm --dir web build` succeeds
  </acceptance_criteria>
  <done>Library renders at `/`, sidebar + greeting + cards visible against seeded data, all unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 4: Start dev server for human verification</name>
  <files></files>
  <read_first></read_first>
  <action>Run `pnpm --dir web build` to populate embedded assets, then `cargo run -p yogurt -- start --dev --no-open --port 7878` in background. Wait until `http://localhost:7878/api/health` returns 200. Seed 2-3 meetings via curl POST so the date-grouping is visible.</action>
  <verify>curl -sf http://localhost:7878/api/health && curl -sf http://localhost:7878/api/meetings</verify>
  <acceptance_criteria>
    - Server responds 200 on /api/health
    - At least 2 meetings exist via POST /api/meetings
  </acceptance_criteria>
  <done>Server up, meetings seeded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Library home view at http://localhost:7878/ — 212px sidebar with logo + "+ New meeting" + nav + Local-only pill + Settings; main pane with Instrument-Serif greeting + mono caption + date-grouped meeting cards (42px tinted avatars + Hanken-bold title + mono meta + Local badge).</what-built>
  <how-to-verify>
Visit http://localhost:7878/ and check:
- Sidebar is 212px wide with the swirl logo + "yogurt" wordmark at top, blueberry "+ New meeting" button below, "All meetings" nav row highlighted in lilac/blueberry, "Starred" below, 3 grey sample folders with hover tooltip "Coming in v1.1", green "Local-only · on" matcha pill above the Settings row at the bottom.
- Main pane shows "Good {morning|afternoon|evening}, you" in Instrument Serif at ~40px, "{N} meetings · all on this Mac" in mono caption underneath.
- Meeting cards group under TODAY / YESTERDAY / EARLIER headers (mono uppercase, muted color); each card has a 42px rounded square avatar with serif initials in a soft color tint, bold title, mono meta line, "Local" badge on the right.
- Clicking "+ New meeting" creates a meeting and navigates to /m/&lt;id&gt; (Phase 3 meeting view).
- Clicking any card navigates to /m/&lt;id&gt;.
- Check ~/.yogurt/notes/ — a markdown file with YAML front-matter should exist for each meeting (Phase 4 MarkdownExporter wiring).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p yogurt-db meetings` passes (7 tests)
- [ ] `cargo test -p yogurt-server --test meetings_api` passes (4 tests)
- [ ] `cargo test -p yogurt-server` (full server) passes
- [ ] `pnpm --dir web test` passes (useGreeting × 4, DateGroup × 5)
- [ ] `pnpm --dir web build` succeeds
- [ ] `grep -n "HashMap" crates/yogurt-server/src/meetings.rs` finds no meeting state
- [ ] Library renders at `/`; clicking "+ New meeting" creates one and navigates
- [ ] User approved visual layout
</verification>

<success_criteria>
- All 4 tasks completed
- LIB-01, LIB-02, LIB-03, LIB-04, LIB-05, LIB-08, LIB-09, LIB-10 satisfied
- MeetingRepo replaces the in-memory store; markdown file written on every mutate
- Visual checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/07-library-onboarding-states/07-01-SUMMARY.md`
</output>
