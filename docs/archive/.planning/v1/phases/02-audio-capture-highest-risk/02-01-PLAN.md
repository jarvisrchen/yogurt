---
phase: 02-audio-capture-highest-risk
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/yogurt-audio/Cargo.toml
  - crates/yogurt-audio/README.md
  - crates/yogurt-audio/src/lib.rs
  - crates/yogurt-audio/src/error.rs
  - crates/yogurt-audio/src/frame.rs
  - crates/yogurt-audio/src/permission.rs
  - crates/yogurt-audio/src/synthetic.rs
  - crates/yogurt-audio/tests/frame_contract.rs
  - crates/yogurt-audio/tests/permission.rs
  - crates/yogurt-audio/tests/synthetic.rs
  - docs/superpowers/notes/2026-06-25-sck-spike-result.md
autonomous: false
requirements:
  - AUDIO-01
must_haves:
  truths:
    - "SCK audio-loopback spike outcome (PASS/PARTIAL/FAIL) is committed and decides Plan 02 path"
    - "yogurt-audio crate builds clean on macOS and Linux (non-macOS via cfg gating)"
    - "Format constants SAMPLE_RATE_HZ=16000 and FRAME_SAMPLES=320 are exported from yogurt-audio"
    - "PermissionStatus enum (Granted/Denied/NotRequired) returned by has_screen_recording_permission()"
    - "Synthetic sine-wave generator emits FRAME_SAMPLES-length Frame at 20ms cadence via broadcast channel"
  artifacts:
    - path: "docs/superpowers/notes/2026-06-25-sck-spike-result.md"
      provides: "Spike outcome (PASS/PARTIAL/FAIL) and the decision driving Task 2.6 path"
      contains: "Outcome"
    - path: "crates/yogurt-audio/Cargo.toml"
      provides: "yogurt-audio crate manifest with cpal, screencapturekit, rubato, thiserror deps"
      contains: "name = \"yogurt-audio\""
    - path: "crates/yogurt-audio/src/frame.rs"
      provides: "Frame struct, Channel enum, SAMPLE_RATE_HZ, FRAME_SAMPLES constants"
      contains: "pub const SAMPLE_RATE_HZ"
    - path: "crates/yogurt-audio/src/error.rs"
      provides: "AudioError enum with PermissionDenied/MicUnavailable/SystemCaptureFailed/UnsupportedPlatform variants"
      contains: "PermissionDenied"
    - path: "crates/yogurt-audio/src/permission.rs"
      provides: "has_screen_recording_permission() + PermissionStatus enum + CGPreflightScreenCaptureAccess extern"
      contains: "CGPreflightScreenCaptureAccess"
    - path: "crates/yogurt-audio/src/synthetic.rs"
      provides: "Sine-wave PCM generator producing Frame at 20ms cadence"
      contains: "spawn_sine_wave"
  key_links:
    - from: "Cargo.toml (root)"
      to: "crates/yogurt-audio/"
      via: "workspace members list + workspace.dependencies"
      pattern: "yogurt-audio"
    - from: "crates/yogurt-audio/src/lib.rs"
      to: "frame, error, permission, synthetic modules"
      via: "pub mod / mod declarations"
      pattern: "pub use.*Frame|pub use.*PermissionStatus"
---

<objective>
Scaffold the `yogurt-audio` crate and retire the highest project-killer risk via a mandatory ScreenCaptureKit audio-loopback spike.

Purpose: Phase 2 is the highest-risk phase in the v1 roadmap because the `screencapturekit` crate is "mainly designed for screen capture" (PRD §13 risk #1). Before we write any production system-audio code in Plan 02, we must prove a one-stream audio-only `SCStream` config delivers non-zero PCM bytes on this Mac — or pivot the next plan to a Swift sidecar fallback path. This plan also lays down the format contract (16 kHz mono i16, 320-sample frames), the permission detection surface, and the broadcast-channel plumbing tested via a synthetic sine-wave generator.

Output:
- `docs/superpowers/notes/2026-06-25-sck-spike-result.md` — the spike decision Plan 02 hinges on.
- `crates/yogurt-audio/` crate scaffold with `Cargo.toml`, `README.md`, `src/{lib,error,frame,permission,synthetic}.rs`, integration tests for the format contract, synthetic plumbing, and permission detection.
- AUDIO-01 satisfied at the API surface: `has_screen_recording_permission()` returns a typed `PermissionStatus`; `request_screen_recording_permission()` triggers the TCC dialog.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-audio-capture-highest-risk/02-CONTEXT.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-2-audio-capture.md
@docs/PITFALLS.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SCK audio-loopback spike (Task 2.0 from superpowers plan)</name>
  <files>crates/yogurt-audio/spike/Cargo.toml, crates/yogurt-audio/spike/src/main.rs, docs/superpowers/notes/2026-06-25-sck-spike-result.md, .gitignore</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-2-audio-capture.md, docs/PITFALLS.md, docs/PRD.md</read_first>
  <action>
Execute Task 2.0 from the superpowers plan exactly (see lines 101–286 of the plan file). Steps:

1. `cargo search screencapturekit --limit 5` — note the latest 0.3.x version. If only 0.2.x exists, STOP and re-check before proceeding (public API differs).

2. Create throwaway spike crate at `crates/yogurt-audio/spike/` (NOT a workspace member — add to root `.gitignore` for the duration of the spike). Write `Cargo.toml` per plan lines 119–132 (deps: `screencapturekit = "0.3"`, `tokio = { version = "1", features = ["full"] }`, `anyhow = "1"`).

3. Write `crates/yogurt-audio/spike/src/main.rs` per plan lines 138–207. Configures `SCStream` with `captures_audio: true`, `excludes_current_process_audio: true`, no video, logs every audio buffer arrival for 5 seconds.

4. Open Spotify (or any system audio source). Then `cd crates/yogurt-audio/spike && cargo run --release`. On first run, macOS triggers the Screen Recording permission dialog — grant it, then quit and re-run (TCC requires relaunch).

5. Interpret outcome per plan lines 229–235:
   - **PASS** (non-zero bytes captured) → Plan 02 uses Path A (in-process SCK).
   - **PARTIAL** (callbacks fire, bytes all zero) → spend at most 30 more minutes debugging; if still zero, **FAIL**.
   - **FAIL** (compile fails, no callbacks, panic) → Plan 02 uses Path B (Swift sidecar).

6. Write `docs/superpowers/notes/2026-06-25-sck-spike-result.md` per plan lines 241–263. Must contain: Outcome (PASS/PARTIAL/FAIL), Machine, screencapturekit version tested, what worked / what didn't, Decision ("Proceed with in-process SCK (Task 2.5 path A)" OR "Switch to Swift sidecar (Task 2.5 path B)"), Notes for Task 2.5 implementer.

7. Delete the spike directory: `rm -rf crates/yogurt-audio/spike/`. Commit ONLY the result note: `git commit -m "docs(audio): record SCK audio-loopback spike result (phase 2 task 2.0)"`.

Do NOT skip the relaunch step in (4) — TCC permission grant requires it.
  </action>
  <verify>
test -f docs/superpowers/notes/2026-06-25-sck-spike-result.md && grep -E "^(## Outcome|PASS|PARTIAL|FAIL)" docs/superpowers/notes/2026-06-25-sck-spike-result.md && test ! -d crates/yogurt-audio/spike/
  </verify>
  <acceptance_criteria>
    - docs/superpowers/notes/2026-06-25-sck-spike-result.md exists and contains "PASS", "PARTIAL", or "FAIL" as the outcome
    - docs/superpowers/notes/2026-06-25-sck-spike-result.md contains a "Decision" section naming Path A or Path B
    - crates/yogurt-audio/spike/ directory has been deleted (throwaway code not committed)
    - The note records the exact screencapturekit crate version tested
  </acceptance_criteria>
  <done>Spike decision is committed and unambiguous; Plan 02 implementer can read this note and know which path (A or B) to take.</done>
</task>

<task type="auto">
  <name>Task 2: Bootstrap yogurt-audio crate (Task 2.1 + Task 2.2 from superpowers plan)</name>
  <files>Cargo.toml, crates/yogurt-audio/Cargo.toml, crates/yogurt-audio/README.md, crates/yogurt-audio/src/lib.rs, crates/yogurt-audio/src/frame.rs, crates/yogurt-audio/src/error.rs, crates/yogurt-audio/tests/frame_contract.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-2-audio-capture.md, Cargo.toml, crates/yogurt-server/Cargo.toml</read_first>
  <action>
Execute Tasks 2.1 + 2.2 from superpowers plan (lines 290–582).

**Task 2.1 — workspace + crate scaffold:**

1. Modify root `Cargo.toml`:
   - Add `"crates/yogurt-audio"` to `[workspace] members`.
   - Append to `[workspace.dependencies]`:
     ```toml
     # Audio capture
     cpal = "0.15"
     screencapturekit = "0.3"
     rubato = "0.16"
     # Typed errors
     thiserror = "2"
     ```

2. Write `crates/yogurt-audio/Cargo.toml` per plan lines 326–355. Dependencies: `tokio`, `tracing`, `anyhow`, `serde`, `thiserror`, `cpal`, `rubato` (all `workspace = true`). Under `[target.'cfg(target_os = "macos")'.dependencies]`: `screencapturekit = { workspace = true }`, `objc2 = "0.5"`, `objc2-foundation = "0.2"`. Under `[dev-dependencies]`: `tokio` and `tracing-subscriber`.

3. Write placeholder `crates/yogurt-audio/src/lib.rs` per plan lines 361–376:
   ```rust
   //! Yogurt audio capture — 16 kHz mono i16 PCM, two channels (mic + system).
   //!
   //! Phase 2 scope: capture only. STT is Phase 3.

   #![deny(rust_2018_idioms, missing_debug_implementations)]

   pub fn version() -> &'static str {
       env!("CARGO_PKG_VERSION")
   }
   ```

4. Write `crates/yogurt-audio/README.md` per plan lines 380–411 — documents the 16 kHz / mono / i16 / 320-sample contract, permissions, non-macOS platforms.

5. Run `cargo build -p yogurt-audio` and confirm clean build.

**Task 2.2 — public types (Frame, Channel, AudioError, format consts):**

6. Write `crates/yogurt-audio/tests/frame_contract.rs` per plan lines 440–463 (TDD-style: tests first). Three tests:
   - `it_exposes_format_constants` — `SAMPLE_RATE_HZ == 16_000`, `FRAME_SAMPLES == 320`.
   - `it_constructs_a_frame_with_correct_length` — `Frame::new(Channel::Mic, 0, vec![0i16; FRAME_SAMPLES])`.
   - `it_panics_on_wrong_length` — `Frame::new(Channel::Mic, 0, vec![0i16; 100])` panics with "FRAME_SAMPLES".

7. Write `crates/yogurt-audio/src/frame.rs` per plan lines 472–515:
   - `pub const SAMPLE_RATE_HZ: u32 = 16_000;`
   - `pub const FRAME_SAMPLES: usize = 320;`
   - `pub enum Channel { Mic, System }` with serde rename_all="lowercase"
   - `pub struct Frame { channel: Channel, monotonic_ms: u64, samples: Vec<i16> }`
   - `Frame::new()` asserts `samples.len() == FRAME_SAMPLES`.

8. Write `crates/yogurt-audio/src/error.rs` per plan lines 521–553:
   - `pub enum AudioError` with variants: `PermissionDenied`, `MicUnavailable(String)`, `SystemCaptureFailed(String)`, `UnsupportedPlatform`, `Cpal(String)`, `Io(#[from] std::io::Error)`.
   - `pub type Result<T> = std::result::Result<T, AudioError>;`

9. Update `crates/yogurt-audio/src/lib.rs` per plan lines 558–569:
   ```rust
   #![deny(rust_2018_idioms, missing_debug_implementations)]

   mod error;
   mod frame;

   pub use error::{AudioError, Result};
   pub use frame::{Channel, Frame, FRAME_SAMPLES, SAMPLE_RATE_HZ};
   ```

10. Run `cargo test -p yogurt-audio --test frame_contract` — expect 3 passed.
  </action>
  <verify>
cargo build -p yogurt-audio && cargo test -p yogurt-audio --test frame_contract
  </verify>
  <acceptance_criteria>
    - Root Cargo.toml contains "crates/yogurt-audio" in workspace members and cpal/screencapturekit/rubato/thiserror in workspace.dependencies
    - crates/yogurt-audio/Cargo.toml contains `name = "yogurt-audio"` and macOS-gated screencapturekit dependency
    - crates/yogurt-audio/src/frame.rs contains `pub const SAMPLE_RATE_HZ: u32 = 16_000` and `pub const FRAME_SAMPLES: usize = 320`
    - crates/yogurt-audio/src/error.rs contains `PermissionDenied`, `MicUnavailable`, `SystemCaptureFailed`, `UnsupportedPlatform` variants
    - `cargo test -p yogurt-audio --test frame_contract` reports 3 passed
    - `cargo build -p yogurt-audio` succeeds clean
  </acceptance_criteria>
  <done>yogurt-audio crate is wired into the workspace; Frame/Channel/AudioError/format constants are exported and tested.</done>
</task>

<task type="auto">
  <name>Task 3: Permission detection + synthetic sine-wave generator (Task 2.3 + Task 2.4 from superpowers plan)</name>
  <files>crates/yogurt-audio/Cargo.toml, crates/yogurt-audio/src/lib.rs, crates/yogurt-audio/src/permission.rs, crates/yogurt-audio/src/synthetic.rs, crates/yogurt-audio/tests/synthetic.rs, crates/yogurt-audio/tests/permission.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-2-audio-capture.md, crates/yogurt-audio/src/frame.rs, crates/yogurt-audio/src/lib.rs</read_first>
  <action>
Execute Tasks 2.3 + 2.4 from superpowers plan (lines 586–1008).

**Task 2.3 — synthetic PCM generator + broadcast plumbing tests:**

1. Append `[features]` to `crates/yogurt-audio/Cargo.toml`:
   ```toml
   [features]
   default = []
   synthetic = []
   ```

2. Write `crates/yogurt-audio/tests/synthetic.rs` per plan lines 612–680. Two `#[tokio::test]` tests:
   - `it_emits_correct_length_frames_at_the_expected_cadence` — 5 frames over ~100 ms, each `FRAME_SAMPLES` long, monotonic_ms increases ~20 ms per frame, sine values non-zero and non-constant.
   - `multiple_subscribers_each_receive_the_same_frames` — two receivers see identical `monotonic_ms`, `samples`, and `Channel::System` tag.

3. Write `crates/yogurt-audio/src/synthetic.rs` per plan lines 689–777:
   - `pub struct SineWaveConfig { channel: Channel, frequency_hz: f32, amplitude: i16 }` with `default_for(channel)` ctor (440 Hz, amplitude 16_000).
   - `pub fn spawn_sine_wave(cfg, tx) -> JoinHandle<()>` — emits sine-wave frames at 20 ms cadence via `tokio::time::interval` with `MissedTickBehavior::Delay`.
   - Internal `fn generate_chunk(cfg, frame_idx) -> Vec<i16>` uses **global sample index** (`frame_idx * FRAME_SAMPLES + i`) to avoid phase discontinuity between frames.
   - Inline `#[cfg(test)]` unit tests: `it_generates_frame_sized_chunks` + `it_produces_continuous_phase_across_frames`.

4. Update `crates/yogurt-audio/src/lib.rs` per plan lines 781–791:
   ```rust
   #![deny(rust_2018_idioms, missing_debug_implementations)]

   mod error;
   mod frame;

   #[cfg(any(test, feature = "synthetic"))]
   pub mod synthetic;

   pub use error::{AudioError, Result};
   pub use frame::{Channel, Frame, FRAME_SAMPLES, SAMPLE_RATE_HZ};
   ```

5. Run `cargo test -p yogurt-audio --features synthetic` — expect 3 frame_contract + 2 synthetic unit + 2 synthetic integration = 7 passed.

**Task 2.4 — permission detection:**

6. Write `crates/yogurt-audio/src/permission.rs` per plan lines 821–919:
   - `pub enum PermissionStatus { Granted, Denied, NotRequired }` with serde rename_all="snake_case".
   - `pub fn has_screen_recording_permission() -> PermissionStatus` — `#[cfg(target_os = "macos")]` calls `macos::check()`, else returns `NotRequired`.
   - `pub fn request_screen_recording_permission() -> PermissionStatus` — `#[cfg(target_os = "macos")]` calls `macos::request()`, else returns `NotRequired`. Docstring documents TCC "restart once" limitation.
   - `#[cfg(target_os = "macos")] mod macos { extern "C" { fn CGPreflightScreenCaptureAccess() -> bool; fn CGRequestScreenCaptureAccess() -> bool; } pub fn check() {...} pub fn request() {...} }`
   - Inline `#[cfg(test)]` unit tests: returns `NotRequired` on non-macOS, returns one of `Granted`/`Denied` on macOS.

7. Update `crates/yogurt-audio/src/lib.rs` per plan lines 924–937:
   ```rust
   mod error;
   mod frame;
   pub mod permission;

   #[cfg(any(test, feature = "synthetic"))]
   pub mod synthetic;

   pub use error::{AudioError, Result};
   pub use frame::{Channel, Frame, FRAME_SAMPLES, SAMPLE_RATE_HZ};
   pub use permission::{
       has_screen_recording_permission, request_screen_recording_permission, PermissionStatus,
   };
   ```

8. Write `crates/yogurt-audio/tests/permission.rs` per plan lines 943–989 — `#![cfg(target_os = "macos")]`, single `#[test] #[ignore]` test prints the two-pass manual smoke checklist (PASS 1 without permission, PASS 2 with permission, plus the Apple Silicon / Intel / macOS 13 / 14 / 15 verification list).

9. Run `cargo test -p yogurt-audio permission` — expect the unit test passes; the integration test is reported as `ignored`.
  </action>
  <verify>
cargo test -p yogurt-audio --features synthetic && cargo build -p yogurt-audio --all-targets
  </verify>
  <acceptance_criteria>
    - crates/yogurt-audio/Cargo.toml contains a `[features]` block with `synthetic = []`
    - crates/yogurt-audio/src/synthetic.rs contains `pub fn spawn_sine_wave` and uses `tokio::time::interval` with `MissedTickBehavior::Delay`
    - crates/yogurt-audio/src/synthetic.rs uses global sample index (frame_idx * FRAME_SAMPLES) for phase continuity
    - crates/yogurt-audio/src/permission.rs contains `extern "C" { fn CGPreflightScreenCaptureAccess() -> bool;`
    - crates/yogurt-audio/src/permission.rs contains the three-variant PermissionStatus enum (Granted, Denied, NotRequired)
    - crates/yogurt-audio/src/lib.rs re-exports has_screen_recording_permission, request_screen_recording_permission, PermissionStatus
    - `cargo test -p yogurt-audio --features synthetic` reports at least 7 passed
    - `cargo test -p yogurt-audio permission` reports the manual-smoke integration test as ignored, not failed
  </acceptance_criteria>
  <done>AUDIO-01 API surface satisfied — first-record permission flow is wired (the prompt fires once Task 2.6 in Plan 02 calls `request_screen_recording_permission()` or `start_capture()` lands). Synthetic broadcast plumbing tested end-to-end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `docs/superpowers/notes/2026-06-25-sck-spike-result.md` exists and names Path A or Path B
- [ ] `crates/yogurt-audio/spike/` directory has been deleted
- [ ] `cargo build -p yogurt-audio --all-targets` succeeds clean (macOS)
- [ ] `cargo test -p yogurt-audio --features synthetic` reports at least 7 passed
- [ ] `cargo test -p yogurt-audio permission` reports the manual-smoke integration test as ignored
- [ ] `cargo clippy -p yogurt-audio --all-targets -- -D warnings` is clean
- [ ] Three commits land in order: spike result note, crate bootstrap + types, permission + synthetic
</verification>

<success_criteria>
- All tasks completed
- Spike outcome unambiguously committed; Plan 02 has a clear Path A vs Path B directive
- AUDIO-01 satisfied at the API level (permission detection + request functions exposed)
- Format constants (SAMPLE_RATE_HZ, FRAME_SAMPLES) and core types (Frame, Channel, AudioError) are stable for downstream consumers
- Synthetic sine-wave generator validates the broadcast-channel plumbing pattern Plan 02 will reuse
- No compilation errors on non-macOS targets (`Channel::System` path returns `UnsupportedPlatform` instead of failing to compile)
</success_criteria>

<output>
After completion, create `.planning/phases/02-audio-capture-highest-risk/02-01-SUMMARY.md` capturing: spike outcome (PASS/PARTIAL/FAIL → Path A or B), test pass counts, any deviations from the superpowers plan, and a one-line directive for Plan 02 ("Implement system audio capture via in-process SCK" OR "Implement system audio capture via Swift sidecar").
</output>
