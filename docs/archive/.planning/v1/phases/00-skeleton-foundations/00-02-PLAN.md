---
phase: 00-skeleton-foundations
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - web/package.json
  - web/tsconfig.json
  - web/vite.config.ts
  - web/index.html
  - web/src/main.tsx
  - web/src/App.tsx
  - web/src/App.test.tsx
  - web/src/index.css
  - web/src/lib/api.ts
  - web/src/vitest.setup.ts
  - crates/yogurt-server/src/assets.rs
  - crates/yogurt-server/src/dev_proxy.rs
  - crates/yogurt-server/src/routes.rs
  - crates/yogurt-server/src/lib.rs
  - crates/yogurt-server/tests/embedded.rs
autonomous: true
requirements:
  - FOUND-03
user_setup: []

must_haves:
  truths:
    - "`pnpm --dir web install && pnpm --dir web build` produces `web/dist/index.html`"
    - "`pnpm --dir web test` passes the App smoke tests"
    - "In Release mode, `GET /` returns the embedded `index.html` (containing `<div id=\"root\">`) and unknown paths SPA-fallback to the same HTML"
    - "In Dev mode with Vite running on :5173, `GET /` on yogurt-server returns the Vite-served React app and `/api/health` still returns JSON directly"
    - "In Dev mode with Vite NOT running, `GET /` returns HTTP 502 with actionable copy mentioning `pnpm --dir web dev`"
  artifacts:
    - path: "web/package.json"
      provides: "Vite 6 + React 19 + Tailwind 4 + TipTap 2 + Vitest 2 scaffold"
      contains: '"react": "^19'
    - path: "web/vite.config.ts"
      provides: "Vite config proxying /api + /ws to :7878 with vitest jsdom config"
      contains: "proxy"
    - path: "web/src/App.tsx"
      provides: "React root rendering yogurt headline + TipTap editor + health fetch"
      contains: "useEditor"
    - path: "web/src/App.test.tsx"
      provides: "Vitest smoke tests for App"
      contains: "describe"
    - path: "crates/yogurt-server/src/assets.rs"
      provides: "rust-embed WebDist struct + serve_embedded SPA fallback handler"
      contains: "RustEmbed"
    - path: "crates/yogurt-server/src/dev_proxy.rs"
      provides: "reqwest-based dev proxy to http://127.0.0.1:5173"
      contains: "5173"
  key_links:
    - from: "crates/yogurt-server/src/routes.rs"
      to: "crates/yogurt-server/src/assets.rs"
      via: "Release-mode fallback handler"
      pattern: "serve_embedded"
    - from: "crates/yogurt-server/src/routes.rs"
      to: "crates/yogurt-server/src/dev_proxy.rs"
      via: "Dev-mode fallback handler"
      pattern: "proxy_to_vite"
    - from: "crates/yogurt-server/src/assets.rs"
      to: "web/dist/"
      via: "rust-embed folder attribute"
      pattern: 'folder\s*=\s*"\.\./\.\./web/dist/"'
---

<objective>
Scaffold the React + Vite + Tailwind 4 + TipTap web app, wire Vitest smoke tests, embed `web/dist` into the server binary via rust-embed (with SPA fallback to `index.html`), and implement the Dev-mode proxy from the axum server to the Vite dev server on :5173.

Purpose: Deliver FOUND-03 — `yogurt start` (Release) serves the React SPA from the embedded binary, and `yogurt start --dev` proxies to Vite for HMR. This is the load-bearing single-static-binary distribution mechanism.
Output: Working two-mode static-asset pipeline. Release embeds; Dev proxies. SPA fallback returns `index.html` for unknown routes so client-side routing works post-Phase 1.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-skeleton-foundations/00-CONTEXT.md
@.planning/phases/00-skeleton-foundations/00-01-SUMMARY.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-0-skeleton.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the Vite + React + Tailwind 4 + TipTap web app</name>
  <files>web/package.json, web/tsconfig.json, web/vite.config.ts, web/index.html, web/src/main.tsx, web/src/App.tsx, web/src/index.css, web/src/lib/api.ts</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-0-skeleton.md</read_first>
  <action>Follow superpowers plan Task 0.5 Steps 1-11 exactly.

1. Verify `pnpm --version` reports 9.x; if missing, `brew install pnpm`.
2. Create `web/package.json` with EXACT content from Task 0.5 Step 2: `name="yogurt-web"`, `private=true`, `version="0.1.0"`, `type="module"`, scripts `dev: vite`, `build: tsc && vite build`, `preview: vite preview`, `test: vitest run`, `test:watch: vitest`. Dependencies: `@tiptap/core ^2.10.0`, `@tiptap/react ^2.10.0`, `@tiptap/starter-kit ^2.10.0`, `react ^19.0.0`, `react-dom ^19.0.0`. DevDependencies: `@tailwindcss/vite ^4.0.0`, `@testing-library/react ^16.1.0`, `@types/react ^19.0.0`, `@types/react-dom ^19.0.0`, `@vitejs/plugin-react ^4.3.0`, `jsdom ^25.0.0`, `tailwindcss ^4.0.0`, `typescript ^5.6.0`, `vite ^6.0.0`, `vitest ^2.1.0`.
3. Create `web/tsconfig.json` per Task 0.5 Step 3: target ES2022, lib ES2022+DOM+DOM.Iterable, module ESNext, moduleResolution bundler, jsx react-jsx, strict true, noUnusedLocals/Parameters/FallthroughCasesInSwitch true, esModuleInterop true, isolatedModules true, skipLibCheck true, resolveJsonModule true, allowImportingTsExtensions false, noEmit true, types ["vitest/globals"], include ["src", "vite.config.ts"].
4. Create `web/vite.config.ts` per Task 0.5 Step 4 — first line MUST be `/// <reference types="vitest/config" />` (so `tsc --noEmit` recognizes the test block). Use `defineConfig` from `vite`, import `react` from `@vitejs/plugin-react`, import `tailwindcss` from `@tailwindcss/vite`. Plugins: `[react(), tailwindcss()]`. Server: port 5173, strictPort true, proxy `/api -> http://localhost:7878` and `/ws -> { target: "ws://localhost:7878", ws: true }`. Test block: `{ environment: "jsdom", globals: true }`.
5. Create `web/index.html` per Task 0.5 Step 5: standard doctype, `<title>yogurt</title>`, `<div id="root"></div>`, `<script type="module" src="/src/main.tsx"></script>`.
6. Create `web/src/index.css` per Task 0.5 Step 6: `@import "tailwindcss";`, `:root { color-scheme: light; }`, `body { margin: 0; font-family: system-ui, -apple-system, "Segoe UI", sans-serif; background: #FBF7EF; color: #211D18; }`. Note: brand palette is a hardcoded throwaway — Phase 1 replaces with proper design tokens.
7. Create `web/src/lib/api.ts` per Task 0.5 Step 7: export interface `HealthResponse { status: string; service: string }` and `export async function fetchHealth(): Promise<HealthResponse>` GETting `/api/health` (the Vite proxy forwards to :7878).
8. Create `web/src/App.tsx` per Task 0.5 Step 8: uses `useEditor` + `EditorContent` from `@tiptap/react`, `StarterKit` from `@tiptap/starter-kit`, `useEffect` + `useState` from React. Renders a `<main>` with the "yogurt" headline, a code-styled health line showing `{health.service} {health.status}` (or "loading…" before fetch resolves), and a bordered section wrapping the editor. Editor content: `<p>Type something — TipTap is working.</p>`.
9. Create `web/src/main.tsx` per Task 0.5 Step 9: React 19 `createRoot(document.getElementById("root")!)` rendering `<StrictMode><App /></StrictMode>` after importing `./index.css`.
10. Run `pnpm --dir web install` — expect lockfile + ~300 packages.
11. Run `pnpm --dir web build` — must succeed, produces `web/dist/index.html` + hashed assets (du -sh ≈ 300-500KB). This is required for the embedded test in Task 3.

DO NOT yet add the Vitest smoke test — Task 2 handles that.</action>
  <verify>pnpm --dir web install && pnpm --dir web build && test -f web/dist/index.html</verify>
  <acceptance_criteria>
    - `web/package.json` declares `react: ^19.0.0`, `vite: ^6.0.0`, `tailwindcss: ^4.0.0`, `@tiptap/react: ^2.10.0`, `vitest: ^2.1.0`
    - `web/vite.config.ts` first line is `/// <reference types="vitest/config" />` AND contains `port: 5173`, `strictPort: true`, proxy rules for `/api` and `/ws`
    - `web/src/App.tsx` imports `useEditor` from `@tiptap/react` and calls `fetchHealth()` from `./lib/api`
    - `pnpm --dir web build` succeeds and produces `web/dist/index.html` containing `<div id="root">`
  </acceptance_criteria>
  <done>Frontend installs, builds, and produces a bundled `web/dist/index.html` ready for embedding in Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Add Vitest smoke test for App</name>
  <files>web/src/App.test.tsx, web/src/vitest.setup.ts, web/vite.config.ts, web/package.json</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-0-skeleton.md, web/src/App.tsx, web/vite.config.ts</read_first>
  <action>Follow superpowers plan Task 0.6 Steps 1-3.

1. Create `web/src/App.test.tsx` per Task 0.6 Step 1: imports from `vitest`, `@testing-library/react`, and `./App`. Mocks `./lib/api` so `fetchHealth` resolves `{ status: "ok", service: "yogurt-server" }`. Two `it()` blocks: (a) "renders the yogurt headline" — uses `findByRole` to await the heading matching `/yogurt/i`; (b) "shows the health response once fetched" — uses `waitFor` + `getByText(/yogurt-server ok/)`.
2. Install jest-dom matchers: `pnpm --dir web add -D @testing-library/jest-dom`. Create `web/src/vitest.setup.ts` containing only `import "@testing-library/jest-dom/vitest";`.
3. Update `web/vite.config.ts` test block to include `setupFiles: ["./src/vitest.setup.ts"]` alongside the existing `environment: "jsdom"` and `globals: true`.
4. Run `pnpm --dir web test` — expect `2 passed`.</action>
  <verify>pnpm --dir web test 2>&1 | grep -E "2 passed|Test Files\s+1 passed"</verify>
  <acceptance_criteria>
    - `web/src/App.test.tsx` exists with two `it(...)` blocks asserting headline and health line
    - `web/src/vitest.setup.ts` imports `@testing-library/jest-dom/vitest`
    - `web/vite.config.ts` test block contains `setupFiles: ["./src/vitest.setup.ts"]`
    - `pnpm --dir web test` reports `2 passed`
  </acceptance_criteria>
  <done>Vitest smoke green; future component changes have a regression net.</done>
</task>

<task type="auto">
  <name>Task 3: Embed web/dist via rust-embed + add Dev-mode Vite proxy</name>
  <files>crates/yogurt-server/Cargo.toml, crates/yogurt-server/src/assets.rs, crates/yogurt-server/src/dev_proxy.rs, crates/yogurt-server/src/routes.rs, crates/yogurt-server/src/lib.rs, crates/yogurt-server/tests/embedded.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-0-skeleton.md, crates/yogurt-server/src/routes.rs, crates/yogurt-server/src/lib.rs, web/dist/index.html</read_first>
  <action>Combine superpowers Task 0.7 (asset embedding + Release mode) and Task 0.8 (Dev-mode proxy).

PART A — rust-embed asset fallback (superpowers Task 0.7 Steps 1-8):

1. Append `mime_guess = { workspace = true }` to `[dependencies]` in `crates/yogurt-server/Cargo.toml` (declares the direct dependency explicitly even though `rust-embed`'s `mime-guess` feature pulls it transitively — keeps the import surface explicit per D-14).
2. Confirm `web/dist/index.html` exists (Plan 02 Task 1 produced it). If missing, run `pnpm --dir web build`.
3. Create `crates/yogurt-server/src/assets.rs` per Task 0.7 Step 3 — EXACT code: `#[derive(RustEmbed)] #[folder = "../../web/dist/"] struct WebDist;` and `pub async fn serve_embedded(uri: Uri) -> Response` that strips leading `/`, treats empty path as `index.html`, looks up via `WebDist::get(candidate)`, uses `mime_guess::from_path(candidate).first_or_octet_stream()` for Content-Type, and on `None` falls back to `WebDist::get("index.html")` with `text/html` (SPA fallback). If even `index.html` is missing, return `(StatusCode::NOT_FOUND, "asset not found")`.
4. Modify `crates/yogurt-server/src/routes.rs` per Task 0.7 Step 4: change signature to `pub fn router(mode: Mode) -> Router`. Build router with only `/api/health` registered as a real route, then `match mode { Mode::Release => router.fallback(serve_embedded), Mode::Dev => router.fallback(crate::dev_proxy::proxy_to_vite) }`. DELETE the old `async fn index()` handler entirely AND the `GET /` route from Plan 01 — leaving them as dead code will fail clippy `-D warnings`. Import `crate::assets::serve_embedded` and `crate::Mode`.
5. Create stub `crates/yogurt-server/src/dev_proxy.rs` per Task 0.7 Step 6 (Part B replaces it): `pub async fn proxy_to_vite(_uri: Uri) -> impl IntoResponse { (StatusCode::SERVICE_UNAVAILABLE, "dev proxy not yet implemented (task 0.8)") }`. This stub is overwritten in Part B below but it lets routes.rs compile first.
6. Modify `crates/yogurt-server/src/lib.rs` per Task 0.7 Step 5: add `mod assets;` and `mod dev_proxy;` declarations alongside `mod routes;`. Update the body of `run` to call `routes::router(mode)` instead of `routes::router()`.
7. Create `crates/yogurt-server/tests/embedded.rs` per Task 0.7 Step 7 — `it_serves_embedded_index_in_release_mode` spawns the server on `127.0.0.1:17880` in `Mode::Release`, GETs `/`, asserts the response text contains `yogurt` (which the embedded `index.html` does via `<title>yogurt</title>`).
8. Run `cargo test -p yogurt-server` — both `it_responds_to_health` AND `it_serves_embedded_index_in_release_mode` must pass.

PART B — real Vite proxy (superpowers Task 0.8 Steps 1-3):

9. REPLACE `crates/yogurt-server/src/dev_proxy.rs` with the real implementation from Task 0.8 Step 1. Signature: `pub async fn proxy_to_vite(method: Method, uri: Uri, headers: HeaderMap, body: Body) -> Response`. Const `VITE_BASE: &str = "http://127.0.0.1:5173";`. Construct target URL from `uri.path_and_query()`. Buffer body via `axum::body::to_bytes(body, usize::MAX).await` (BAD_GATEWAY on error). Create `reqwest::Client`, set method + body, forward non-hop-by-hop headers. On upstream `Ok(resp)`: copy status, copy non-hop-by-hop response headers, copy body. On `Err(e)`: log `tracing::warn!(target = %target, ?e, "vite proxy: upstream error — is `pnpm --dir web dev` running?")` and return `502 Bad Gateway` with Content-Type `text/plain` and the EXACT body `format!("yogurt dev proxy: cannot reach vite at {VITE_BASE}\n\nrun: pnpm --dir web dev")`. Implement `fn is_hop_by_hop(name: &HeaderName) -> bool` matching the case-insensitive list: `connection`, `keep-alive`, `proxy-authenticate`, `proxy-authorization`, `te`, `trailers`, `transfer-encoding`, `upgrade`, `host`.
10. Append to `crates/yogurt-server/tests/embedded.rs` the dev-mode test per Task 0.8 Step 2: `it_returns_bad_gateway_in_dev_mode_when_vite_is_down` spawns on `127.0.0.1:17881` in `Mode::Dev`, sleeps 200ms, GETs `/`, asserts status `502` (because no Vite is running).
11. Run `cargo test -p yogurt-server` — all 3 tests pass.
12. Manual two-terminal smoke (document; do not block on it): Terminal 1 `pnpm --dir web dev`, Terminal 2 `cargo run -p yogurt -- start --dev --no-open`, browser to `http://localhost:7878` shows the React app proxied from Vite; editing `web/src/App.tsx` HMR-refreshes; `curl localhost:7878/api/health` still returns the JSON.</action>
  <verify>cargo test --workspace && cargo clippy --all-targets -- -D warnings</verify>
  <acceptance_criteria>
    - `crates/yogurt-server/src/assets.rs` contains `#[derive(RustEmbed)]` with `#[folder = "../../web/dist/"]` AND a `serve_embedded` fallback that returns `index.html` for missing assets
    - `crates/yogurt-server/src/routes.rs` no longer contains `async fn index` (deleted) and uses `router.fallback(...)` based on `Mode`
    - `crates/yogurt-server/src/dev_proxy.rs` contains `const VITE_BASE: &str = "http://127.0.0.1:5173";` and the `is_hop_by_hop` helper with all 9 listed headers
    - `crates/yogurt-server/src/dev_proxy.rs` error body literally contains the substring `pnpm --dir web dev`
    - `crates/yogurt-server/Cargo.toml` `[dependencies]` contains `mime_guess = { workspace = true }`
    - `cargo test -p yogurt-server` runs 3 tests (`it_responds_to_health`, `it_serves_embedded_index_in_release_mode`, `it_returns_bad_gateway_in_dev_mode_when_vite_is_down`) all passing
    - `cargo clippy --all-targets -- -D warnings` passes (no dead `async fn index`)
  </acceptance_criteria>
  <done>Release embeds; Dev proxies; SPA fallback works; clippy clean.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm --dir web build` succeeds and produces `web/dist/index.html`
- [ ] `pnpm --dir web test` reports 2 passed
- [ ] `cargo test --workspace` passes including the new embedded + dev-proxy tests
- [ ] `cargo clippy --all-targets -- -D warnings` clean
- [ ] `cargo run -p yogurt --release -- start --no-open` then `curl -s localhost:7878/` returns HTML containing `<div id="root">` (verifies binary serves embedded asset, not file-system)
</verification>

<success_criteria>
- All 3 tasks completed
- Frontend installs, builds, tests pass
- Release mode serves embedded `web/dist/index.html` with SPA fallback
- Dev mode proxies to Vite at :5173 with actionable 502 when Vite is down
- FOUND-03 (server serves "Hello yogurt" React page via `rust-embed`) demonstrably met
</success_criteria>

<output>
After completion, create `.planning/phases/00-skeleton-foundations/00-02-SUMMARY.md`
</output>
