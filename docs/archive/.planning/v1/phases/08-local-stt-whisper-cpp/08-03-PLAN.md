---
phase: 08-local-stt-whisper-cpp
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - crates/yogurt-server/Cargo.toml
  - crates/yogurt-server/src/api/stt_models.rs
  - crates/yogurt-server/src/api/mod.rs
  - crates/yogurt-server/src/ws.rs
  - crates/yogurt-server/src/meetings/start.rs
  - crates/yogurt-db/migrations/0008_add_stt_model.sql
  - web/src/lib/api/stt.ts
  - web/src/hooks/useModelDownloadProgress.ts
  - web/src/components/settings/Settings.tsx
  - web/src/components/settings/LocalSTTCard.tsx
  - web/src/components/settings/ModelPicker.tsx
  - web/src/components/dialogs/ModelDownloadDialog.tsx
  - web/src/components/settings/ModelPicker.test.tsx
autonomous: false
requirements: [LOCAL-01, LOCAL-03, LOCAL-04]
user_setup: []

must_haves:
  truths:
    - "start_meeting() branches between DeepgramAdapter (cloud) and WhisperLocal (local) based on settings.stt_provider"
    - "WhisperLocal::load is wrapped in tokio::task::spawn_blocking when invoked from start_meeting"
    - "Local STT card replaces the Phase-7 'Coming soon' stub in Settings → Transcription"
    - "Clicking an undownloaded model pill opens the STATE-04 download modal with live progress (bytes / MB-per-sec / ETA)"
    - "On M1 Air, small.en produces first transcript bar in < 3 seconds (LOCAL-04)"
    - "With wifi OFF after model is downloaded, a new meeting transcribes a 30 s sentence end-to-end"
  artifacts:
    - path: "crates/yogurt-server/src/api/stt_models.rs"
      provides: "REST: GET list / POST download / DELETE for whisper.cpp models"
      contains: "Router::new"
    - path: "crates/yogurt-server/src/meetings/start.rs"
      provides: "Branches on settings.stt_provider; refactored helper fn select_stt for testability"
      contains: "WhisperLocal"
    - path: "crates/yogurt-server/src/ws.rs"
      provides: "Three new ws event variants: SttModelDownloadProgress / Complete / Error + broadcaster fan-out methods"
      contains: "SttModelDownloadProgress"
    - path: "crates/yogurt-db/migrations/0008_add_stt_model.sql"
      provides: "ALTER TABLE settings ADD COLUMN stt_model TEXT"
      contains: "stt_model"
    - path: "web/src/components/settings/LocalSTTCard.tsx"
      provides: "Replaces Phase-7 Coming-soon stub; matcha card with ModelPicker + ModelDownloadDialog wiring"
      contains: "LocalSTTCard"
    - path: "web/src/components/dialogs/ModelDownloadDialog.tsx"
      provides: "STATE-04 matcha modal wired to useModelDownloadProgress WS hook"
      contains: "useModelDownloadProgress"
    - path: "web/src/hooks/useModelDownloadProgress.ts"
      provides: "WS subscription that surfaces per-model download progress as React state"
      contains: "stt_model_download_progress"
  key_links:
    - from: "crates/yogurt-server/src/meetings/start.rs"
      to: "yogurt_stt::WhisperLocal"
      via: "Local branch instantiates via spawn_blocking; cloud branch instantiates DeepgramAdapter"
      pattern: "WhisperLocal::load"
    - from: "crates/yogurt-server/src/api/stt_models.rs"
      to: "yogurt_stt::models::download"
      via: "POST /api/stt/models/:name/download spawns background task that calls models::download with progress callback"
      pattern: "models::download"
    - from: "web/src/components/dialogs/ModelDownloadDialog.tsx"
      to: "/ws/app (or shared /ws)"
      via: "useModelDownloadProgress hook subscribes for stt_model_download_progress events"
      pattern: "WebSocket"
    - from: "crates/yogurt-server/src/meetings/start.rs"
      to: "tokio::task::spawn_blocking"
      via: "WhisperLocal::load wrapped in spawn_blocking to avoid blocking the scheduler"
      pattern: "spawn_blocking.*WhisperLocal::load"
---

<objective>
Wire everything end-to-end. REST endpoints + WS event variants on the server. Branch `start_meeting()` on `stt_provider`. Replace the Phase-7 "Coming soon" stub with the real `LocalSTTCard` + `ModelPicker` + `ModelDownloadDialog`. Then run the M1 Air bench acceptance gate — the user runs a real download + meeting, confirms `< 3s` first-bar latency on `small.en`, and verifies offline mode works.

Purpose: This is the LOCAL-04 (< 3s lag on M1 Air) acceptance gate that turns Phase 8 from "code that compiles" into "feature that ships." The bench is the kill criterion: if M1 Air can't hit < 3s on `small.en`, the dual-state preview/settled split needs tuning OR Phase 8 escalates to WhisperKit/ANE (v2 — see CONTEXT deferred ideas).
Output: Live local-STT flow accessible from Settings → Transcription, user-verified on real Apple Silicon hardware.

This plan is NOT autonomous — Task 5 is a `checkpoint:human-verify` that requires the user to run a real model download + meeting on an M1 Air.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-local-stt-whisper-cpp/08-CONTEXT.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-8-local-stt.md
@docs/PRD.md

# Need types from Plans 01 (WhisperLocal, Stt trait), 02 (REGISTRY, download, DownloadProgress).
@.planning/phases/08-local-stt-whisper-cpp/08-01-SUMMARY.md
@.planning/phases/08-local-stt-whisper-cpp/08-02-SUMMARY.md

# Phase 5 settings DB / TanStack Query infra; Phase 7 STATE-04 modal visual spec.
@.planning/phases/05-llm-client-settings-keychain/05-SUMMARY.md
@.planning/phases/07-library-onboarding-states/07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WS event variants + REST endpoints for model management</name>
  <files>crates/yogurt-server/src/ws.rs, crates/yogurt-server/src/api/stt_models.rs, crates/yogurt-server/src/api/mod.rs, crates/yogurt-server/Cargo.toml</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-8-local-stt.md, crates/yogurt-server/src/ws.rs, crates/yogurt-server/src/api/mod.rs, crates/yogurt-server/Cargo.toml</read_first>
  <action>
ORDER: WS variants FIRST (Task 8.8 in source plan), THEN REST handlers (Task 8.7) — source plan recommends this swap because REST handlers reference the broadcaster methods.

Step 1: In `crates/yogurt-server/Cargo.toml` `[dependencies]`:
```toml
yogurt-stt = { path = "../yogurt-stt", features = ["local-stt"] }
futures-util = { workspace = true }
```

Step 2: In `crates/yogurt-server/src/ws.rs`:
- Add three variants to the existing `WsEvent` (or whatever name Phase 3 shipped) enum — match the existing `#[serde(tag = "type", rename_all = "snake_case")]` convention:
  - `SttModelDownloadProgress { model: String, bytes_downloaded: u64, total_bytes: u64, bytes_per_sec: u64, eta_seconds: Option<u64> }`
  - `SttModelDownloadComplete { model: String }`
  - `SttModelDownloadError { model: String, error: String }`
- Add three methods on `Broadcaster` (or whatever Phase 3 named it):
  - `pub async fn send_stt_model_download_progress(&self, model: &str, p: &yogurt_stt::models::DownloadProgress)`
  - `pub async fn send_stt_model_download_complete(&self, model: &str)`
  - `pub async fn send_stt_model_download_error(&self, model: &str, error: &str)`
- Add an inline `#[cfg(test)] mod tests_dl_events` with `dl_progress_serializes_with_tag` — asserts `j["type"] == "stt_model_download_progress"`, `j["model"] == "small.en"`, etc.

Step 3: Create `crates/yogurt-server/src/api/stt_models.rs` per source plan Task 8.7:
- `pub fn router() -> Router<AppState>` mounting:
  - `GET /api/stt/models` → returns `Vec<ModelView>` (name, size_mb, downloaded, intel_supported)
  - `POST /api/stt/models/:name/download` → 202 Accepted, spawns background task calling `yogurt_stt::models::download(spec, |p| broadcaster.send_stt_model_download_progress(...))` then sends Complete/Error on terminal state
  - `DELETE /api/stt/models/:name` → 204 No Content; removes the file
- Each handler returns `Err((StatusCode::NOT_FOUND, ...))` if the model name isn't in REGISTRY.

Step 4: In `crates/yogurt-server/src/api/mod.rs` append `pub mod stt_models;` and `.merge(stt_models::router())` wherever the API router is composed.

Step 5: `cargo test -p yogurt-server ws::tests_dl_events` — pass. `cargo check -p yogurt-server` — clean.
  </action>
  <verify>cargo test -p yogurt-server ws::tests_dl_events passes; cargo check -p yogurt-server compiles cleanly</verify>
  <acceptance_criteria>
    - crates/yogurt-server/src/ws.rs contains `SttModelDownloadProgress`, `SttModelDownloadComplete`, `SttModelDownloadError`
    - crates/yogurt-server/src/api/stt_models.rs contains routes for GET / POST / DELETE
    - api/mod.rs merges the stt_models router
    - Cargo.toml depends on `yogurt-stt` with `features = ["local-stt"]`
    - Inline ws serialization test passes
  </acceptance_criteria>
  <done>WS events serialize correctly; REST endpoints compile and mount</done>
</task>

<task type="auto">
  <name>Task 2: Branch start_meeting on stt_provider + DB migration for stt_model column</name>
  <files>crates/yogurt-server/src/meetings/start.rs, crates/yogurt-db/migrations/0008_add_stt_model.sql</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-8-local-stt.md, crates/yogurt-server/src/meetings/start.rs, .planning/phases/05-llm-client-settings-keychain/05-SUMMARY.md</read_first>
  <action>
Step 1: `rg -n "DeepgramAdapter|fn start_meeting" crates/yogurt-server/src/meetings/start.rs` to locate the Phase 3 implementation. The DeepgramAdapter construction needs to become a branch.

Step 2: Refactor the adapter-selection block into a SYNC helper for testability — `fn select_stt(settings: &Settings) -> Result<SttSpec>` that returns an enum/spec the async caller turns into the actual adapter. This makes the branch unit-testable without needing to mock the broadcast channel.

Then in `start_meeting`:
```rust
let stt: Arc<dyn Stt> = match settings.stt_provider.as_str() {
    "cloud" => Arc::new(yogurt_stt::DeepgramAdapter::new(settings.deepgram_api_key.clone())?),
    "local" => {
        let model_name = settings.stt_model.as_deref().unwrap_or("small.en");
        let spec = yogurt_stt::models::lookup(model_name)
            .ok_or_else(|| anyhow!("unknown local stt model: {model_name}"))?;
        if !yogurt_stt::models::is_downloaded(spec) {
            return Err(anyhow!(
                "local stt model {model_name} is not downloaded; \
                 download it from Settings → Transcription → Local"
            ));
        }
        let path = yogurt_stt::models::model_path(spec)?;
        // Loading the model is blocking — spawn_blocking keeps the runtime healthy. (LOCAL-05)
        let loaded = tokio::task::spawn_blocking(move || yogurt_stt::WhisperLocal::load(path))
            .await
            .map_err(|e| anyhow!("join: {e}"))??;
        Arc::new(loaded)
    }
    other => return Err(anyhow!("unknown stt_provider: {other}")),
};
```

Everything downstream (channel wiring, broadcast pipe) stays identical because both adapters implement the same trait — that is the WHOLE POINT of Phase 8.

Step 3: Create `crates/yogurt-db/migrations/0008_add_stt_model.sql`:
```sql
ALTER TABLE settings ADD COLUMN stt_model TEXT;
```
(Follow Phase 5's exact migration numbering convention; if Phase 5 ended at migration N, use N+1. Pick a sensible number — `0008` is a placeholder based on phase ordering.)

Add `pub stt_model: Option<String>` to the Rust `Settings` struct row, default to `Some("small.en".to_string())` when local is selected.

Step 4: Add unit tests inside `start.rs`:
```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn rejects_unknown_provider() {
        let s = Settings { stt_provider: "satellite".into(), stt_model: None, deepgram_api_key: None, ..Default::default() };
        assert!(select_stt(&s).is_err());
    }

    #[test]
    fn rejects_local_when_model_missing() {
        let s = Settings { stt_provider: "local".into(), stt_model: Some("ghost.en".into()), ..Default::default() };
        let msg = format!("{}", select_stt(&s).unwrap_err());
        assert!(msg.contains("unknown") || msg.contains("not downloaded"));
    }
}
```

Step 5: `cargo test -p yogurt-server meetings::start` — pass.

CRITICAL: confirm `spawn_blocking` wraps `WhisperLocal::load` (LOCAL-05). Grep proof: `rg 'spawn_blocking.*WhisperLocal::load' crates/yogurt-server/src/meetings/start.rs` must match.
  </action>
  <verify>cargo test -p yogurt-server meetings::start — 2 passing tests; grep 'spawn_blocking.*WhisperLocal::load' matches</verify>
  <acceptance_criteria>
    - crates/yogurt-server/src/meetings/start.rs contains a synchronous `select_stt` helper
    - File contains both `DeepgramAdapter` and `WhisperLocal` branch arms
    - `WhisperLocal::load(path)` is invoked inside `tokio::task::spawn_blocking`
    - crates/yogurt-db/migrations/0008_add_stt_model.sql contains `ALTER TABLE settings ADD COLUMN stt_model TEXT`
    - Two unit tests `rejects_unknown_provider` and `rejects_local_when_model_missing` pass
  </acceptance_criteria>
  <done>Branch compiles, tests pass, spawn_blocking wraps the heavy load call</done>
</task>

<task type="auto">
  <name>Task 3: Frontend API client + WS progress hook</name>
  <files>web/src/lib/api/stt.ts, web/src/hooks/useModelDownloadProgress.ts</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-8-local-stt.md, web/src/lib/api/, .planning/phases/05-llm-client-settings-keychain/05-SUMMARY.md</read_first>
  <action>
Step 1: Create `web/src/lib/api/stt.ts` per source plan Task 8.10 Step 1:
- `interface ModelView { name: "tiny.en" | "small.en" | "medium.en" | "large-v3"; size_mb: number; downloaded: boolean; intel_supported: boolean; }`
- `export const sttKeys = { models: ["stt", "models"] as const };`
- `async fetchModels(): Promise<ModelView[]>` — GET `/api/stt/models`.
- `useModels()` — TanStack Query wrapper.
- `useDownloadModel()` — useMutation against `POST /api/stt/models/:name/download`; on success invalidates `sttKeys.models`.
- `useDeleteModel()` — useMutation against `DELETE /api/stt/models/:name`; same invalidation.

Step 2: Create `web/src/hooks/useModelDownloadProgress.ts` per source plan Task 8.10 Step 2:
- `interface DownloadState { bytesDownloaded, totalBytes, bytesPerSec, etaSeconds, complete, error }`
- `ServerEvent` discriminated union for the three new ws events from Task 1.
- `useModelDownloadProgress(model: string | null): DownloadState | null` — opens a WebSocket on mount to `${ws-scheme}://${host}/ws/app` (or whichever path Phase 5 shipped — `rg "ws/app|/ws\"" web/src` to find the actual route), filters messages by `ev.model === model`, surfaces the latest state. On `complete`, invalidates `sttKeys.models` via `useQueryClient`. On `error`, populates the `error` field. Closes the socket on unmount.

If Phase 5 used a different WS path (e.g. `/ws` shared with meetings), MATCH the actual path — the hook just needs to receive `stt_model_download_*` events.
  </action>
  <verify>pnpm --dir web build — tsc + vite both succeed</verify>
  <acceptance_criteria>
    - web/src/lib/api/stt.ts exports `useModels`, `useDownloadModel`, `useDeleteModel`, `sttKeys`, `fetchModels`, `ModelView`
    - web/src/hooks/useModelDownloadProgress.ts exports `useModelDownloadProgress`
    - The hook handles all three event types: `stt_model_download_progress`, `stt_model_download_complete`, `stt_model_download_error`
    - On complete, calls `qc.invalidateQueries({ queryKey: sttKeys.models })`
    - pnpm --dir web build succeeds
  </acceptance_criteria>
  <done>API client + WS hook compile and ship</done>
</task>

<task type="auto">
  <name>Task 4: LocalSTTCard + ModelPicker + ModelDownloadDialog + Settings rewire</name>
  <files>web/src/components/settings/LocalSTTCard.tsx, web/src/components/settings/ModelPicker.tsx, web/src/components/dialogs/ModelDownloadDialog.tsx, web/src/components/settings/Settings.tsx, web/src/components/settings/ModelPicker.test.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-8-local-stt.md, web/src/components/settings/Settings.tsx, .planning/phases/07-library-onboarding-states/07-SUMMARY.md, docs/PRD.md</read_first>
  <action>
Step 1: Create `web/src/components/settings/ModelPicker.tsx` per source plan Task 8.11 Step 1:
- Props: `{ models: ModelView[]; selected: string | null; onSelect: (name) => void; onRequestDownload: (name) => void }`.
- Pill row layout (`flex flex-wrap gap-2`).
- Each pill is a `<button>` with `aria-pressed={isSelected}`.
- If `m.downloaded`: clicking calls `onSelect(m.name)`; if selected, render `✓` glyph.
- If NOT downloaded: clicking calls `onRequestDownload(m.name)`; render `↓` glyph with `title="download (${m.size_mb} MB)"`.
- Intel detection via `navigator.userAgent` regex; if Intel AND `!m.intel_supported`, render a `slow` warning chip (straw color).
- Matcha color tokens per PRD §16.2: selected uses `var(--matcha)` border + `var(--mtsoft)` bg.

Step 2: Create `web/src/components/dialogs/ModelDownloadDialog.tsx` per source plan Task 8.11 Step 2 — this is the STATE-04 modal:
- Native `<dialog>` element with `ref={ref}` and `d.showModal()` on mount when `model` is truthy.
- Wires `useModelDownloadProgress(model)` and `useDownloadModel()`; calls `startDownload(model)` once on open.
- Visual layout per PRD §5.11: 420px wide, white bg, matcha-tinted ↓ avatar circle, Instrument-Serif title showing one of three states (Downloading / Downloaded / Couldn't download), mono caption with `whisper.cpp · {sizeMb} MB`, matcha progress bar, mono byte readout (`{bytes_downloaded} / {total_bytes} · {bytes_per_sec}/s · {eta}s left`), body copy explaining `~/.yogurt/models/`, Cancel + "Run in background" button pair.
- On `progress.complete`, `setTimeout(onClose, 600)` so user sees 100% momentarily.
- V1 limitation per CONTEXT D-13: "Cancel" just closes the dialog — true cancellation deferred. Document inline.

Step 3: Create `web/src/components/settings/LocalSTTCard.tsx` per source plan Task 8.11 Step 3:
- Props: `{ active: boolean; selectedModel: string; onSelectModel: (name) => void; onActivate: () => void }`.
- `useModels()` from TanStack Query.
- Card chrome: matcha border when `active`, line color otherwise.
- Header: "Local · whisper.cpp" title + radio button labeled "Use Local" wired to `onActivate`.
- Body: explanatory text + `<ModelPicker />` + mono footer ("Models download on first use · stored in `~/.yogurt/models`").
- Local state for the download dialog: `const [downloading, setDownloading] = useState<{ name: string; sizeMb: number } | null>(null)`.
- `<ModelDownloadDialog model={downloading?.name ?? null} sizeMb={...} onClose={() => setDownloading(null)} />`.

Step 4: Modify `web/src/components/settings/Settings.tsx`:
- DROP the "Coming soon" badge JSX (Phase 7 STATE-04 was visual-only; Phase 8 wires it).
- Replace the stub with a 2-col grid: `<CloudSTTCard ... />` (existing Phase 5) + `<LocalSTTCard ... />` (new).
- Pass `active={settings.stt_provider === "local"}`, `selectedModel={settings.stt_model ?? "small.en"}`, `onSelectModel={(name) => updateSettings({ stt_provider: "local", stt_model: name })}`, `onActivate={() => updateSettings({ stt_provider: "local", stt_model: settings.stt_model ?? "small.en" })}`.

Step 5: Create `web/src/components/settings/ModelPicker.test.tsx` per source plan Task 8.11 Step 5 — two Vitest tests:
- `calls onSelect when a downloaded model is clicked` — render with `tiny.en` downloaded + selected; click tiny.en; assert `onSelect("tiny.en")`, `onRequestDownload` NOT called.
- `calls onRequestDownload when an undownloaded model is clicked` — click `small.en` (not downloaded); assert `onRequestDownload("small.en")`, `onSelect` NOT called.

Step 6: If `clsx` isn't a Phase-5 dep, add it: `pnpm --dir web add clsx`.

Step 7: `pnpm --dir web test` then `pnpm --dir web build` — both green.
  </action>
  <verify>pnpm --dir web test passes (including 2 new ModelPicker tests); pnpm --dir web build succeeds</verify>
  <acceptance_criteria>
    - web/src/components/settings/Settings.tsx no longer contains the literal string "Coming soon"
    - web/src/components/settings/LocalSTTCard.tsx exports `LocalSTTCard` and renders `ModelPicker` + `ModelDownloadDialog`
    - web/src/components/settings/ModelPicker.tsx handles Intel-warning chip via navigator.userAgent
    - web/src/components/dialogs/ModelDownloadDialog.tsx uses native `<dialog>` element AND calls `useModelDownloadProgress(model)`
    - web/src/components/settings/ModelPicker.test.tsx contains 2 test cases
    - pnpm --dir web test passes; pnpm --dir web build succeeds
  </acceptance_criteria>
  <done>Settings page renders the Local card with a working picker and modal</done>
</task>

<task type="auto">
  <name>Task 5: Pre-bench build + start the dev server</name>
  <files></files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-8-local-stt.md</read_first>
  <action>
Run the full pre-acceptance suite:
```
cargo fmt --all
cargo clippy --all-targets --features local-stt -- -D warnings
cargo test --workspace --features local-stt
pnpm --dir web test
pnpm --dir web build
```
Expected: all green.

Then start the binary in release mode in the background so the bench checkpoint has a server to hit:
```
cargo run -p yogurt --release --features local-stt -- start --no-open
```
Wait for `localhost:7878` to respond `200` to `GET /`. Report the URL in the checkpoint.
  </action>
  <verify>curl -sf http://localhost:7878/ returns 200</verify>
  <acceptance_criteria>
    - cargo test --workspace --features local-stt passes
    - pnpm --dir web build succeeds
    - Yogurt binary is running and serving localhost:7878
  </acceptance_criteria>
  <done>All gates green; server up for manual bench</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 8 end-to-end Local STT path — server running at http://localhost:7878.

What's been built and is ready for verification:
1. WhisperLocal adapter (Metal-accelerated whisper.cpp) gated behind `local-stt` Cargo feature
2. VAD-driven chunking with dual whisper_state (preview greedy + settled beam search)
3. Model download manager with SHA256 verify + Range-header resume
4. REST endpoints + WS progress events for model management
5. Settings → Transcription "Local · whisper.cpp" card with model picker + download modal
6. start_meeting branches on stt_provider; WhisperLocal::load wrapped in spawn_blocking (LOCAL-05)
  </what-built>
  <how-to-verify>
**This bench MUST be run on an M1 Air (not just an M3 Max). LOCAL-04 acceptance is < 3 s lag on M1 Air.**

1. Open http://localhost:7878/settings
2. In the Transcription section, locate the "Local · whisper.cpp" card. Confirm:
   - No "Coming soon" badge anywhere on the page
   - Card matches PRD §5.6 visual spec (matcha tinting, pill row of model names)
   - 4 model pills visible: `tiny.en`, `small.en ↓`, `medium.en ↓`, `large-v3 ↓` (those not yet downloaded show the ↓ glyph)
3. Click the "Use Local" radio button on the Local card. Confirm the matcha border appears.
4. Click the `small.en ↓` pill. Confirm:
   - The STATE-04 download dialog appears (PRD §5.11 matcha modal)
   - Progress bar fills with matcha green
   - Mono caption tick: bytes_downloaded / total_bytes / bytes_per_sec / ETA tick visibly
   - Dialog auto-closes ~600 ms after reaching 100%
5. After download completes:
   - The pill should now render `small.en ✓`
   - `~/.yogurt/models/ggml-small.en.bin` exists; `shasum -a 256 ~/.yogurt/models/ggml-small.en.bin` matches the registry value in `crates/yogurt-stt/src/models.rs` REGISTRY entry for `small.en`
6. Click "+ New meeting". Talk for 30 seconds.
   **CRITICAL — TIME THIS:** First transcript bar should appear in < 3 seconds from the start of speech.
   Confirm: transcript text appears, looks like coherent English (not Deepgram-quality but recognizable).
7. End the meeting. Confirm transcript persisted.
8. **OFFLINE TEST (the kill criterion):**
   - Disable wifi
   - Close the browser
   - Stop and restart `yogurt` (still with `--features local-stt`)
   - Start a new meeting; talk
   - Confirm transcription STILL works with no network access
9. **Intel Mac fallback note (optional, if you have one nearby):** On an Intel Mac, `medium.en` and `large-v3` pills should show a yellow "slow" chip. `small.en` should still work, slower-than-realtime but acceptable.

If first-transcript-bar latency on M1 Air > 3 seconds:
- The dual-state preview/settled split needs tuning, OR
- Phase 8 escalates to WhisperKit/ANE (v2 deferred work — see CONTEXT deferred ideas).
- Type "escalate to v2 — M1 Air bench failed at <Ns>" and we'll capture the bench data and stop the plan.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what went wrong (especially: the exact M1 Air first-bar latency in seconds, and offline-test pass/fail)</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test --workspace --features local-stt` passes
- [ ] `cargo build --no-default-features -p yogurt-stt` completes in < 30 s (no whisper.cpp build) — ROADMAP Phase 8 success criterion #5
- [ ] `pnpm --dir web test` and `pnpm --dir web build` pass
- [ ] User confirmed: M1 Air `small.en` first-transcript-bar latency < 3 s
- [ ] User confirmed: offline test passed (wifi off → new meeting → transcription works)
- [ ] User confirmed: SHA256 of `~/.yogurt/models/ggml-small.en.bin` matches REGISTRY
- [ ] `grep -c 'spawn_blocking' crates/yogurt-server/src/meetings/start.rs` ≥ 1 (around the `WhisperLocal::load` call)
</verification>

<success_criteria>
- All tasks completed
- start_meeting branches between DeepgramAdapter and WhisperLocal based on settings.stt_provider — drop-in adapter swap (LOCAL-01)
- Models download to ~/.yogurt/models with SHA256 verify (LOCAL-03)
- User-verified < 3 s first transcript bar on M1 Air with small.en (LOCAL-04)
- All whisper.cpp invocations are on spawn_blocking (LOCAL-05) — manual code review pass
- Offline test passes: wifi-off → new meeting → transcript still flows
- "Coming soon" badge from Phase 7 STATE-04 is gone; real Local STT card with picker + dialog ships
</success_criteria>

<output>
After completion, create `.planning/phases/08-local-stt-whisper-cpp/08-03-SUMMARY.md`.
</output>
