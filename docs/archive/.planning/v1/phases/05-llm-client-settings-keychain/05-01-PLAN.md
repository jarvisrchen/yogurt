---
phase: 05-llm-client-settings-keychain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/yogurt-llm/Cargo.toml
  - crates/yogurt-llm/src/lib.rs
  - crates/yogurt-llm/src/types.rs
  - crates/yogurt-llm/src/streaming.rs
  - crates/yogurt-llm/tests/mock_server.rs
  - crates/yogurt-llm/tests/streaming.rs
  - crates/yogurt-server/Cargo.toml
  - crates/yogurt-server/src/enhance.rs
  - crates/yogurt-server/tests/enhance_uses_active_provider.rs
autonomous: true
requirements: [LLM-01, LLM-02, LLM-03]
must_haves:
  truths:
    - "yogurt-llm crate exposes the LlmClient trait with both `complete` and `stream` methods"
    - "OpenAiCompatClient honors a caller-supplied base URL (wiremock URI in tests, https://api.minimaxi.chat/v1 in dev) — never a hardcoded host"
    - "SSE streaming yields ChatChunk { delta, done } values terminated by a `done=true` chunk"
    - "Phase 4's enhance handler is rewired to look up the active provider + key via AppState and call OpenAiCompatClient — no MockLLM references remain"
  artifacts:
    - path: crates/yogurt-llm/src/lib.rs
      provides: "LlmClient trait + OpenAiCompatClient adapter + re-exports of ChatMessage/ChatRequest/ChatResponse/ChatChunk"
      contains: "trait LlmClient"
    - path: crates/yogurt-llm/src/streaming.rs
      provides: "SSE → BoxStream<Result<ChatChunk>> via eventsource-stream"
      contains: "Eventsource"
    - path: crates/yogurt-llm/tests/mock_server.rs
      provides: "wiremock non-streaming round-trip + 401 error surfacing"
      contains: "wiremock"
    - path: crates/yogurt-llm/tests/streaming.rs
      provides: "wiremock SSE streaming round-trip"
      contains: "[DONE]"
    - path: crates/yogurt-server/tests/enhance_uses_active_provider.rs
      provides: "End-to-end: provider configured via /api/settings → enhance hits the configured base_url"
      contains: "active_provider"
  key_links:
    - from: crates/yogurt-server/src/enhance.rs
      to: crates/yogurt-llm/src/lib.rs
      via: "yogurt_llm::{LlmClient, OpenAiCompatClient, ChatRequest, ChatMessage}"
      pattern: "use yogurt_llm"
    - from: crates/yogurt-server/src/enhance.rs
      to: crates/yogurt-db (providers + keychain)
      via: "AppState.db.providers + AppState.keys"
      pattern: "providers::active|s\\.keys\\.get"
---

<objective>
Promote Phase 4's hardcoded ~50 LOC `OpenAiCompatClient` into a new `yogurt-llm` crate behind the `LlmClient` trait, add SSE streaming via `eventsource-stream`, and rewire the Phase 4 enhance handler to look up the active provider + API key from `AppState` and call the trait-bounded client.

Purpose: Satisfy LLM-01/02/03 (trait + arbitrary-base-url adapter + working SSE) and unblock Phase 6 chat, which will consume `OpenAiCompatClient::stream()` shipped here.
Output: A no-axum `yogurt-llm` library crate with wiremock-tested non-streaming + streaming paths, and a rewired `enhance.rs` whose tests prove the active provider's `base_url` is actually called.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llm-client-settings-keychain/05-CONTEXT.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md

# Required forward references (read at execute time):
# @.planning/phases/04-augmented-notes-hero/04-SUMMARY.md
# (Phase 4 shipped the MockLLM/hardcoded client; executor must read this to know the exact symbol names to rewire.)

# This plan depends on AppState (built by plan 05-02). Wave 1 publishes both crates;
# the enhance rewire task in this plan reads AppState from plan 05-02's output.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold yogurt-llm crate with non-streaming OpenAI-compatible client</name>
  <files>Cargo.toml, crates/yogurt-llm/Cargo.toml, crates/yogurt-llm/src/lib.rs, crates/yogurt-llm/src/types.rs, crates/yogurt-llm/src/streaming.rs, crates/yogurt-llm/tests/mock_server.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md (Tasks 5.5 + 5.1 Step 1 for workspace dep additions), Cargo.toml (existing workspace structure)</read_first>
  <action>Execute superpowers Task 5.5 verbatim:
1. Append `"crates/yogurt-llm"` (and `"crates/yogurt-db"` placeholder for plan 05-02) to root `Cargo.toml` `[workspace] members`.
2. Append workspace deps to root `Cargo.toml` `[workspace.dependencies]`: `eventsource-stream = "0.2"`, `futures-util = "0.3"`, `wiremock = "0.6"`.
3. Write `crates/yogurt-llm/Cargo.toml` exactly as in superpowers Task 5.5 Step 1 — workspace-inherited package metadata + deps on `reqwest`, `serde`, `serde_json`, `tokio`, `futures-util`, `eventsource-stream`, `async-trait = "0.1"`, `anyhow`, `thiserror = "2"`, `tracing`; dev-deps `wiremock` + tokio test-util.
4. Write `crates/yogurt-llm/src/types.rs` with `ChatMessage` (+ `system`/`user`/`assistant` ctors), `ChatRequest { messages, stream }`, `ChatResponse { content, model }`, `ChatChunk { delta, done }`, plus the private `OpenAiRequest`/`OpenAiResponse`/`OpenAiChoice`/`OpenAiStreamChunk`/`OpenAiStreamChoice`/`OpenAiDelta` wire types.
5. Write `crates/yogurt-llm/src/lib.rs` with `mod streaming; mod types; pub use types::...;` then the `#[async_trait] trait LlmClient { complete; stream; }` and the `OpenAiCompatClient { base_url, api_key, model, http }` struct with `new(base_url, api_key, model)` constructor and `endpoint() -> "{base}/chat/completions"`. Impl `LlmClient::complete` that POSTs the OpenAI shape with `bearer_auth(&self.api_key)`, surfaces non-2xx as `anyhow!("LLM call failed: {status} — {body}")`, and extracts `choices[0].message.content`. `stream()` delegates to `streaming::stream(self, req).await`. Trim trailing `/` from base_url in the constructor. Add `#[doc(hidden)] pub fn http_for_streaming/base_url_for_streaming/api_key_for_streaming/model_for_streaming` accessors so `streaming.rs` can reach private fields without making them `pub(crate)`.
6. Write a placeholder `crates/yogurt-llm/src/streaming.rs` that returns `anyhow::bail!("streaming not yet implemented (Task 5.6)")` — Task 2 in this plan replaces it.
7. Write `crates/yogurt-llm/tests/mock_server.rs` with the two tests from superpowers Task 5.5 Step 2: `it_sends_messages_and_returns_assistant_content` (verifies Bearer auth + JSON body + content extraction) and `it_surfaces_4xx_as_error` (asserts 401 body contains "Invalid API key" surfaces as error).</action>
  <verify>cargo test -p yogurt-llm --test mock_server</verify>
  <acceptance_criteria>
    - `crates/yogurt-llm/Cargo.toml` exists and declares `async-trait = "0.1"` and `eventsource-stream` workspace dep
    - `crates/yogurt-llm/src/lib.rs` contains `pub trait LlmClient` AND `pub struct OpenAiCompatClient` AND `impl LlmClient for OpenAiCompatClient`
    - `crates/yogurt-llm/src/types.rs` contains `pub struct ChatChunk` with `pub delta: String` and `pub done: bool`
    - `OpenAiCompatClient::new` trims trailing `/` from `base_url` (verifiable by reading the source for `trim_end_matches`)
    - `cargo test -p yogurt-llm --test mock_server` passes 2 tests
    - Root `Cargo.toml` workspace members includes `"crates/yogurt-llm"`
  </acceptance_criteria>
  <done>2 wiremock tests pass; trait + non-streaming impl compile.</done>
</task>

<task type="auto">
  <name>Task 2: Implement SSE streaming via eventsource-stream</name>
  <files>crates/yogurt-llm/src/streaming.rs, crates/yogurt-llm/tests/streaming.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md (Task 5.6), crates/yogurt-llm/src/lib.rs (from Task 1)</read_first>
  <action>Execute superpowers Task 5.6 verbatim:
1. Replace `crates/yogurt-llm/src/streaming.rs` with the real impl: POST to `{base_url}/chat/completions` with `stream: true`, `.bytes_stream()`, `.eventsource()`, then `.map(|ev| -> Result<ChatChunk>)` that returns `ChatChunk { delta: "", done: true }` for `[DONE]` events, parses other events as `OpenAiStreamChunk`, and emits `ChatChunk { delta: choice.delta.content.unwrap_or_default(), done: choice.finish_reason.is_some() }`. Surface non-2xx as `anyhow!("LLM stream open failed: {status} — {body}")`. Box the mapped stream.
2. Confirm the `#[doc(hidden)]` accessors `http_for_streaming/base_url_for_streaming/api_key_for_streaming/model_for_streaming` exist on `OpenAiCompatClient` (added in Task 1).
3. Write `crates/yogurt-llm/tests/streaming.rs` with the test from superpowers Task 5.6 Step 1: wiremock serves a hand-crafted SSE body emitting `Hel` / `lo ` / `yogurt.` deltas + finish_reason then `[DONE]`. Test asserts `deltas.join("") == "Hello yogurt."` and `saw_done == true`.</action>
  <verify>cargo test -p yogurt-llm</verify>
  <acceptance_criteria>
    - `crates/yogurt-llm/src/streaming.rs` contains `eventsource_stream::Eventsource` or `.eventsource()` invocation
    - `crates/yogurt-llm/src/streaming.rs` contains a `[DONE]` literal check
    - `cargo test -p yogurt-llm` passes 3 tests total (mock_server: 2 + streaming: 1)
    - Streaming stream is `Send + 'static` (compile-time assertion via `BoxStream<'static, Result<ChatChunk>>` signature)
  </acceptance_criteria>
  <done>3 yogurt-llm tests pass; SSE → ChatChunk stream works end-to-end against wiremock.</done>
</task>

<task type="auto">
  <name>Task 3: Rewire enhance.rs to use OpenAiCompatClient via AppState + acceptance test</name>
  <files>crates/yogurt-server/Cargo.toml, crates/yogurt-server/src/enhance.rs, crates/yogurt-server/tests/enhance_uses_active_provider.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md (Task 5.8), .planning/phases/04-augmented-notes-hero/04-SUMMARY.md (Phase 4's actual enhance handler shape + MockLLM symbol names), .planning/phases/05-llm-client-settings-keychain/05-02-SUMMARY.md (AppState shape from plan 05-02 — read at execute time after plan 05-02 lands)</read_first>
  <action>Execute superpowers Task 5.8:
1. Add `yogurt-llm = { path = "../yogurt-llm" }` to `crates/yogurt-server/Cargo.toml` `[dependencies]`. (Plan 05-02 adds `yogurt-db` + `async-trait`; if this plan runs first in wave 1, also add those — coordinate at execute time by reading the file before editing.)
2. Modify `crates/yogurt-server/src/enhance.rs`:
   - Import `yogurt_llm::{ChatMessage, ChatRequest, LlmClient, OpenAiCompatClient}` and `yogurt_db::providers` (from plan 05-02) and `crate::state::AppState` (from plan 05-02).
   - Replace the existing handler body with: extract `State(s): State<AppState>`; call `providers::active(&s.db)` → 412 PRECONDITION_FAILED if `None`; call `s.keys.get(&provider.id)` → 412 if `None`; build `OpenAiCompatClient::new(provider.base_url, api_key, provider.model)`; render `crate::prompts::enhance_system()` (Phase 4 helper) as the system message; build the user message as `format!("NOTES:\n{}\n\nTRANSCRIPT:\n{}", body.notes, body.transcript)`; call `client.complete(ChatRequest { messages: vec![ChatMessage::system(system), ChatMessage::user(user)], stream: false })`; return `Json(EnhanceResp { enriched_md: resp.content })`.
   - The exact `prompts::enhance_system()` symbol name may differ — substitute the actual Phase 4 symbol after reading `04-SUMMARY.md`.
3. Delete or rewrite any Phase 4 test that asserted MockLLM-specific output. Search: `rg "MockLLM|mock_llm|mock-llm" crates/yogurt-server`. Each test that depended on the mock must either be deleted or rewritten to use the wiremock + in-memory state pattern shown in step 4.
4. Write `crates/yogurt-server/tests/enhance_uses_active_provider.rs` exactly as in superpowers Task 5.8 Step 1: stand up a wiremock `MockServer`; boot the real axum server with `AppState::in_memory()` on port 18010; create a Minimax provider via `POST /api/settings/providers` with `base_url = llm.uri()`; set its key via `POST /api/settings/providers/:id/key`; activate via `POST /api/settings/providers/:id/activate`; trigger `POST /api/enhance` with `{ "notes": "- foo", "transcript": "T: hello" }`; assert response status is success AND `body["enriched_md"]` contains `"bullet from minimax"` (the wiremock-provided string). The `.expect(1)` on the Mock asserts wiremock saw the call.
5. If Phase 4 only created `/api/meetings/:id/enhance` (no test endpoint), add a `POST /api/meetings` seed call before triggering enhance, or hit the meeting endpoint with a seeded fixture — the structural guarantee being tested ("enhance hits the active provider") is the same.</action>
  <verify>cargo test -p yogurt-server --test enhance_uses_active_provider && cargo test -p yogurt-server</verify>
  <acceptance_criteria>
    - `crates/yogurt-server/src/enhance.rs` contains `OpenAiCompatClient::new`
    - `crates/yogurt-server/src/enhance.rs` contains `providers::active`
    - `crates/yogurt-server/src/enhance.rs` contains `s.keys.get` (or equivalent `ApiKeyStore` read)
    - `crates/yogurt-server/src/enhance.rs` contains NO references to `MockLLM`, `mock_llm`, or any hardcoded URL like `api.openai.com` / `api.minimaxi.chat` (base_url MUST come from the DB)
    - `rg "MockLLM|mock_llm" crates/yogurt-server` returns no matches in non-test or non-commented source
    - `crates/yogurt-server/tests/enhance_uses_active_provider.rs` exists and contains `wiremock::MockServer` + `bullet from minimax`
    - `cargo test -p yogurt-server --test enhance_uses_active_provider` passes
    - `cargo test -p yogurt-server` passes full suite (after deleting MockLLM-specific tests)
  </acceptance_criteria>
  <done>Acceptance test green; enhance handler proven to route through the configured base_url; no MockLLM residue in source.</done>
</task>

</tasks>

<verification>
- [ ] `cargo test -p yogurt-llm` passes (3 tests)
- [ ] `cargo test -p yogurt-server --test enhance_uses_active_provider` passes
- [ ] `cargo test -p yogurt-server` passes full suite
- [ ] `cargo build --release` succeeds with the new crate
- [ ] `cargo fmt --all && cargo clippy --all-targets -- -D warnings` clean
- [ ] No hardcoded LLM hostnames remain in `crates/yogurt-server/src/enhance.rs` — base URL is always read from DB
</verification>

<success_criteria>
- All tasks completed
- LlmClient trait + OpenAiCompatClient (non-streaming + SSE streaming) ship in `yogurt-llm`
- Phase 4's enhance handler now reads the active provider from AppState and calls the trait-bounded client
- wiremock acceptance test proves end-to-end provider → enhance flow
- LLM-01, LLM-02, LLM-03 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-llm-client-settings-keychain/05-01-SUMMARY.md`
</output>
