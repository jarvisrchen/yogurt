---
phase: 05-llm-client-settings-keychain
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - crates/yogurt-server/src/api/mod.rs
  - crates/yogurt-server/src/api/settings.rs
  - crates/yogurt-server/src/routes.rs
  - crates/yogurt-server/tests/settings_api.rs
  - web/package.json
  - web/src/lib/queryClient.ts
  - web/src/lib/api/settings.ts
  - web/src/main.tsx
  - web/src/routes/Settings.tsx
  - web/src/components/settings/SidebarNav.tsx
  - web/src/components/settings/ProviderCard.tsx
  - web/src/components/settings/ProviderRow.tsx
  - web/src/components/settings/PresetChip.tsx
autonomous: false
requirements: [SET-01, SET-02, SET-03, SET-04, SET-05, SET-06]
must_haves:
  truths:
    - "/api/settings returns a SettingsView { general, providers, presets } where each provider exposes `api_key_masked` (••••XXXX or null) — never the raw key"
    - "POST /api/settings/providers/:id/key stores the secret only via ApiKeyStore — no plaintext in the DB"
    - "POST /api/settings/providers/:id/activate atomically deactivates other LLM providers and activates the target"
    - "Settings page at /settings renders the 212px sidebar + main content layout"
    - "Sidebar shows Model / Transcription / Audio / General with active = lilac bg + blueberry text"
    - "Sidebar footer shows green matcha 'Local-only · on' pill when no active provider has a non-localhost base_url, plus JetBrains-Mono caption `keys → macOS Keychain` / `data → ~/.yogurt/`"
    - "Active provider card is rendered with a 1.5px blueberry border showing BASE URL + MODEL in mono and API KEY masked as ••••XXXX + green ✓ stored"
    - "Inactive providers stack as plain rows with 'Set active' link"
    - "Preset chips (Ollama, LM Studio, OpenRouter, Minimax, OpenAI) appear with dashed border + '+ Add' link for custom"
  artifacts:
    - path: crates/yogurt-server/src/api/settings.rs
      provides: "Router with GET/PATCH /api/settings + provider CRUD + activate + key-set"
      contains: "api_key_masked"
    - path: crates/yogurt-server/tests/settings_api.rs
      provides: "Integration tests INCLUDING api_responses_never_include_the_raw_api_key (the load-bearing security test)"
      contains: "api_responses_never_include_the_raw_api_key"
    - path: web/src/lib/queryClient.ts
      provides: "TanStack Query singleton with 30s staleTime"
      contains: "QueryClient"
    - path: web/src/lib/api/settings.ts
      provides: "Typed fetch wrappers + SettingsView/ProviderView/Preset types"
      contains: "api_key_masked"
    - path: web/src/routes/Settings.tsx
      provides: "/settings page composition with section state"
      contains: "SidebarNav"
    - path: web/src/components/settings/SidebarNav.tsx
      provides: "212px left rail + Local-only matcha pill + JetBrains-Mono footer caption"
      contains: "Local-only · on"
    - path: web/src/components/settings/ProviderCard.tsx
      provides: "Active provider card with blueberry 1.5px border + masked key + ✓ stored badge"
      contains: "border-[1.5px] border-[var(--blue)]"
    - path: web/src/components/settings/PresetChip.tsx
      provides: "Dashed-border preset chip that clones into a new inactive provider"
      contains: "border-dashed"
  key_links:
    - from: web/src/lib/api/settings.ts
      to: /api/settings
      via: "fetch"
      pattern: "fetch.*\\/api\\/settings"
    - from: crates/yogurt-server/src/api/settings.rs
      to: crates/yogurt-db (providers + settings + keychain)
      via: "yogurt_db::providers + yogurt_db::settings"
      pattern: "yogurt_db::"
    - from: web/src/routes/Settings.tsx
      to: web/src/lib/api/settings.ts
      via: "useQuery({ queryKey: ['settings'], queryFn: settingsApi.get })"
      pattern: "settingsApi"
---

<objective>
Mount the `/api/settings*` REST routes (provider CRUD + key-set + activate + presets) with a load-bearing "no raw key leak" test, wire `@tanstack/react-query` into the React app, and ship the Settings page's left sidebar + Model section (active `ProviderCard` + inactive `ProviderRow`s + dashed preset chips).

Purpose: Satisfy SET-01 (page layout), SET-02 (sidebar sections), SET-03 (footer pill + mono caption), SET-04 (active provider blueberry card with masked key), SET-05 (inactive rows), SET-06 (preset chips).
Output: Working REST API with security tests, TanStack Query infrastructure, typed fetch client, Settings page shell, and the Model section fully wired to the backend.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llm-client-settings-keychain/05-CONTEXT.md
@.planning/phases/05-llm-client-settings-keychain/05-02-SUMMARY.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md

# Phase 1 design tokens are required:
# @web/src/styles/tokens.css (or wherever Phase 1 placed the CSS variables)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount /api/settings* routes + load-bearing no-key-leak integration tests</name>
  <files>crates/yogurt-server/src/api/mod.rs, crates/yogurt-server/src/api/settings.rs, crates/yogurt-server/src/routes.rs, crates/yogurt-server/tests/settings_api.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md (Task 5.7 Steps 3-7), .planning/phases/05-llm-client-settings-keychain/05-02-SUMMARY.md (AppState shape)</read_first>
  <action>Execute superpowers Task 5.7 Steps 3-7:

1. Create or update `crates/yogurt-server/src/api/mod.rs`: `pub mod settings;` (preserve any existing Phase 4 modules).
2. Write `crates/yogurt-server/src/api/settings.rs` per Task 5.7 Step 3:
   - `pub fn router() -> Router<AppState>` mounts:
     - `GET/PATCH /api/settings`
     - `GET/POST /api/settings/providers`
     - `PATCH/DELETE /api/settings/providers/:id`
     - `POST /api/settings/providers/:id/activate`
     - `POST /api/settings/providers/:id/key`
     - `GET /api/settings/presets`
   - `ProviderView` (`Serialize`) MUST include `api_key_masked: Option<String>` — populated from `state.keys.masked(&p.id)`. **No `api_key` field. Ever.**
   - `to_view(state, provider)` constructs the view.
   - `SettingsView { general, providers, presets }` returned by `GET /api/settings`.
   - Handlers: `get_settings`, `patch_settings`, `list_providers`, `list_presets`, `create_provider` (uses `providers::NewProvider` directly), `update_provider` (own `UpdateProviderBody` deserializer), `delete_provider` (also calls `s.keys.delete(&id)` to clean up Keychain, then `providers::delete`), `activate_provider` (calls `providers::set_active` then returns the newly-active view), `set_provider_key` (verifies provider exists, then `s.keys.set(&id, &body.api_key)`, returns 204).
   - `Error { NotFound, Internal(String) }` enum + `From<anyhow::Error>` + `IntoResponse`.
3. Modify `crates/yogurt-server/src/routes.rs` to merge the settings router:
   ```rust
   .merge(crate::api::settings::router())
   ```
   (Preserve Phase 4's enhance router merge.) `router(mode)` returns `Router<AppState>`. The mode-based fallback (Release → `serve_embedded`, Dev → `dev_proxy`) is preserved.
4. Write `crates/yogurt-server/tests/settings_api.rs` per Task 5.7 Step 6 — three tests:
   - `it_lists_seeded_settings_with_no_providers`: boots on port 18001, asserts `general.port == 7878`, no providers, ≥5 presets.
   - `it_creates_a_provider_and_round_trips_via_get`: posts a Minimax provider on 18002, lists, asserts arr[0].id matches AND `api_key_masked == null` (no key set).
   - `api_responses_never_include_the_raw_api_key`: boots on 18003, creates a provider, sets key to `"sk-supersecret-XYZA"`, lists, asserts the response JSON does NOT contain `"sk-supersecret-XYZA"` AND DOES contain `"••••XYZA"`. **This test is load-bearing — never weaken it.**

   Each test uses a `boot(port)` helper that spawns `run_with_state(addr, Mode::Release, state)` on a fresh in-memory `AppState`, sleeps 200ms.</action>
  <verify>cargo test -p yogurt-server --test settings_api</verify>
  <acceptance_criteria>
    - `crates/yogurt-server/src/api/settings.rs` contains `api_key_masked: Option<String>` field on `ProviderView`
    - `crates/yogurt-server/src/api/settings.rs` does NOT contain any `api_key:` field declaration in the serialize-output direction (grep verifies)
    - `crates/yogurt-server/src/api/settings.rs` routes include `/api/settings/providers/:id/activate` and `/api/settings/providers/:id/key`
    - `crates/yogurt-server/tests/settings_api.rs` contains a test named `api_responses_never_include_the_raw_api_key`
    - `cargo test -p yogurt-server --test settings_api` passes all 3 tests
    - Bullet character `••••` (U+2022 × 4) appears in the masked output
  </acceptance_criteria>
  <done>3 integration tests pass; no-key-leak invariant proven; routes mounted.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TanStack Query + typed settings API client</name>
  <files>web/package.json, web/src/lib/queryClient.ts, web/src/lib/api/settings.ts, web/src/main.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md (Task 5.9 in full), web/src/main.tsx (existing)</read_first>
  <action>Execute superpowers Task 5.9:
1. `pnpm --dir web add @tanstack/react-query @tanstack/react-query-devtools`
2. `pnpm --dir web add -D msw`
3. Write `web/src/lib/queryClient.ts` per Task 5.9 Step 2 — `new QueryClient({ defaultOptions: { queries: { staleTime: 30_000, refetchOnWindowFocus: false, retry: 1 }, mutations: { retry: 0 } } })`.
4. Write `web/src/lib/api/settings.ts` per Task 5.9 Step 3:
   - Types: `General`, `ProviderView` (with `api_key_masked: string | null`), `Preset`, `SettingsView`, `NewProvider`, `UpdateProvider`, `AudioDevice`.
   - `http<T>(input, init?)` helper that throws on non-2xx, returns `undefined` on 204, else `res.json()`.
   - `settingsApi.{ get, patch, createProvider, updateProvider, deleteProvider, activateProvider, setProviderKey }`.
   - `audioApi.{ devices }` (re-exported for the Audio section in plan 05-04).
5. Modify `web/src/main.tsx` per Task 5.9 Step 4 — wrap `<App />` in `<QueryClientProvider client={queryClient}>` + render `<ReactQueryDevtools initialIsOpen={false} />` conditionally in dev (`import.meta.env.DEV`).</action>
  <verify>pnpm --dir web build</verify>
  <acceptance_criteria>
    - `web/package.json` contains `@tanstack/react-query` in dependencies
    - `web/package.json` contains `msw` in devDependencies
    - `web/src/lib/api/settings.ts` declares `api_key_masked: string | null` on `ProviderView`
    - `web/src/lib/api/settings.ts` exports `settingsApi` with `setProviderKey`, `activateProvider`, `createProvider`, `deleteProvider`, `updateProvider`, `get`, `patch`
    - `web/src/main.tsx` wraps the root in `<QueryClientProvider>`
    - `pnpm --dir web build` succeeds (tsc + vite)
  </acceptance_criteria>
  <done>TanStack Query installed, queryClient + typed settings client written, app wrapped, build green.</done>
</task>

<task type="auto">
  <name>Task 3: Settings page shell + SidebarNav + Model section components (Card / Row / Chip)</name>
  <files>web/src/routes/Settings.tsx, web/src/components/settings/SidebarNav.tsx, web/src/components/settings/ProviderCard.tsx, web/src/components/settings/ProviderRow.tsx, web/src/components/settings/PresetChip.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md (Task 5.10 Steps 1-4 + 7), web/src/lib/api/settings.ts (from Task 2)</read_first>
  <action>Execute superpowers Task 5.10 Steps 1-4 + 7 (the Settings page composition + sidebar + Model section, NOT the Transcription/Audio/General sections — those land in plan 05-04):

1. Write `web/src/components/settings/SidebarNav.tsx` per Task 5.10 Step 1:
   - 212px wide nav (`w-[212px]`), `bg-[var(--paper)]`, right border `border-neutral-200`.
   - Section buttons for Model / Transcription / Audio / General. Active state: `bg-[var(--blsoft)] text-[var(--blue)]`. Inactive: `text-[var(--ink)] hover:bg-neutral-100`.
   - Footer: green matcha "Local-only · on" pill rendered iff `!providers.some(p => p.is_active && !/localhost|127\.0\.0\.1/.test(p.base_url))`. The pill: `bg-[var(--matcha)] text-white px-2.5 py-1 rounded-full` with a tiny white dot.
   - Below the pill: JetBrains-Mono caption `keys → macOS Keychain` / `data → ~/.yogurt/` — `font-mono text-[10px] text-neutral-500`.
2. Write `web/src/components/settings/ProviderCard.tsx` per Task 5.10 Step 2:
   - `<article className="rounded-xl border-[1.5px] border-[var(--blue)] bg-white p-5 shadow-sm space-y-4">`
   - Header: serif name (`font-serif text-xl`) + "ACTIVE" pill (`text-[10px] font-mono uppercase tracking-wider bg-[var(--blsoft)] text-[var(--blue)]`).
   - Edit toggle (state: `editing` boolean).
   - Two-column grid: BASE URL + MODEL labels (`text-[10px] font-mono uppercase tracking-wider text-neutral-500`); read view = `<code>{base_url}</code>`; edit view = input with `focus:border-[var(--blue)]`.
   - Save button uses `useMutation({ mutationFn: () => settingsApi.updateProvider(provider.id, draft), onSuccess: () => qc.invalidateQueries({ queryKey: ['settings'] }) })`.
   - API KEY section: label "API KEY · in Keychain"; if `provider.api_key_masked` present, render `{masked} <span className="text-[var(--matcha)]">✓ stored</span>` in font-mono; else "No key stored yet."; below: `<input type="password" placeholder="Paste new key…" />` + Save button calling `settingsApi.setProviderKey(provider.id, keyDraft)`.
3. Write `web/src/components/settings/ProviderRow.tsx` per Task 5.10 Step 3:
   - `flex items-center justify-between border-b border-neutral-200 py-3`.
   - Left: name + mono `{base_url}` + `✓ key` (matcha) or `no key` (neutral-400) indicator.
   - Right: "Set active" link (`text-[var(--blue)] hover:underline`) calling `settingsApi.activateProvider(provider.id)`, "Remove" link (`text-neutral-400 hover:text-[var(--strawberry)]`) calling `settingsApi.deleteProvider(provider.id)`.
4. Write `web/src/components/settings/PresetChip.tsx` per Task 5.10 Step 4:
   - `<button className="text-xs font-mono uppercase tracking-wider px-3 py-1.5 rounded-full border border-dashed border-neutral-400 text-neutral-600 hover:border-[var(--blue)] hover:text-[var(--blue)] disabled:opacity-50">`
   - On click, calls `settingsApi.createProvider({ name: preset.name, base_url: preset.base_url, model: preset.default_model })` then invalidates `["settings"]`.
5. Write `web/src/routes/Settings.tsx` per Task 5.10 Step 7 (Model section only — Transcription/Audio/General render placeholder "Coming up in plan 05-04"):
   - `useState<Section>("model")`.
   - `useQuery({ queryKey: ["settings"], queryFn: settingsApi.get })`.
   - Layout: `<div className="flex min-h-screen bg-[var(--paper)]"><SidebarNav ... /><main className="flex-1 max-w-3xl px-10 py-8 space-y-10">...`.
   - Model section: header (serif "Model" + mono "OpenAI-compatible" + paragraph); `active` provider → `<ProviderCard />`; inactive providers → `<ProviderRow />` stack; "Clone a preset →" dashed-border divider + `data.presets.map(p => <PresetChip key={p.name} preset={p} />)` + `+ Add` link.
   - Other sections render a placeholder `<section>Coming up in plan 05-04</section>` (will be replaced).
   - Mount `<Route path="/settings" element={<Settings />} />` in the Phase-3 router file (likely `web/src/App.tsx` or `web/src/router.tsx`). Read the file first.</action>
  <verify>pnpm --dir web build</verify>
  <acceptance_criteria>
    - `web/src/components/settings/SidebarNav.tsx` contains `w-[212px]`
    - `web/src/components/settings/SidebarNav.tsx` contains `Local-only · on` literal
    - `web/src/components/settings/SidebarNav.tsx` contains `keys → macOS Keychain` and `data → ~/.yogurt/`
    - `web/src/components/settings/ProviderCard.tsx` contains `border-[1.5px] border-[var(--blue)]`
    - `web/src/components/settings/ProviderCard.tsx` contains `setProviderKey` (the key-save mutation)
    - `web/src/components/settings/ProviderCard.tsx` contains `✓ stored` literal
    - `web/src/components/settings/ProviderRow.tsx` contains `Set active`
    - `web/src/components/settings/PresetChip.tsx` contains `border-dashed`
    - `web/src/routes/Settings.tsx` mounts the Model section with `ProviderCard` + `ProviderRow` + `PresetChip`
    - `pnpm --dir web build` succeeds
  </acceptance_criteria>
  <done>Settings page shell renders; Model section fully wired to /api/settings; sidebar shows section nav + Local-only pill + Keychain caption.</done>
</task>

<task type="auto">
  <name>Start dev servers for visual verification</name>
  <files>none</files>
  <action>Run `pnpm --dir web dev` in background, run `cargo run -p yogurt -- start --dev --no-open` in background. Wait for both to be ready (web on :5173, axum on :7878).</action>
  <verify>curl -fsS http://localhost:7878/api/settings | head -1</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Settings page Model section + sidebar — running at http://localhost:7878/settings</what-built>
  <how-to-verify>Visit http://localhost:7878/settings (or http://localhost:5173/settings if hot-reload is on Vite). Visual checks:
  1. Left sidebar is 212px wide with Model / Transcription / Audio / General — Model is active (lilac bg + blueberry text).
  2. Sidebar footer shows green matcha "Local-only · on" pill (assuming no active provider has a non-localhost URL — should be true on first run).
  3. Sidebar footer caption renders in JetBrains-Mono: `keys → macOS Keychain` / `data → ~/.yogurt/`.
  4. Model section header: serif "Model" + mono "OpenAI-compatible" label.
  5. Click the "Minimax" preset chip — a new row appears in the inactive providers section showing `Minimax · https://api.minimax.io/v1 · no key`.
  6. Click "Set active" on the Minimax row — it should promote to the top as a 1.5px blueberry-bordered card with serif "Minimax" + "ACTIVE" pill.
  7. Paste any string into the "Paste new key…" input and click Save key — the card should show `••••<last4 of pasted string>` + green `✓ stored`.
  8. The "Local-only · on" matcha pill should DISAPPEAR after step 6 (Minimax has a non-localhost base_url).
  9. Preset chips have dashed borders and are uppercase mono.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- [ ] `cargo test -p yogurt-server --test settings_api` passes
- [ ] `cargo test -p yogurt-server` passes full suite
- [ ] `pnpm --dir web build` succeeds (tsc + vite)
- [ ] Human-verify checkpoint approved
- [ ] `rg "api_key:" crates/yogurt-server/src/api/settings.rs` shows ONLY `api_key_masked` (and `api_key` in `SetKeyBody` for input — never on output)
</verification>

<success_criteria>
- All tasks completed
- /api/settings* routes mounted; no-key-leak test passes
- TanStack Query infrastructure wired
- Settings page renders Model section with sidebar; user can clone presets, activate providers, paste keys
- SET-01, SET-02, SET-03, SET-04, SET-05, SET-06 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-llm-client-settings-keychain/05-03-SUMMARY.md`
</output>
