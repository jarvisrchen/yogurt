---
phase: 05-llm-client-settings-keychain
plan: 04
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - web/src/components/settings/STTPicker.tsx
  - web/src/components/settings/AudioSection.tsx
  - web/src/components/settings/GeneralSection.tsx
  - web/src/routes/Settings.tsx
  - web/src/routes/Settings.test.tsx
autonomous: false
requirements: [SET-07, SET-08, SET-09]
must_haves:
  truths:
    - "Transcription section shows Cloud (selected, blueberry-bordered) + Local (greyed, 'Coming in v1' badge)"
    - "Cloud sub-providers Deepgram (active, blsoft pill) / AssemblyAI / Groq render with neutral borders"
    - "Audio section dropdown lists input devices from GET /api/audio/devices; selecting one persists via PATCH /api/settings"
    - "General section has a Port number input + 'Open browser on start' checkbox; both persist via PATCH /api/settings"
    - "Port change applies on next yogurt start (caption explains)"
    - "Settings persist across page reload (PATCH writes go to SQLite settings KV table)"
    - "Vitest smoke test passes: Settings renders the active provider card with masked key + 'Local-only · on' pill"
  artifacts:
    - path: web/src/components/settings/STTPicker.tsx
      provides: "Cloud + Local card pair, Local disabled with 'Coming in v1' matcha badge"
      contains: "Coming in v1"
    - path: web/src/components/settings/AudioSection.tsx
      provides: "Input device dropdown wired to audioApi.devices + settingsApi.patch"
      contains: "audio_input_device"
    - path: web/src/components/settings/GeneralSection.tsx
      provides: "Port input + open-browser-on-start checkbox, both persist via settingsApi.patch"
      contains: "open_browser_on_start"
    - path: web/src/routes/Settings.test.tsx
      provides: "Vitest smoke test asserting active card masked key + Local-only pill"
      contains: "Local-only"
  key_links:
    - from: web/src/components/settings/AudioSection.tsx
      to: /api/audio/devices
      via: "useQuery({ queryKey: ['audio-devices'], queryFn: audioApi.devices })"
      pattern: "audioApi.devices"
    - from: web/src/components/settings/GeneralSection.tsx
      to: /api/settings (PATCH)
      via: "useMutation calling settingsApi.patch"
      pattern: "settingsApi.patch"
---

<objective>
Complete the Settings page with the Transcription / Audio / General sections, wire the Vitest smoke test, and run the end-to-end acceptance flow (paste a real Minimax key → set active → trigger Re-enhance → verify real provider response).

Purpose: Satisfy SET-07 (Transcription card pair), SET-08 (Audio + General side-by-side), SET-09 (config persists in SQLite — primary source of truth; TOML mirror is deferred).
Output: All four Settings sections render and persist; smoke test green; manual acceptance walk-through documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-llm-client-settings-keychain/05-CONTEXT.md
@.planning/phases/05-llm-client-settings-keychain/05-03-SUMMARY.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md

# Phase 2 audio endpoint is the integration point:
# GET /api/audio/devices is consumed by AudioSection.
</context>

<tasks>

<task type="auto">
  <name>Task 1: STTPicker + AudioSection + GeneralSection components</name>
  <files>web/src/components/settings/STTPicker.tsx, web/src/components/settings/AudioSection.tsx, web/src/components/settings/GeneralSection.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md (Task 5.10 Steps 5-6), web/src/lib/api/settings.ts (audioApi + settingsApi)</read_first>
  <action>Execute superpowers Task 5.10 Steps 5-6:

1. Write `web/src/components/settings/STTPicker.tsx` per Step 5 — two-column grid:
   - Cloud card: `rounded-xl border-[1.5px] border-[var(--blue)] bg-white p-5`. Header: serif "Cloud" + mono "Selected" (blueberry). Paragraph: "Real-time partials, ~2s end-to-end. Audio is sent to the provider." Sub-chips for "Deepgram" / "AssemblyAI" / "Groq" — Deepgram active = `bg-[var(--blsoft)] text-[var(--blue)]`, others = `border border-neutral-300 text-neutral-500`.
   - Local card: `rounded-xl border border-neutral-300 bg-neutral-50 p-5 opacity-60`. Header: serif "Local · whisper.cpp" + matcha "Coming in v1" badge (`bg-[var(--matchasoft)] text-[var(--matcha)] px-2 py-0.5 rounded`). Paragraph: "Fully on-device transcription via Metal-accelerated whisper.cpp." Sub-chips: `tiny.en / small.en / medium.en / large-v3` (font-mono, neutral-400).

2. Write `web/src/components/settings/AudioSection.tsx` per Step 6:
   - `useQuery({ queryKey: ["audio-devices"], queryFn: audioApi.devices })`.
   - `useMutation({ mutationFn: (audio_input_device: string) => settingsApi.patch({ audio_input_device }) })`.
   - `<select>` with `<option value="">System default</option>` + `devices.data?.map(d => <option key={d.id} value={d.id}>{d.name}{d.is_default ? " (default)" : ""}</option>)`.
   - Bottom caption: `text-xs font-mono text-neutral-500`: "System audio is captured via ScreenCaptureKit — no extra setup."
   - Header: serif "Audio".

3. Write `web/src/components/settings/GeneralSection.tsx` per Step 6:
   - Header: serif "General".
   - Port input: `<input type="number" min={1024} max={65535}>` with `defaultValue={general.port}` and `onBlur` that calls `settingsApi.patch({ port })` if the value parsed differently.
   - "Open browser on start" checkbox with `defaultChecked={general.open_browser_on_start}` and `onChange` calling `settingsApi.patch({ open_browser_on_start: e.target.checked })`.
   - Caption: `text-xs font-mono text-neutral-500`: "Port change applies on next `yogurt start`."

4. Modify `web/src/routes/Settings.tsx` to replace the plan-05-03 placeholders:
   - `{section === "transcription" && <section className="space-y-4"><h2 className="font-serif text-2xl">Transcription</h2><STTPicker /></section>}`
   - `{section === "audio" && <AudioSection general={data.general} />}`
   - `{section === "general" && <GeneralSection general={data.general} />}`</action>
  <verify>pnpm --dir web build</verify>
  <acceptance_criteria>
    - `web/src/components/settings/STTPicker.tsx` contains `Coming in v1` literal AND `border-[1.5px] border-[var(--blue)]` for the Cloud card
    - `web/src/components/settings/AudioSection.tsx` contains `audioApi.devices` AND `audio_input_device`
    - `web/src/components/settings/GeneralSection.tsx` contains `open_browser_on_start` AND `min={1024}` AND `max={65535}`
    - `web/src/routes/Settings.tsx` references all three new components
    - `pnpm --dir web build` succeeds
  </acceptance_criteria>
  <done>All four Settings sections render and persist via PATCH /api/settings.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest smoke test for Settings page</name>
  <files>web/src/routes/Settings.test.tsx</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-5-llm-client-and-settings.md (Task 5.10 Step 8), web/src/routes/Settings.tsx</read_first>
  <action>Write `web/src/routes/Settings.test.tsx` per Task 5.10 Step 8:
- Vitest test that mocks `../lib/api/settings` to return a fixed `SettingsView` with one active Minimax provider (`api_key_masked: "••••WXYZ"`) and one preset (`Ollama (local)`).
- Renders `<Settings />` inside a `<QueryClientProvider>` with `retry: false`.
- Asserts: `screen.getByText("Minimax")` AND `screen.getByText("••••WXYZ")` AND `screen.getByText(/Local-only · on/)` are all in the document. (Local-only is true because Minimax's base URL is api.minimax.io, but the mock fixture per the plan uses Minimax as active with the prod URL — DOUBLE CHECK: the plan's test fixture actually has `is_active: true` on a non-localhost provider, which means "Local-only · on" should NOT render. The plan's expected assertion may be wrong; verify against the actual SidebarNav logic. If the mock's active provider has a non-localhost base_url, expect "Local-only · on" to NOT appear. Adjust the test to use `queryByText` for the negative case OR change the fixture so the active provider has a localhost base_url like `http://localhost:11434/v1`.)

The safer fixture: mark Minimax as `is_active: false`, add an Ollama (localhost) preset as the active one — that way "Local-only · on" renders correctly. Adjust the test accordingly to match the SidebarNav semantics implemented in plan 05-03.</action>
  <verify>pnpm --dir web test -- Settings</verify>
  <acceptance_criteria>
    - `web/src/routes/Settings.test.tsx` exists
    - Test imports `<Settings />`, wraps in `<QueryClientProvider>`, mocks `../lib/api/settings`
    - Asserts at least: active provider name renders AND its masked key renders
    - `pnpm --dir web test -- Settings` passes (or `pnpm --dir web test` if no test filter)
  </acceptance_criteria>
  <done>Vitest smoke green; settings page rendering proven without backend.</done>
</task>

<task type="auto">
  <name>Start dev servers for end-to-end acceptance</name>
  <files>none</files>
  <action>Run `pnpm --dir web dev` in background and `cargo run -p yogurt -- start --dev --no-open` in background. Wait until both are ready.</action>
  <verify>curl -fsS http://localhost:7878/api/settings | jq .general.port</verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Settings page (all 4 sections) + end-to-end Minimax round-trip — running at http://localhost:7878/settings</what-built>
  <how-to-verify>Visit http://localhost:7878/settings. Walk through each section:

  1. **Model** (already verified in plan 05-03): preset chips work, provider activation works, masked-key display works.

  2. **Transcription**: Click "Transcription" in the sidebar.
     - Cloud card on left is selected (1.5px blueberry border + "Selected" badge).
     - Sub-pills show "Deepgram" highlighted (blsoft bg + blueberry text) + AssemblyAI / Groq with neutral borders.
     - Local card on right is greyed (opacity-60) with "Coming in v1" matcha badge.
     - whisper.cpp model names (tiny/small/medium/large-v3) appear in mono.

  3. **Audio**: Click "Audio" in the sidebar.
     - Header "Audio" in serif.
     - Input device dropdown populates from `GET /api/audio/devices`. Default option is "System default". If Phase 2 returned real devices, they appear.
     - Selecting a non-default device persists — refresh the page and the same device is still selected.
     - Bottom caption: "System audio is captured via ScreenCaptureKit — no extra setup."

  4. **General**: Click "General" in the sidebar.
     - Port input shows `7878` (the seeded default).
     - "Open browser on start" checkbox is checked.
     - Change port to `7879` and tab out (blur) — refresh the page → port still shows `7879`.
     - Toggle the checkbox → refresh → state preserved.
     - Caption explains "Port change applies on next `yogurt start`."

  5. **End-to-end Minimax round-trip** (acceptance gate):
     - In Model, click the `Minimax` preset chip if not already present, click `Set active`.
     - Paste your real `MINIMAX_API_KEY` from `.env.local` (or any provider's key) into the key input, click `Save key`.
     - Verify the card now shows `••••<last4>` + green `✓ stored`.
     - Open Keychain Access.app, search for "yogurt" — confirm one entry exists under service="yogurt", account=the provider's ULID. Confirm the password matches the pasted key (you'll be prompted to authenticate).
     - Navigate to a Phase-4 meeting (or trigger `/api/enhance` directly via curl with `{"notes":"- topic","transcript":"T: hello world"}`). Verify the response's `enriched_md` is real Minimax-style output, NOT the Phase-4 MockLLM placeholder text.

  6. **No-key-leak check** (CLI):
     - Run `curl -s localhost:7878/api/settings | jq` in a terminal. Verify there is no `sk-` prefix anywhere in the output and that `api_key_masked` is the only key-related field. The raw key must NEVER appear.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Final format + lint + workspace test sweep</name>
  <files>none (verification only)</files>
  <action>Run the cleanup pass per superpowers Task 5.11 Step 5:
1. `cargo fmt --all`
2. `cargo clippy --all-targets -- -D warnings`
3. `cargo test --workspace`
4. `pnpm --dir web test`
5. `pnpm --dir web build`</action>
  <verify>cargo fmt --all -- --check && cargo clippy --all-targets -- -D warnings && cargo test --workspace && pnpm --dir web test && pnpm --dir web build</verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --check` clean
    - `cargo clippy --all-targets -- -D warnings` clean
    - `cargo test --workspace` passes
    - `pnpm --dir web test` passes
    - `pnpm --dir web build` succeeds
  </acceptance_criteria>
  <done>Workspace green, no clippy warnings, frontend builds.</done>
</task>

</tasks>

<verification>
- [ ] All three new components (STTPicker / AudioSection / GeneralSection) render and persist
- [ ] Vitest smoke test passes
- [ ] Human-verify checkpoint approved (all 4 sections + end-to-end Minimax round-trip + no-key-leak)
- [ ] `cargo test --workspace` passes
- [ ] `pnpm --dir web test` passes
- [ ] Workspace fmt + clippy clean
</verification>

<success_criteria>
- All tasks completed
- All four Settings sections (Model / Transcription / Audio / General) ship and persist
- End-to-end Minimax round-trip works manually
- Keychain holds the key under service="yogurt"; no plaintext in DB or API responses
- SET-07, SET-08, SET-09 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-llm-client-settings-keychain/05-04-SUMMARY.md`
</output>
