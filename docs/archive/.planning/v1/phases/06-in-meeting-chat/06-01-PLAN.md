---
phase: 06-in-meeting-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/yogurt-db/src/migrations/V002__chat_messages.sql
  - crates/yogurt-db/src/chat.rs
  - crates/yogurt-db/src/lib.rs
  - crates/yogurt-db/Cargo.toml
  - crates/yogurt-db/tests/chat.rs
  - crates/yogurt-server/src/state.rs
  - crates/yogurt-server/src/lib.rs
  - crates/yogurt-server/src/routes.rs
  - crates/yogurt-server/src/ws.rs
  - crates/yogurt-server/src/api/mod.rs
  - crates/yogurt-server/src/api/chat.rs
  - crates/yogurt-server/src/test_support.rs
  - crates/yogurt-server/Cargo.toml
  - crates/yogurt-server/tests/chat_streaming.rs
autonomous: true
requirements:
  - CHAT-05
  - CHAT-06
  - CHAT-07
must_haves:
  truths:
    - "POST /api/meetings/:id/chat persists a user message and a placeholder assistant message, returning a ulid message_id within 100ms"
    - "Streaming LLM tokens fan out over /ws/meetings/:id as WsEvent::ChatChunk events in order for one message_id, terminating with done=true"
    - "First chat_chunk event arrives within 2s of the POST when the LLM client is reachable (verified via MockLlmClient in CI)"
    - "On meeting view remount, GET /api/meetings/:id/chat returns full chat history in chronological order"
    - "Stream errors surface as a chat_chunk with delta='[stream error: ...]' and done=true"
  artifacts:
    - path: "crates/yogurt-db/src/migrations/V002__chat_messages.sql"
      provides: "chat_messages table with ulid PK, meeting_id FK, role check, content, created_at + index"
      contains: "CREATE TABLE chat_messages"
    - path: "crates/yogurt-db/src/chat.rs"
      provides: "Role enum + ChatMessage struct + insert/list/get/update methods on Db"
      contains: "pub fn insert_chat_message"
    - path: "crates/yogurt-server/src/state.rs"
      provides: "AppState consolidating Db + LlmClient + per-meeting WS broadcast channels"
      contains: "pub struct AppState"
    - path: "crates/yogurt-server/src/api/chat.rs"
      provides: "POST/GET /api/meetings/:id/chat handler + spawn_stream LLM->WS task"
      contains: "pub async fn post_chat"
    - path: "crates/yogurt-server/src/ws.rs"
      provides: "WsEvent::ChatChunk variant"
      contains: "ChatChunk"
  key_links:
    - from: "crates/yogurt-server/src/api/chat.rs"
      to: "yogurt_llm::LlmClient::stream_chat"
      via: "Phase 5 LLM trait — no hardcoded client"
      pattern: "state\\.llm\\.stream_chat"
    - from: "crates/yogurt-server/src/api/chat.rs"
      to: "yogurt_prompts chat-system.md"
      via: "Phase 4 bundled prompts crate accessor"
      pattern: "yogurt_prompts|ChatSystemPrompt|CHAT_SYSTEM"
    - from: "crates/yogurt-server/src/api/chat.rs"
      to: "yogurt_db::Db::insert_chat_message"
      via: "Phase 5 Db handle"
      pattern: "insert_chat_message"
    - from: "crates/yogurt-server/src/api/chat.rs"
      to: "tokio::sync::broadcast::Sender<WsEvent>"
      via: "AppState::channel_for(meeting_id)"
      pattern: "channel_for"
---

<objective>
Build the server-side half of in-meeting chat: V002 `chat_messages` migration, `yogurt-db` CRUD, `AppState` consolidation, `WsEvent::ChatChunk` variant, `POST /api/meetings/:id/chat` handler, and the async LLM-stream → WS-broadcast task. All wired through Phase 5's `LlmClient` trait and Phase 4's `chat-system.md` prompt loader.

Purpose: Provide the streaming chat backend that the frontend (Plan 06-02) consumes. Without this, the pill UI has nothing to send to.
Output: Migration, CRUD module + tests, `AppState` refactor, WS event variant, REST endpoint, streaming task, and `chat_streaming.rs` integration test using a `MockLlmClient`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-in-meeting-chat/06-CONTEXT.md
@docs/superpowers/plans/2026-06-25-yogurt-phase-6-in-meeting-chat.md
@docs/PRD.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: V002 chat_messages migration + yogurt-db CRUD (TDD)</name>
  <files>crates/yogurt-db/src/migrations/V002__chat_messages.sql, crates/yogurt-db/src/chat.rs, crates/yogurt-db/src/lib.rs, crates/yogurt-db/Cargo.toml, crates/yogurt-db/tests/chat.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-6-in-meeting-chat.md, .planning/phases/06-in-meeting-chat/06-CONTEXT.md, crates/yogurt-db/src/lib.rs, crates/yogurt-db/src/migrations/</read_first>
  <action>
Follow Task 6.1 + 6.2 of the superpowers plan exactly.

Step 1: Confirm `crates/yogurt-db/src/migrations/V001__meetings.sql` exists from Phase 5. If absent, abort with a clear error — Phase 5 must merge first.

Step 2: Create `crates/yogurt-db/src/migrations/V002__chat_messages.sql` with this exact schema (lower-case column names, `ulid` IDs, FK ON DELETE CASCADE):
```sql
CREATE TABLE chat_messages (
    id          TEXT PRIMARY KEY,
    meeting_id  TEXT NOT NULL REFERENCES meetings(id) ON DELETE CASCADE,
    role        TEXT NOT NULL CHECK (role IN ('user', 'assistant')),
    content     TEXT NOT NULL,
    created_at  INTEGER NOT NULL
);
CREATE INDEX idx_chat_meeting ON chat_messages(meeting_id, created_at);
```
Prepend the file with a comment noting that the FK depends on Phase 5's `meetings(id)` stub; if it's missing at integration time, drop the FK and re-add via V003 in Phase 7 (per superpowers plan §"What this phase does NOT do").

Step 3: Write the failing integration test FIRST at `crates/yogurt-db/tests/chat.rs` covering: (a) insert + list returns 2 messages in chronological order, (b) messages are scoped per meeting_id. Use `Db::open(":memory:")` and seed meetings via raw `db.conn().execute("INSERT INTO meetings (id, title, started_at) VALUES (?, ?, ?)", ...)`. Test the EXACT helpers `db.insert_chat_message(&msg)`, `db.list_chat_messages(meeting_id)`, with `ChatMessage::new(meeting_id, Role::User|Role::Assistant, content)`.

Step 4: Run `cargo test -p yogurt-db --test chat` — expect compile failure on `yogurt_db::chat` import.

Step 5: Create `crates/yogurt-db/src/chat.rs` with:
  - `pub enum Role { User, Assistant }` with `#[serde(rename_all = "lowercase")]`, `as_str()`, and `from_str(s) -> Result<Self>` (returns Err for unknown roles).
  - `pub struct ChatMessage { id, meeting_id, role, content, created_at }` with `pub fn new(meeting_id, role, content) -> Self` minting a ulid + `chrono::Utc::now().timestamp_millis()`.
  - `impl crate::Db { pub fn insert_chat_message, pub fn list_chat_messages, pub fn get_chat_message, pub fn update_chat_message_content }` using `self.conn()` (rusqlite Connection).
  - `list_chat_messages` MUST `ORDER BY created_at ASC, id ASC` (id as tiebreaker for same-millis inserts).

Step 6: In `crates/yogurt-db/src/lib.rs` add `pub mod chat;`. Confirm `Db::conn(&self) -> &rusqlite::Connection` is public; expose it if not.

Step 7: Ensure `Cargo.toml` has `ulid = "1"` and `chrono = { version = "0.4", default-features = false, features = ["clock"] }`. Add if missing.

Step 8: Run `cargo test -p yogurt-db --test chat` — expect both tests PASS. Then run `cargo test -p yogurt-db --lib` to confirm migration loader picks up V002 without breaking Phase 5 tests.

Do NOT modify Phase 5's `meetings.rs` or `V001__meetings.sql`.
  </action>
  <verify>cargo test -p yogurt-db --test chat && cargo test -p yogurt-db --lib</verify>
  <acceptance_criteria>
    - File `crates/yogurt-db/src/migrations/V002__chat_messages.sql` contains the string "CREATE TABLE chat_messages"
    - V002 SQL contains "REFERENCES meetings(id)" AND "ON DELETE CASCADE"
    - V002 SQL contains "CREATE INDEX idx_chat_meeting ON chat_messages(meeting_id, created_at)"
    - `crates/yogurt-db/src/chat.rs` contains `pub enum Role` with both `User` and `Assistant`
    - `crates/yogurt-db/src/chat.rs` contains `pub fn insert_chat_message`, `pub fn list_chat_messages`, `pub fn get_chat_message`, `pub fn update_chat_message_content`
    - `crates/yogurt-db/src/chat.rs` contains `ORDER BY created_at ASC`
    - `crates/yogurt-db/src/lib.rs` contains `pub mod chat;`
    - `crates/yogurt-db/tests/chat.rs` defines `it_inserts_and_lists_messages_in_chronological_order` and `it_scopes_messages_by_meeting`
    - `cargo test -p yogurt-db --test chat` reports 2 passed, 0 failed
  </acceptance_criteria>
  <done>V002 migration loads cleanly, CRUD methods are exposed on Db, both integration tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: AppState extraction + WsEvent::ChatChunk variant</name>
  <files>crates/yogurt-server/src/state.rs, crates/yogurt-server/src/lib.rs, crates/yogurt-server/src/routes.rs, crates/yogurt-server/src/ws.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-6-in-meeting-chat.md, crates/yogurt-server/src/lib.rs, crates/yogurt-server/src/routes.rs, crates/yogurt-server/src/ws.rs</read_first>
  <action>
Follow Task 6.3 + 6.4 of the superpowers plan.

Step 1: Inspect existing state passing — `grep -rn "with_state\|Extension::" crates/yogurt-server/src/` — to identify the Phase 3 broadcast::Sender + Phase 5 Db Extension hookups.

Step 2: Create `crates/yogurt-server/src/state.rs`:
```rust
use std::collections::HashMap;
use std::sync::Arc;
use tokio::sync::{broadcast, RwLock};
use yogurt_db::Db;
use yogurt_llm::LlmClient;
use crate::ws::WsEvent;

#[derive(Clone)]
pub struct AppState {
    pub db: Arc<Db>,
    pub llm: Arc<dyn LlmClient>,
    pub ws_channels: Arc<RwLock<HashMap<String, broadcast::Sender<WsEvent>>>>,
}

impl AppState {
    pub fn new(db: Arc<Db>, llm: Arc<dyn LlmClient>) -> Self { /* ... */ }
    pub async fn channel_for(&self, meeting_id: &str) -> broadcast::Sender<WsEvent> {
        // Read-lock fast path; write-lock + or_insert_with(|| broadcast::channel::<WsEvent>(256).0) slow path
    }
}
```

Step 3: In `crates/yogurt-server/src/lib.rs` build `AppState::new(db.clone(), llm.clone())` and pass it to `routes::router(state.clone(), mode)`. Use `Router::with_state(state)`.

Step 4: Migrate Phase 3 WS handler + Phase 5 meetings handlers from `Extension(...)` to `State(state): State<AppState>`. The WS handler now fetches its broadcaster via `state.channel_for(&meeting_id).await` instead of receiving a top-level `broadcast::Sender`. Keep the diff narrow — change only signatures and the channel acquisition.

Step 5: In `crates/yogurt-server/src/ws.rs`, append the `ChatChunk` variant to the existing `WsEvent` enum, preserving all Phase 3 variants (Transcript, NotesSynced, EnhanceProgress, etc.):
```rust
ChatChunk {
    message_id: String,
    delta: String,
    #[serde(default)]
    done: bool,
},
```
Enum must already use `#[serde(tag = "type", rename_all = "snake_case")]` so the on-wire `"type"` becomes `"chat_chunk"`.

Step 6: Confirm the WS forward loop is generic (`rx.recv().await` → `Message::Text(serde_json::to_string(&event)?)`). If it pattern-matches per variant, refactor to a generic forward.

Step 7: Add a unit test in `ws.rs`:
```rust
#[cfg(test)]
mod tests {
    use super::*;
    #[test]
    fn it_serializes_chat_chunk_with_expected_keys() {
        let ev = WsEvent::ChatChunk { message_id: "01HXMSG".into(), delta: "hello".into(), done: false };
        let json = serde_json::to_value(&ev).unwrap();
        assert_eq!(json["type"], "chat_chunk");
        assert_eq!(json["message_id"], "01HXMSG");
        assert_eq!(json["delta"], "hello");
        assert_eq!(json["done"], false);
    }
}
```

Step 8: Run `cargo test -p yogurt-server` and ensure ALL pre-existing tests (Phase 3 WS, Phase 5 meetings) still pass after the refactor.
  </action>
  <verify>cargo test -p yogurt-server</verify>
  <acceptance_criteria>
    - `crates/yogurt-server/src/state.rs` contains `pub struct AppState` with `db: Arc<Db>`, `llm: Arc<dyn LlmClient>`, `ws_channels: Arc<RwLock<HashMap<String, broadcast::Sender<WsEvent>>>>`
    - `crates/yogurt-server/src/state.rs` contains `pub async fn channel_for`
    - `crates/yogurt-server/src/ws.rs` contains `ChatChunk` variant inside `WsEvent`
    - `crates/yogurt-server/src/ws.rs` contains test `it_serializes_chat_chunk_with_expected_keys`
    - `grep -rn "State<AppState>" crates/yogurt-server/src/` returns at least one match (handlers migrated)
    - `cargo test -p yogurt-server` passes (existing + new ws shape test)
  </acceptance_criteria>
  <done>AppState is the single shared-state type; ChatChunk variant serializes with `"type":"chat_chunk"`; all prior tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: POST /api/meetings/:id/chat + spawn_stream LLM-to-WS bridge (TDD)</name>
  <files>crates/yogurt-server/src/api/chat.rs, crates/yogurt-server/src/api/mod.rs, crates/yogurt-server/src/routes.rs, crates/yogurt-server/src/test_support.rs, crates/yogurt-server/src/lib.rs, crates/yogurt-server/Cargo.toml, crates/yogurt-server/tests/chat_streaming.rs</files>
  <read_first>docs/superpowers/plans/2026-06-25-yogurt-phase-6-in-meeting-chat.md, crates/yogurt-server/src/api/mod.rs, crates/yogurt-server/src/routes.rs, crates/yogurt-llm/src/lib.rs</read_first>
  <action>
Follow Task 6.5 + 6.6 of the superpowers plan.

Step 1: Write the failing integration test FIRST at `crates/yogurt-server/tests/chat_streaming.rs` covering two cases:
  - `it_returns_message_id_on_post_chat`: POST returns 200 with body `{ "message_id": "<26-char ulid>" }`.
  - `it_streams_chat_chunks_in_order_over_ws`: Connect WS to `/ws/meetings/:id`, POST `{ content: "say something" }`, collect `chat_chunk` events for 2s; assert concatenated `delta`s equal `"hello world."` (mock LLM returns `["hello ", "world", "."]`), assert at least one event has `done: true`, all events share the returned `message_id`.

Both tests use `yogurt_server::test_support::run_with_mock_llm(addr, &["hello ", "world", "."])` and `yogurt_server::test_support::seed_meeting(meeting_id)` — feature-gated under `#[cfg(any(test, feature = "test-support"))]`.

Step 2: Create `crates/yogurt-server/src/test_support.rs` with:
  - `pub async fn run_with_mock_llm(addr: SocketAddr, chunks: &'static [&'static str]) -> anyhow::Result<()>` — boots an axum server with a `MockLlmClient` (impl of `yogurt_llm::LlmClient`) whose `stream_chat` returns `futures_util::stream::iter(chunks.iter().map(|s| Ok(s.to_string())))`.
  - `pub async fn seed_meeting(id: &str)` — opens the shared in-memory Db and inserts a meeting row.
  - Feature-gate with `[features] test-support = []` in `Cargo.toml`; expose `pub mod test_support;` in `lib.rs` behind `#[cfg(any(test, feature = "test-support"))]`.

Add to `[dev-dependencies]` of `crates/yogurt-server/Cargo.toml`:
```toml
futures-util = "0.3"
tokio-tungstenite = "0.24"
reqwest = { version = "0.12", features = ["json"] }
```

Step 3: Run `cargo test -p yogurt-server --test chat_streaming` — expect compile failure (handler doesn't exist).

Step 4: Create `crates/yogurt-server/src/api/chat.rs` with:
  - `ChatRequest { content: String }` (deserialize), `ChatResponse { message_id: String }` (serialize), `ChatHistoryResponse { messages: Vec<ChatMessage> }`.
  - `pub async fn post_chat(State(state), Path(meeting_id), Json(req))` —
      1. 400 if `req.content.trim().is_empty()`.
      2. Insert user `ChatMessage::new(&meeting_id, Role::User, req.content.clone())`.
      3. Insert empty assistant `ChatMessage::new(&meeting_id, Role::Assistant, "")` — capture its id.
      4. Call `spawn_stream(state.clone(), meeting_id.clone(), assistant_msg.id.clone()).await`.
      5. Return `Json(ChatResponse { message_id: assistant_msg.id })`.
  - `pub async fn get_chat_history(State(state), Path(meeting_id))` returns `Json(ChatHistoryResponse { messages: state.db.list_chat_messages(...) })`.
  - `pub async fn spawn_stream(state: AppState, meeting_id: String, message_id: String)` that spawns a tokio task which:
      1. Loads the system prompt via `yogurt_prompts` (use the exact symbol Phase 4 exposes — likely `yogurt_prompts::CHAT_SYSTEM` or `ChatSystemPrompt::load()`; confirm via `grep -rn "CHAT_SYSTEM\|ChatSystemPrompt\|chat-system" crates/yogurt-prompts/src/`).
      2. Loads transcript via `state.db.get_meeting_transcript(&meeting_id)`; fall back to empty string with `tracing::warn!` on error.
      3. Loads chat history via `state.db.list_chat_messages(&meeting_id)`; filter out the empty placeholder assistant row matching `message_id`.
      4. Builds `Vec<yogurt_llm::Message>`: system prompt, then system "TRANSCRIPT SO FAR (most recent at bottom):\n\n{transcript}", then alternating history turns mapped via `Role::User → MessageRole::User`, `Role::Assistant → MessageRole::Assistant`.
      5. Gets `tx = state.channel_for(&meeting_id).await`.
      6. Calls `state.llm.stream_chat(messages).await`. On error: send `WsEvent::ChatChunk { message_id, delta: "\n\n[stream error: {e}]", done: true }` and return.
      7. Otherwise iterate stream: accumulate full text, send `WsEvent::ChatChunk { message_id: message_id.clone(), delta, done: false }` per chunk. On chunk-error, break.
      8. Persist accumulated text via `state.db.update_chat_message_content(&message_id, &accumulated)`.
      9. Send terminal `WsEvent::ChatChunk { message_id, delta: String::new(), done: true }`.

Step 5: Wire into `crates/yogurt-server/src/api/mod.rs` with `pub mod chat;` and add route in `routes.rs`:
```rust
.route("/api/meetings/:id/chat",
    axum::routing::post(api::chat::post_chat).get(api::chat::get_chat_history))
```

Step 6: Run `cargo test -p yogurt-server --test chat_streaming` — both tests must PASS. The streaming test asserts collected text equals `"hello world."` exactly, so make sure the terminal `done: true` chunk emits AFTER all content chunks.

Critical: Do NOT instantiate an LLM client directly inside `spawn_stream` — always go through `state.llm.stream_chat(...)`. Do NOT hardcode the system prompt — always load from `yogurt_prompts`.

If `db.get_meeting_transcript` does not exist in Phase 5 yet, document the gap in the commit message and have `spawn_stream` fall back to `String::new()` — Phase 7 (library) will fill the gap.
  </action>
  <verify>cargo test -p yogurt-server --test chat_streaming && cargo test -p yogurt-server</verify>
  <acceptance_criteria>
    - `crates/yogurt-server/src/api/chat.rs` contains `pub async fn post_chat`, `pub async fn get_chat_history`, `pub async fn spawn_stream`
    - `crates/yogurt-server/src/api/chat.rs` contains `state.llm.stream_chat` (uses Phase 5 trait, not hardcoded)
    - `crates/yogurt-server/src/api/chat.rs` references `yogurt_prompts` (loads chat-system.md from Phase 4 crate, not inline)
    - `crates/yogurt-server/src/api/chat.rs` contains `WsEvent::ChatChunk`
    - `crates/yogurt-server/src/api/chat.rs` contains `done: true` terminal-chunk emission
    - `crates/yogurt-server/src/routes.rs` contains `/api/meetings/:id/chat` and `api::chat::post_chat`
    - `crates/yogurt-server/tests/chat_streaming.rs` contains both `it_returns_message_id_on_post_chat` and `it_streams_chat_chunks_in_order_over_ws`
    - `crates/yogurt-server/src/test_support.rs` exists and exposes `run_with_mock_llm` + `seed_meeting`
    - `cargo test -p yogurt-server --test chat_streaming` reports 2 passed
  </acceptance_criteria>
  <done>POST returns ulid immediately; mock LLM tokens stream out as ordered chat_chunk events terminating with done=true; full server suite still green.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p yogurt-db --test chat` passes (2 tests)
- [ ] `cargo test -p yogurt-server` passes (Phase 3 + 5 + new ws shape test)
- [ ] `cargo test -p yogurt-server --test chat_streaming` passes (2 tests)
- [ ] `grep -rn "state.llm.stream_chat" crates/yogurt-server/src/` returns at least one match (no hardcoded LLM)
- [ ] `grep -rn "yogurt_prompts" crates/yogurt-server/src/api/chat.rs` returns at least one match (prompt loaded from Phase 4 crate)
- [ ] `cargo build --workspace` succeeds with no warnings introduced
</verification>

<success_criteria>
- All 3 tasks completed with their atomic commits
- V002 migration applies cleanly to a fresh DB
- POST returns `message_id` < 100ms; first WS chat_chunk arrives < 2s (asserted by `it_streams_chat_chunks_in_order_over_ws` against MockLlmClient)
- LlmClient trait from Phase 5 is the only LLM call path; chat-system.md is loaded via Phase 4's `yogurt-prompts` crate
- CHAT-05, CHAT-06, CHAT-07 fully implemented on the server side
</success_criteria>

<output>
After completion, create `.planning/phases/06-in-meeting-chat/06-01-SUMMARY.md` documenting:
- Exact `yogurt_prompts` symbol used (e.g. `CHAT_SYSTEM` vs `ChatSystemPrompt::load()`)
- Whether `db.get_meeting_transcript` existed or required a fallback
- Any deltas from the superpowers plan (e.g. variant names already taken in WsEvent)
- Confirmation that `MockLlmClient` is feature-gated and not pulled into release builds
</output>
