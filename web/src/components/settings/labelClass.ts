/**
 * Shared class set for a small-caps "eyebrow" field/section label — a group
 * name like BASE URL, MODEL, Today, Labels, Microphone. Sans (Manrope);
 * CSS `uppercase` does the case transform so the JSX text itself can stay
 * naturally cased.
 */
export const LABEL =
  "text-[11px] font-semibold uppercase tracking-[0.06em] text-mut";
